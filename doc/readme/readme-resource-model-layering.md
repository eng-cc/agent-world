# README 资源模型口径修订：内建最小资源 + 模块扩展资源（设计文档）

## 目标
- 修订 `README.md` 的资源描述，使其与当前工程实现一致，避免“文档承诺资源维度”与“代码内建资源枚举”产生歧义。
- 明确 Agent World 的资源分层：
  - 内建最小资源（由 runtime 共识层直接校验与计费）；
  - 模块扩展资源（由 WASM 模块在世界内定义与演化）。
- 回答并固化策略：`Compound/Hardware/Data` 当前是否需要下沉到 WASM 模块定义。

## 范围
- 修改 `README.md` 中“关键资源”和“资源约束”的表述。
- 在 README 中新增资源分层说明（内建资源与模块资源的职责边界）。
- 不修改 `ResourceKind` 枚举，不调整 runtime/simulator 序列化结构，不新增兼容迁移。

## 接口/数据
- 现有内建资源类型（代码事实）：`Electricity` / `Compound` / `Hardware` / `Data`。
- 资源分层策略：
  - 内建资源：保留为最小可验证经济底座，用于全局守恒、跨模块结算、运行时计费。
  - 模块扩展资源：通过 WASM 模块状态、事件和治理动作定义，不进入内建枚举。
- README 将明确：算力/存储/带宽可作为系统成本维度，在当前实现中由内建资源组合近似表达，并可继续在模块层细分。

## 里程碑
- M1：完成设计文档与项目管理文档建档。
- M2：完成 README 口径修订并通过基础校验。

## 风险
- 术语风险：旧讨论中“算力/存储/带宽”常被理解为独立硬资源；修订后需强调“抽象成本维度”含义。
- 认知风险：若不说明分层，容易误解为“必须不断扩展内建枚举”。
- 扩展风险：未来若确需新增内建资源，应坚持“最小内核”原则，避免破坏可演化性。
