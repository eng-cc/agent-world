# 2026-03-13 Devlog

## 00:36:12 CST / producer_system_designer
- 完成内容：回写 `PRD-WORLD_SIMULATOR-037` 的执行边界，明确 `OpenClaw(Local HTTP)` 在 `test_tier_required` 的当前可执行子集为 `wait` / `wait_ticks` / `move_agent`；`speak_to_nearby` / `inspect_target` / `simple_interact` 改记为保留 intent，待 runtime 补齐动作语义后再进入真正可执行范围。同时补充 `unsupported_semantic_action` 的失败策略：required 路径统一降级为 `Wait` 并写入结构化错误，禁止把未落地的语义伪装为已执行成功。
- 遗留事项：后续仍需由 `agent_engineer` / `runtime_engineer` 联动补齐三类保留 intent 的 runtime `Action` 语义与 adapter 映射；在此之前，`TASK-WORLD_SIMULATOR-121` 继续保持 pending，且 parity 门禁不变。

## 00:36:12 CST / agent_engineer
- 完成内容：继续推进 `TASK-WORLD_SIMULATOR-121`，在 `crates/agent_world/src/simulator/tests/openclaw_adapter.rs` 补充 `OpenClawAdapter -> ProviderBackedAgentBehavior -> runtime -> feedback` 的真实闭环回归，验证当前 `move_agent` 子集可通过 mock local HTTP provider 驱动 agent 完成位移并回写 feedback；同时补充“保留 intent 被错误编码为已实现动作语义时，adapter/runner 必须降级为 `Wait`”的防回归测试，锁定当前边界行为。
- 遗留事项：下一步继续评估 `speak_to_nearby` / `inspect_target` / `simple_interact` 需要新增哪些 runtime `Action` / 事件 / trace 字段，确认最小实现路径后再继续收口 `TASK-WORLD_SIMULATOR-121`。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p agent_world openclaw_adapter -- --nocapture`，5 项 OpenClaw adapter 定向测试全部通过。


## 09:35:56 CST / agent_engineer
- 完成内容：继续推进 `TASK-WORLD_SIMULATOR-121` 并实际收口 phase-1 白名单：在 simulator 中新增 `Action::SpeakToNearby`、`Action::InspectTarget`、`Action::SimpleInteract` 及对应 `WorldEventKind::AgentSpoke`、`WorldEventKind::TargetInspected`、`WorldEventKind::SimpleInteractionPerformed`，为三类低频交互补齐最小 runtime 语义（校验 + 事件）；同步更新 `OpenClawAdapter` 的 action_ref 映射与 replay/submitter 规则，并在 `crates/agent_world/src/simulator/tests/openclaw_adapter.rs` 补齐六类白名单动作的 mock local HTTP + runner/feedback 闭环回归。
- 遗留事项：`TASK-WORLD_SIMULATOR-121` 的代码实现已完成，但最终体验签收仍受 `PRD-WORLD_SIMULATOR-038` parity 门禁约束；当前继续切向 `TASK-WORLD_SIMULATOR-122`，补 launcher / viewer 的 provider 状态、最近延迟与最近动作/trace 摘要展示。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p agent_world openclaw_adapter -- --nocapture`，8 项 OpenClaw adapter / runner 定向测试全部通过。

## 09:35:56 CST / viewer_engineer
- 完成内容：为 `TASK-WORLD_SIMULATOR-122` 预热，在 `crates/agent_world_proto/src/viewer.rs` 与 `crates/agent_world/src/viewer/protocol.rs` 新增 `AgentSpoke`、`TargetInspected`、`SimpleInteractionPerformed` 三类 Viewer 事件筛选枚举与匹配规则，确保后续 Viewer 面板可按新增 OpenClaw phase-1 事件类型过滤最近动作；同步补齐 viewer/proto 定向测试。
- 遗留事项：下一步继续把 provider 最近延迟、最后错误、最近动作/trace 摘要真正接到 launcher / viewer 面板，而不仅是先补协议筛选入口。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p agent_world viewer_event_kind_matches_world_event_kind -- --nocapture` 与 `env -u RUSTC_WRAPPER cargo test -p agent_world_proto viewer -- --nocapture`，相关 Viewer 协议/枚举测试通过。

## 11:58:00 CST / viewer_engineer
- 完成内容：完成 `TASK-WORLD_SIMULATOR-122` 的 UI 闭环，在 `agent_world_client_launcher` 为 `OpenClaw(Local HTTP)` 增加顶栏 provider 状态徽标与 probe 诊断摘要（info/health/total 延迟、队列深度、最近错误）；在 `agent_world_viewer` 右侧详情面板新增 `Provider Debug` 卡片，汇总最近 provider/model、最近延迟、最近动作/trace 摘要，并提供 `全部 / 仅 OpenClaw / 仅错误` 调试筛选入口。同步补齐 provider debug 文本测试与 launcher probe 断言。
- 遗留事项：下一步切到 `PRD-WORLD_SIMULATOR-038` 的真实 `OpenClaw(Local HTTP)` 单 NPC parity 试点；`T5` 仍需真实本机 provider 进程配合，现阶段只完成 required 层 UI/诊断铺垫。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher openclaw -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p agent_world_viewer provider_debug_summary_filters_openclaw_and_errors -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p agent_world_viewer update_ui_populates_agent_selection_details_with_llm_trace -- --nocapture`，均通过。

## 11:58:00 CST / producer_system_designer
- 完成内容：回写 `doc/world-simulator/project.md` 与 `doc/world-simulator/llm/llm-openclaw-local-http-provider-integration-2026-03-12.project.md`，将 `TASK-WORLD_SIMULATOR-122 / T4` 标记完成，并把下一优先项切换为 `PRD-WORLD_SIMULATOR-038` parity 采证与 `T5` experimental 试点准备。
- 遗留事项：待真实本机 `OpenClaw(Local HTTP)` 可用后，继续收集 `P0` parity 证据并决定是否放宽 experimental 约束。

## 13:12:00 CST / agent_engineer
- 完成内容：为 `PRD-WORLD_SIMULATOR-038 / T4` 新增可执行 parity bench 入口 `crates/agent_world/src/bin/world_openclaw_parity_bench.rs` 与批处理脚本 `scripts/openclaw-parity-p0.sh`，支持 builtin / `openclaw_local_http` 单样本运行、落盘 `raw/*.jsonl`、单样本 summary、聚合 `combined.csv`、`failures.md` 与 `scorecard-links.md`；脚本默认面向 `P0`，可直接对接真实本机 `OpenClaw(Local HTTP)` 做 experimental 采证。
- 遗留事项：当前仅完成 bench harness 与 openclaw-only mock smoke；真实 builtin vs OpenClaw 双边样本仍需本机 `OPENAI_API_KEY` / `OpenClaw(Local HTTP)` provider 就绪后执行，且 QA/producer 双签尚未产出。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_openclaw_parity_bench -- --nocapture` 与 `bash scripts/openclaw-parity-p0.sh --openclaw-only --samples 1 --ticks 3 --run-id mock_smoke --out-dir .tmp/openclaw_parity_mock_smoke`（配本地 mock provider），均通过并生成聚合产物。

## 13:12:00 CST / qa_engineer
- 完成内容：对 parity harness 做 required 层结构验收，确认 `combined.csv`、`failures.md`、`scorecard-links.md` 与单样本 summary 已按专题文档落盘，mock smoke 下失败签名不会再误报普通 wait trace 为 `unclassified`。
- 遗留事项：test_tier_full 的真实 builtin/OpenClaw 双边对标、主观评分卡与阻断结论仍待执行；当前 QA 结论仅覆盖 harness 产物结构与 mock localhost 冒烟，不构成 parity 通过线签收。



## 13:42:41 CST / producer_system_designer
- 完成内容：为 `PRD-WORLD_SIMULATOR-037/038` 回写专用玩法口径，新增 `doc/world-simulator/llm/openclaw-agent-profile-agent_world_p0_low_freq_npc-2026-03-13.md`，冻结 `OpenClaw(Local HTTP)` 首期 `P0` 默认 profile 为 `agent_world_p0_low_freq_npc`，明确其目标优先级、白名单动作偏好、禁行动作与恢复策略；同步回写 `llm-openclaw-local-http-provider-integration`、`llm-openclaw-agent-experience-parity` 与模块 `project/prd` 入口。
- 遗留事项：launcher 配置里的 `openclaw_agent_profile` 目前仍停留在产品入口层，主启动链路尚未把该字段透传到正式 game launcher；真实用户机试点仍需在后续 `T5` 中接通这一段。

## 13:42:41 CST / agent_engineer
- 完成内容：推进 `TASK-WORLD_SIMULATOR-123`，在 `crates/agent_world/src/simulator/decision_provider.rs` 为 `DecisionRequest` 新增 `agent_profile`，并把 `ProviderBackedAgentBehavior::with_agent_profile(...)` 接到 request build；同时更新 `world_openclaw_parity_bench` 与 `scripts/openclaw-parity-p0.sh`，新增 `--openclaw-agent-profile`，默认使用 `agent_world_p0_low_freq_npc`，并将 profile 写入 OpenClaw provider summary。
- 遗留事项：真实 builtin/OpenClaw 双边 parity 样本与 QA/producer 双签仍待本机 `OPENAI_API_KEY` 与真实 `OpenClaw(Local HTTP)` provider 就绪后执行；launcher 主链路 profile 透传未在本任务内完成。
- 验证：已执行 `env -u RUSTC_WRAPPER cargo test -p agent_world decision_provider -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p agent_world openclaw_adapter -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p agent_world openclaw_local_http -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_openclaw_parity_bench -- --nocapture` 与 `bash scripts/openclaw-parity-p0.sh --help`，均通过。

## 13:42:41 CST / qa_engineer
- 完成内容：为 `agent_profile` 接线补齐 required 回归要求：`decision_provider` 需断言 recorded request 保留 profile，`openclaw_adapter`/`openclaw_local_http` 需验证 local HTTP request 体携带 `agent_profile`，parity bench 需断言 CLI 默认值与自定义 profile 透传。
- 遗留事项：真实 builtin/OpenClaw 双边对标、主观评分卡与 launcher 主链路 profile 透传仍待后续 `T4/T5` 与真实本机 provider 环境完成。
- 验证结论：required 层回归通过；当前新增字段未破坏现有 parity bench CLI、local HTTP request 体与 summary provider 信息结构。
