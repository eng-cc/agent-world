- 时刻：2026-03-01 00:21:41 CST
- 完成内容：完成“按 `doc/game-test.md` 执行真实玩家游玩并填写卡片”任务（2026-03-01 首轮样本）。
  - 使用 `./scripts/run-game-test.sh` 启动默认 Web 闭环链路（`ws://127.0.0.1:5011`），启动日志目录：`output/playwright/playability/startup-20260301-000045/`。
  - 使用 Playwright（headed）执行一轮真实玩家控制序列并录屏，产物目录：`output/playwright/playability/20260301-001603-long/`。
  - 新增测试卡片：`doc/playability_test_result/card_2026_03_01_00_20_13.md`。
  - 回填量化产物：`manual_metrics.json`、`manual_metrics.md`、`manual-control.log`、`console_all_messages.log`、`playthrough.webm`。
  - 更新项目文档：`doc/game-test.project.md`；更新活跃样本说明：`doc/playability_test_result/README.md`。
- 测试：
  - test_tier_full（真实玩家闭环 + 录屏）：`./scripts/run-game-test.sh` + `PLAYWRIGHT_CLI_SESSION=playability-manual-20260301-001603 ./.codex/skills/playwright/scripts/playwright_cli.sh open/video-start/eval/console/video-stop/close`。
  - test_tier_required（产物完整性）：`ls -l output/playwright/playability/20260301-001603-long/`。
  - test_tier_required（量化结果核对）：`cat output/playwright/playability/20260301-001603-long/manual_metrics.json`、`sed -n '1,120p' output/playwright/playability/20260301-001603-long/manual_metrics.md`。
  - test_tier_required（卡片落盘）：`test -f doc/playability_test_result/card_2026_03_01_00_20_13.md`。
- 遗留事项：
  - 最新样本 A 段出现首控 `play` 已接受但无推进（`accepted_no_progress`），需要继续复测确认是否为间歇性抖动。
- 时刻：2026-03-01 00:01:29 CST
- 完成内容：完成“Viewer Texture Inspector 视觉细节系统优化”T2（构图候选策略 + 资源包 + 灯光预设 + 元数据）。
  - 更新 `scripts/viewer-texture-inspector.sh`：
    - 新增构图策略参数：`--composition-profile`（`legacy` / `art_review_v2`）；
    - 新增灯光策略参数：`--lighting-profile`（`art_review_v1` / `art_review_v2`）；
    - 新增实体资源包参数：`--resource-pack-file`（entity/variant 级 mesh/texture 覆盖）；
    - 新增面板策略参数：`--art-hide-panel` / `--no-art-hide-panel`；
    - `--crop-window` 新增 `auto`，按实体与面板可见性自动推导裁切窗口；
    - power 变体校验由“单 fallback”升级为“多候选 closeup 重试”，并输出候选索引/标签与尝试次数。
  - 新增资源包样例：`scripts/viewer-texture-inspector-resource-pack.example.env`。
  - 元数据新增：`composition_profile`、`lighting_profile`、`panel_hidden`、`closeup_pose_label`、`closeup_candidate_*`、`resource_pack_*`、`crop_window_requested/effective` 等。
  - 更新项目管理文档：`doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.project.md`，T2 勾选完成，状态推进至 T3。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（帮助输出）：`./scripts/viewer-texture-inspector.sh --help`。
  - test_tier_required（资源包缺失校验）：`./scripts/viewer-texture-inspector.sh --resource-pack-file .tmp/not_exists.env --inspect power_plant --variants default --out-dir .tmp/texture_inspector_invalid_pack_test`（期望失败并报缺失文件）。
  - test_tier_full（power 实跑，默认 profile）：`./scripts/viewer-texture-inspector.sh --inspect power_plant --scenario power_bootstrap --art-capture --preview-mode direct_entity --material-profile art_review_v1 --semantic-gate-mode auto --detail-edge-threshold 0.35 --variant-ssim-threshold 0.9995 --no-prewarm --out-dir output/texture_inspector/framework_t2_visual_opt_power_plant_20260228`（`min_edge_energy_initial=0.426658`，`status=passed`）。
  - test_tier_full（候选重试分支验证）：`./scripts/viewer-texture-inspector.sh --inspect power_plant --scenario power_bootstrap --art-capture --preview-mode direct_entity --material-profile art_review_v1 --semantic-gate-mode auto --detail-edge-threshold 0.90 --variant-ssim-threshold 0.9995 --no-prewarm --out-dir output/texture_inspector/probe_power_plant_retry_candidates_20260228`（`retry_candidates_attempted=3`，`retry_candidate_used=exhausted`，验证多候选链路生效）。
- 遗留事项：
  - 执行 T3：完成 power 场景正式回归与结项文档。

- 时刻：2026-03-01 00:25:47 CST
- 完成内容：完成“Viewer Texture Inspector 视觉细节系统优化”T3（power 场景回归 + 结项记录）。
  - 执行正式回归并产出：`output/texture_inspector/framework_t3_visual_opt_power_scene_20260301`。
  - 回归结果：
    - `power_plant/variant_validation.txt`：`status=passed`，`min_pair_ssim_initial=0.986556`，`min_edge_energy_initial=0.430453`。
    - `power_storage/variant_validation.txt`：`status=passed`，`min_pair_ssim_initial=0.987376`，`min_edge_energy_initial=0.476528`。
  - 抽查截图发现 closeup 仍有“大色块贴脸”问题；同时 `meta.txt` 中 `selection_gate_orbit_radius_closeup=0.023040` 持续偏小，确认“指标通过但可视可读性不足”仍存在。
  - 更新结项文档与项目状态：
    - `doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.md`
    - `doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.project.md`
- 测试：
  - test_tier_full：`./scripts/viewer-texture-inspector.sh --inspect power_plant,power_storage --scenario power_bootstrap --art-capture --preview-mode direct_entity --material-profile art_review_v1 --semantic-gate-mode auto --detail-edge-threshold 0.35 --variant-ssim-threshold 0.9995 --no-prewarm --out-dir output/texture_inspector/framework_t3_visual_opt_power_scene_20260301`
  - test_tier_full（视觉抽检）：查看 `viewer_art.png` / `viewer_art_closeup.png`（power_plant、power_storage）。
- 遗留事项：
  - 执行 T4：设施实体尺度归一化（power_plant/power_storage）并复跑视觉回归。

- 时刻：2026-03-01 02:16:50 CST
- 完成内容：完成“Viewer Texture Inspector 视觉细节系统优化”T4（设施尺度归一化 + direct_entity 构图修复）收尾。
  - 收敛代码改造：
    - `scene_helpers_entities.rs`：power 设施按 location 半径进行尺度归一，并同步写入 `Transform.scale`/`BaseScale`。
    - `scene_helpers.rs`：当 `show_locations=0` 时不生成 location mesh，但保留 `location_positions`/`location_radii_cm`，确保设施缩放可用。
    - `viewer_automation.rs`：Focus 步骤对 power 目标引入基于 `BaseScale` 的自动 orbit 半径策略（含最小半径地板）。
    - `scripts/viewer-texture-inspector.sh`：`direct_entity` 默认关闭 location 干扰并强制 `crop_window_effective=none`（仅 `--crop-window auto`），补充 `capture_auto_focus_target` 与 selection highlight 元数据链路。
    - `selection_emphasis.rs`：高亮开关关闭时停止 Halo 渲染。
  - 验证证据（基于 probe 目录）：
    - `probe_focus_radius_power_default_isolated_nocrop_20260301`：`crop_window_effective=none`，`selection_gate_pass=1`，`closeup_edge_energy=0.372909`。
    - `probe_power_storage_direct_entity_nocrop_20260301`：`crop_window_effective=none`，`selection_gate_pass=1`，`closeup_edge_energy=0.372909`。
  - 结论：T4 已修复“自动裁切导致细节误损”问题，当前主问题已转为 T5 的“跨变体材质差异不足（SSIM 过高）”。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/viewer-texture-inspector.sh`
  - test_tier_required（Rust 定向单测）：
    - `env -u RUSTC_WRAPPER cargo test -p agent_world_viewer spawn_power_entities_scale_with_location_radius_units -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world_viewer spawn_power_entities_without_location_mesh_when_locations_hidden -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world_viewer automation_focus_radius_for_power_target_has_reasonable_floor -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world_viewer selection_emphasis_hides_halo_when_selection_clears -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world_viewer selection_emphasis_hides_halo_for_fragment_selection -- --nocapture`
  - test_tier_full（回归采集）：
    - 复跑命令 `./scripts/viewer-texture-inspector.sh --inspect power_plant,power_storage --scenario power_bootstrap --art-capture --preview-mode direct_entity --material-profile art_review_v1 --semantic-gate-mode auto --detail-edge-threshold 0.35 --variant-ssim-threshold 0.9995 --no-prewarm --out-dir output/texture_inspector/framework_t4_visual_opt_power_scene_direct_entity_nocrop_20260301_rerun` 因候选重试链路耗时过长已中止；T4 判定采用上述 probe 证据。
- 遗留事项：
  - 执行 T5：实现 power_plant/power_storage 实体级材质差异增强链路并完成阈值回归。

- 时刻：2026-03-01 11:30:16 CST
- 完成内容：完成“Viewer Texture Inspector 视觉细节系统优化”T5（实体级材质差异增强 + 构图半径框架修正）收尾。
  - 收敛代码改造：
    - `crates/agent_world_viewer/src/viewer_3d_config.rs`：新增 power 实体独立材质配置与 env 解析（`AGENT_WORLD_VIEWER_MATERIAL_POWER_PLANT_*` / `AGENT_WORLD_VIEWER_MATERIAL_POWER_STORAGE_*`）。
    - `crates/agent_world_viewer/src/main.rs`、`main_ui_runtime.rs`、`theme_runtime.rs`：power_plant / power_storage 材质应用切到独立通道。
    - `crates/agent_world_viewer/src/viewer_3d_config_profile_tests.rs`：补齐 power 材质 env override 与 invalid fallback 测试断言。
    - `scripts/viewer-texture-inspector.sh`：
      - power direct_entity 的 hero/closeup/fallback/retry 构图参数改为 safe-radius（`zoom≈3.x`）；
      - 保留并扩展 power 材质/颜色覆写链路与 `viewer_art_closeup_ssim.png` 指标图链路。
    - `scripts/viewer-texture-inspector-resource-pack.example.env`：补充 material/color 字段说明与示例。
  - 框架结论：
    - 根因不是单纯“材质参数幅度不足”，而是 closeup 常进入模型内部导致主体缺失、背景占比过高；
    - T5 在框架层同时修复“实体级材质控制”和“构图半径策略”，避免继续靠阈值打补丁。
  - 正式回归产物：`output/texture_inspector/framework_t5_power_material_split_zoom_profile_20260301`
    - `power_plant`：`min_pair_ssim_initial=0.994287`，`min_edge_energy_initial=0.470741`，`status=passed`。
    - `power_storage`：`min_pair_ssim_initial=0.994574`，`min_edge_energy_initial=0.453686`，`status=passed`。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/viewer-texture-inspector.sh`
  - test_tier_required（Rust 定向单测）：
    - `env -u RUSTC_WRAPPER cargo test -p agent_world_viewer load_viewer_3d_config_applies_env_overrides -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world_viewer load_viewer_3d_config_ignores_invalid_values -- --nocapture`
  - test_tier_full（T5 正式回归）：
    - `./scripts/viewer-texture-inspector.sh --inspect power_plant,power_storage --scenario power_bootstrap --art-capture --preview-mode direct_entity --material-profile art_review_v1 --semantic-gate-mode auto --detail-edge-threshold 0.35 --variant-ssim-threshold 0.9995 --no-prewarm --out-dir output/texture_inspector/framework_t5_power_material_split_zoom_profile_20260301`
  - test_tier_full（视觉抽检）：
    - 查看 `viewer_art.png` 与 `viewer_art_closeup.png`（`power_plant/default`、`power_storage/default`），确认主体入画且材质差异可读。
- 遗留事项：
  - 无（本项目 T0~T5 已全部完成）。

- 时刻：2026-03-01 11:38:43 CST
- 完成内容：启动“Viewer Texture Inspector 框架合理性优化（2026-03-01）”并完成 T0 建档。
  - 新建设计文档：`doc/scripts/viewer-texture-inspector-framework-rationalization-2026-03-01.md`。
  - 新建项目管理文档：`doc/scripts/viewer-texture-inspector-framework-rationalization-2026-03-01.project.md`。
  - 明确本阶段目标：Rust 配置解析模块化 + Shell 构图策略统一入口，优先解决可维护性与扩展性。
- 测试：
  - test_tier_required：文档任务，无代码改动；进入 T1/T2 后执行代码与脚本回归。
- 遗留事项：
  - 执行 T1：拆分 `viewer_3d_config` 解析模块并验证兼容性。

- 时刻：2026-03-01 11:40:43 CST
- 完成内容：完成“Viewer Texture Inspector 框架合理性优化（2026-03-01）”T1（Rust 配置解析模块化）。
  - `crates/agent_world_viewer/src/viewer_3d_config.rs`：移除底部解析函数实现，改为引用独立解析模块。
  - 新增 `crates/agent_world_viewer/src/viewer_3d_config_parsing.rs`：集中承载 `parse_*` 系列函数。
  - 外部接口保持不变：`resolve_viewer_3d_config` / `resolve_viewer_external_*` 调用链不变。
  - 结果：`viewer_3d_config.rs` 行数由 1254 降至 1089，满足单文件不超过 1200 行约束。
  - 更新项目管理文档：`doc/scripts/viewer-texture-inspector-framework-rationalization-2026-03-01.project.md`，T1 勾选完成并推进到 T2。
- 测试：
  - test_tier_required（Rust 定向单测）：
    - `env -u RUSTC_WRAPPER cargo test -p agent_world_viewer load_viewer_3d_config_applies_env_overrides -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world_viewer load_viewer_3d_config_ignores_invalid_values -- --nocapture`
  - test_tier_required（结构约束核查）：`wc -l crates/agent_world_viewer/src/viewer_3d_config.rs crates/agent_world_viewer/src/viewer_3d_config_parsing.rs`
- 遗留事项：
  - 执行 T2：统一 shell 中 power pose 解析入口并完成回归。

- 时刻：2026-03-01 11:44:53 CST
- 完成内容：完成“Viewer Texture Inspector 框架合理性优化（2026-03-01）”T2（Shell 构图策略结构化）。
  - `scripts/viewer-texture-inspector.sh`：
    - 将 power 的 `hero/closeup/fallback` 构图参数统一收敛为 `resolve_power_pose(pose_kind, focus_target, profile)`；
    - 将 retry 候选入口统一为 `emit_power_retry_closeup_candidates`，与主 pose 解析共用 profile/focus 语义；
    - 保持原参数字面值与 CLI 接口不变，降低后续扩展时的分支复制成本。
  - 更新文档：
    - `doc/scripts/viewer-texture-inspector-framework-rationalization-2026-03-01.md` 增补执行结论与回归指标。
    - `doc/scripts/viewer-texture-inspector-framework-rationalization-2026-03-01.project.md` 勾选 T2，项目状态置为完成。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/viewer-texture-inspector.sh`
  - test_tier_full（power 单变体最小闭环）：
    - `./scripts/viewer-texture-inspector.sh --inspect power_plant --variants default --scenario power_bootstrap --art-capture --preview-mode direct_entity --material-profile art_review_v1 --semantic-gate-mode auto --detail-edge-threshold 0.35 --variant-ssim-threshold 0.9995 --no-prewarm --out-dir output/texture_inspector/framework_t2_pose_unify_power_plant_default_20260301`
    - 关键元数据：`selection_gate_orbit_radius_closeup=0.102400`
  - test_tier_full（power 三变体回归）：
    - `./scripts/viewer-texture-inspector.sh --inspect power_plant --variants all --scenario power_bootstrap --art-capture --preview-mode direct_entity --material-profile art_review_v1 --semantic-gate-mode auto --detail-edge-threshold 0.35 --variant-ssim-threshold 0.9995 --no-prewarm --out-dir output/texture_inspector/framework_t2_pose_unify_power_plant_variants_20260301`
    - `variant_validation.txt`：`status=passed`，`min_pair_ssim_initial=0.994289`，`min_edge_energy_initial=0.470741`，`retry_candidates_attempted=0`。
- 遗留事项：
  - 无（本项目 T0~T2 已全部完成）。
- 时刻：2026-03-01 00:29:58 CST
- 完成内容：完成“S10 Distfs Probe Bootstrap”T0（建档）。
  - 新建设计文档：`doc/testing/s10-distfs-probe-bootstrap-2026-02-28.md`。
  - 新建项目管理文档：`doc/testing/s10-distfs-probe-bootstrap-2026-02-28.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/testing/s10-distfs-probe-bootstrap-2026-02-28.md doc/testing/s10-distfs-probe-bootstrap-2026-02-28.project.md`
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/testing/s10-distfs-probe-bootstrap-2026-02-28.md`
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/testing/s10-distfs-probe-bootstrap-2026-02-28.project.md`
- 遗留事项：
  - 执行 T1：实现 reward worker 的 distfs seed bootstrap。

- 时刻：2026-03-01 00:30:12 CST
- 完成内容：完成“S10 Distfs Probe Bootstrap”T1（正向实现）。
  - 更新 `crates/agent_world/src/bin/world_chain_runtime/reward_runtime_worker.rs`：
    - 新增 `ensure_distfs_probe_seed_blob`，当 `storage_root` 下 blob 为空时自动写入最小 seed blob；
    - 在 reward worker 启动时执行 seed bootstrap（失败仅记 `last_error`，不阻断主循环）；
    - 新增单测 `ensure_distfs_probe_seed_blob_populates_empty_store_once` 验证幂等行为。
  - 更新 `scripts/s10-five-node-game-soak.sh` 门禁判定：
    - 引入 `gate_data_warnings`，仅“数据未就绪”归类为 `insufficient_data`；
    - `known_peer_heads_zero_samples`、`http_failure_samples` 等一般告警改为 `pass + notes`。
  - 更新项目文档：`doc/testing/s10-distfs-probe-bootstrap-2026-02-28.project.md`（T1 勾选完成，推进到 T2）。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world --bin world_chain_runtime`
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_chain_runtime`
  - test_tier_required：`bash -n scripts/s10-five-node-game-soak.sh`
- 遗留事项：
  - 执行 T2：复跑 S10 发布基线并确认 `metric_gate=pass`。
