- 日期：2026-02-14
- 完成内容：同步 WASM 工件以修复 CI 检查失败。
  - 问题诊断：
    - CI 在执行 `./scripts/sync-m1-builtin-wasm-artifacts.sh --check` 时失败。
    - 错误信息：`error: embedded artifact is stale vs built wasm for module: m1.rule.move`。
    - 根因：合并 main 分支后，嵌入的 WASM 工件与新构建的 WASM 模块哈希不匹配（built=68f0bad4... vs embedded=97dccdc5...）。
  - 解决方案：
    - 运行 `./scripts/sync-m1-builtin-wasm-artifacts.sh` 重新生成并同步所有 WASM 工件。
    - 更新了 10 个模块的 .wasm 文件和 m1_builtin_modules.sha256 哈希清单。
  - 验证通过：
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check` 验证通过，所有工件哈希匹配。
    - 输出：`check ok: per-module embedded artifacts and hash manifest are in sync`。
- 遗留事项：无（本任务已完成）。
