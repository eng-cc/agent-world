## 13:22:06 CST / producer_system_designer
- 完成内容：
  - 新增 `doc/p2p/blockchain/p2p-mainnet-crypto-security-baseline-2026-03-23.{prd,design,project}.md`，把“当前项目密码学安全程度是否能对标主流公链”正式收成 `doc/p2p` 安全专题，而不是继续停留在聊天判断。
  - 回写 `doc/p2p/prd.md`、`doc/p2p/project.md`、`doc/p2p/prd.index.md` 与 `doc/p2p/README.md`，新增 `PRD-P2P-014 / TASK-P2P-032`，固定当前整体 verdict 为 `not_mainnet_grade`，并把 producer 下一优先级切到“主链 Token 签名交易鉴权”而不是继续把创世地址 ceremony 当成第一优先级。
  - 明确当前四类系统级 blocker：主链资产动作未统一走签名交易模型、生产级 keystore 缺失、治理 finality signer 仍含 deterministic local seed 路径、创世 freeze sheet 仍存在 `TBD_BEFORE_MINT` / `pending_binding`。
- 执行验证：
  - `rg -n "not_mainnet_grade|mainnet-grade|TransferMainToken|config.toml|deterministic local seed|TBD_BEFORE_MINT" doc/p2p/blockchain/p2p-mainnet-crypto-security-baseline-2026-03-23.prd.md doc/p2p/blockchain/p2p-mainnet-crypto-security-baseline-2026-03-23.design.md doc/p2p/blockchain/p2p-mainnet-crypto-security-baseline-2026-03-23.project.md doc/p2p/prd.md doc/p2p/project.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 下一步应进入新的签名交易鉴权专题，统一 `TransferMainToken / ClaimMainTokenVesting / InitializeMainTokenGenesis / DistributeMainTokenTreasury` 的提交鉴权模型；在该专题收口前，不应把“对标主流公链安全”作为公开口径。

## 15:08:44 CST / runtime_engineer
- 完成内容：
  - 新增 `doc/p2p/token/mainchain-token-signed-transaction-authorization-2026-03-23.{prd,design,project}.md`，把“主链 Token 签名交易鉴权”正式拆成独立专题，并回写 `doc/p2p/prd.md`、`doc/p2p/project.md`、`doc/p2p/prd.index.md` 与 `doc/p2p/README.md`，新增 `PRD-P2P-015 / TASK-P2P-033`。
  - 为 `crates/oasis7/src/bin/oasis7_chain_runtime/transfer_submit_api.rs` 补齐 `public_key/signature` 请求字段、`ed25519` 验签、`awt:pk:<public_key_hex>` 账户绑定与固定域前缀 canonical payload；公开 `transfer submit` 不再接受未签名 JSON。
  - 同步更新 `crates/oasis7/src/bin/oasis7_web_launcher.rs` 与 `crates/oasis7/src/bin/oasis7_web_launcher/control_plane.rs` 的转账代理请求结构与定向测试，确保控制面字段与 runtime 一致。
  - 扩充 `crates/oasis7/src/bin/oasis7_chain_runtime/transfer_submit_api_tests.rs`，覆盖有效签名成功、缺签名拒绝、错误签名拒绝、账户绑定不匹配拒绝，并保持既有 explorer/status/history 回归通过。
- 执行验证：
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 transfer_submit --bin oasis7_chain_runtime`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 submit_chain_transfer_remote --bin oasis7_web_launcher`
  - `env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_chain_runtime --bin oasis7_web_launcher`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - `ClaimMainTokenVesting / InitializeMainTokenGenesis / DistributeMainTokenTreasury` 仍未并入统一 signed transaction model。
  - Web/native 转账 UI 还没有自己产出签名材料的交互闭环，后续需要 `viewer_engineer` 跟进。
