- 时刻：2026-03-02 13:26:45 CST
- 完成内容：完成“客户端启动器中英文切换与必填配置校验（2026-03-02）”T0（建档）。
  - 新建设计文档：`doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.md doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.project.md`。
- 遗留事项：
  - 执行 T1：实现启动器中英文切换。

- 时刻：2026-03-02 13:31:19 CST
- 完成内容：完成“客户端启动器中英文切换与必填配置校验（2026-03-02）”T1（启动器 UI 中英文切换）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：新增 `UiLanguage` 与环境变量语言解析（`AGENT_WORLD_CLIENT_LAUNCHER_LANG` / `LANG`），默认回落中文。
  - 新增语言切换控件（中文/English），并将标题、字段标签、按钮、状态文案切到双语渲染。
  - 新增 `LauncherStatus` 状态枚举，状态文本按当前语言动态显示，避免切换语言后状态文案不一致。
  - 补充单测：语言标签解析与状态本地化断言。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs`（968 行，满足单文件不超过 1200 行约束）。
- 遗留事项：
  - 执行 T2：新增必填配置阻断校验与提示清单。

- 时刻：2026-03-02 13:35:41 CST
- 完成内容：完成“客户端启动器中英文切换与必填配置校验（2026-03-02）”T2（必填配置阻断校验 + 提示清单 + 单测）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：新增 `ConfigIssue` 与 `collect_required_config_issues`，覆盖 scenario/live/web/viewer/launcher 与 chain 相关必填与格式校验。
  - 启动器 UI 新增启动前校验提示区：
    - 当校验通过时显示“可启动”；
    - 当存在阻断项时逐条列出问题（中英文），并禁用“启动”按钮。
  - `start_process` 增加防线：即使按钮层误触发，也会先做 preflight 校验并写入日志后拒绝启动。
  - 补充单测：缺失/非法配置项收集与合法配置放行；并整理文件空行使 `main.rs` 回落到 1190 行（满足单文件约束）。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs`（1190 行）。
- 遗留事项：
  - 执行 T3：回归测试、文档收口与项目结项。

- 时刻：2026-03-02 13:38:05 CST
- 完成内容：完成“客户端启动器中英文切换与必填配置校验（2026-03-02）”T3（回归测试 + 文档收口 + 结项）。
  - 更新项目管理文档：`doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.project.md`，T3 勾选完成，状态更新为“已完成（T0~T3）”。
  - 更新设计文档：`doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.md`，补充“完成态（2026-03-02）”。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。

- 时刻：2026-03-02 14:58:16 CST
- 完成内容：完成“客户端启动器优雅退出与级联进程关闭（2026-03-02）”T0（建档）。
  - 新建设计文档：`doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.md doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.project.md`。
- 遗留事项：
  - 执行 T1：实现优雅中断 + 超时强杀的停止流程。

- 时刻：2026-03-02 15:01:04 CST
- 完成内容：完成“客户端启动器优雅退出与级联进程关闭（2026-03-02）”T1（停止流程改造）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：`stop_child_process` 由“直接 kill”改为“优雅中断（Unix 下发送 SIGINT）-> 超时轮询等待 -> 强杀兜底”。
  - 新增停止策略常量：`GRACEFUL_STOP_TIMEOUT_MS`、`STOP_POLL_INTERVAL_MS`。
  - 新增 `send_interrupt_signal`（Unix: `libc::kill(pid, SIGINT)`；非 Unix: no-op fallback）。
  - 窗口关闭路径（`Drop`）与“停止”按钮均复用 `stop_child_process`，统一优雅退出语义。
  - 更新 `crates/agent_world_client_launcher/Cargo.toml`：新增 Unix 目标依赖 `libc`。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
- 遗留事项：
  - 执行 T2：补充停止逻辑测试，并处理 `main.rs` 1200 行约束（当前 `wc -l` 为 1229）。

- 时刻：2026-03-02 15:04:58 CST
- 完成内容：完成“客户端启动器优雅退出与级联进程关闭（2026-03-02）”T2（测试补充 + 行数约束收敛）。
  - 将启动器测试从 `main.rs` 拆分至 `crates/agent_world_client_launcher/src/tests.rs`，`main.rs` 仅保留 `#[cfg(test)] mod tests;`。
  - 新增停止逻辑测试：
    - `stop_child_process_handles_already_exited_child`
    - `stop_child_process_sends_interrupt_for_graceful_exit`（Unix）
  - 行数约束收敛：`crates/agent_world_client_launcher/src/main.rs` 由 1232 行降至 1016 行，满足单文件不超过 1200 行约束。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs crates/agent_world_client_launcher/src/tests.rs`。
- 遗留事项：
  - 执行 T3：回归测试、文档收口与项目结项。

- 时刻：2026-03-02 15:08:33 CST
- 完成内容：完成“客户端启动器优雅退出与级联进程关闭（2026-03-02）”T3（回归测试 + 文档收口 + 结项）。
  - 更新项目管理文档：`doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.project.md`，T3 勾选完成，状态置为“已完成（T0~T3）”。
  - 更新设计文档：`doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.md`，补充“完成态（2026-03-02）”。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs`（1016 行）。
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。
- 时刻：2026-03-02 14:52:54 CST
- 完成内容：完成“客户端启动器反馈入口（2026-03-02）”T0（建档）。
  - 新建设计文档：`doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.md doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.project.md`。
- 遗留事项：
  - 执行 T1：实现启动器反馈入口、反馈包落盘与测试。

- 时刻：2026-03-02 15:02:46 CST
- 完成内容：完成“客户端启动器反馈入口（2026-03-02）”T1（反馈入口实现 + 落盘 + 测试）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：
    - 新增反馈表单状态（类型/标题/描述/反馈目录）与提交入口按钮。
    - 新增反馈表单必填校验与错误提示（中英文）。
    - 新增提交结果提示与日志记录（成功显示反馈文件路径，失败显示错误原因）。
  - 新增 `crates/agent_world_client_launcher/src/feedback_entry.rs`：
    - 实现反馈报告 JSON 落盘（自动创建目录、文件名时间戳 + 类型后缀）。
    - 自动附带启动器配置快照与最近日志窗口（最新 200 行）。
    - 增加模块单测（表单校验、日志窗口截断、JSON 落盘、文件名时间戳格式）。
  - 新增 `crates/agent_world_client_launcher/src/main_tests.rs`，并将原 `main.rs` 测试迁移为外置测试模块，避免主文件继续膨胀。
  - 更新依赖：`crates/agent_world_client_launcher/Cargo.toml` 增加 `serde` / `serde_json`。
  - 更新项目管理文档：`doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.project.md`，标记 T1 完成并进入 T2。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs crates/agent_world_client_launcher/src/feedback_entry.rs crates/agent_world_client_launcher/src/main_tests.rs`（`main.rs` 1143 行，满足单文件不超过 1200 行约束）。
- 遗留事项：
  - 执行 T2：更新设计文档完成态、项目状态收口并补充本任务结项日志。

- 时刻：2026-03-02 15:05:12 CST
- 完成内容：完成“客户端启动器反馈入口（2026-03-02）”T2（测试与收口）。
  - 更新设计文档：`doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.md`，补充“完成态（2026-03-02）”。
  - 更新项目管理文档：`doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.project.md`，标记 T2 完成，状态收敛为“已完成（T0~T2）”。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
  - test_tier_required：`wc -l doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.md doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险|## 完成态" doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/game-client-launcher-feedback-entry-2026-03-02.project.md`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs`（1143 行，满足单文件不超过 1200 行约束）。
- 遗留事项：
  - 无（本项目 T0~T2 已全部完成）。

- 时刻：2026-03-02 15:33:39 CST
- 完成内容：完成“启动器 Viewer 鉴权自动继承 Node 配置（2026-03-02）”T0（建档）。
  - 新建设计文档：`doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.md`。
  - 新建项目管理文档：`doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.md doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.project.md`。
- 遗留事项：
  - 执行 T1：实现 `world_game_launcher` 注入 Web Viewer 鉴权配置并补测试。

- 时刻：2026-03-02 15:36:56 CST
- 完成内容：完成“启动器 Viewer 鉴权自动继承 Node 配置（2026-03-02）”T1（启动器注入）。
  - 更新 `crates/agent_world/src/bin/world_game_launcher.rs`：
    - 新增 `config.toml [node]` 鉴权读取逻辑（`private_key/public_key` + `player_id`）。
    - 静态服务返回 `index.html` 时注入 `window.__AGENT_WORLD_VIEWER_AUTH_ENV`，向 Web Viewer 传递 `AGENT_WORLD_VIEWER_PLAYER_ID` / `AGENT_WORLD_VIEWER_AUTH_PUBLIC_KEY` / `AGENT_WORLD_VIEWER_AUTH_PRIVATE_KEY`。
    - 保留原有 trunk reload script 清理逻辑，并叠加鉴权注入。
  - 更新 `crates/agent_world/src/bin/world_game_launcher/world_game_launcher_tests.rs`：
    - 补充鉴权注入脚本与 node 配置读取定向测试。
  - 更新项目管理文档：`doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.project.md`，标记 T1 完成并推进到 T2。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_game_launcher world_game_launcher_tests -- --nocapture`。
- 遗留事项：
  - 执行 T2：实现 `agent_world_viewer` 的 wasm 注入读取与 native `config.toml` 鉴权回退。

- 时刻：2026-03-02 15:42:04 CST
- 完成内容：完成“启动器 Viewer 鉴权自动继承 Node 配置（2026-03-02）”T2（Viewer 回退）。
  - 更新 `crates/agent_world_viewer/src/egui_right_panel_chat_auth.rs`：
    - Viewer 鉴权解析增加 wasm 注入读取：优先读取 `window.__AGENT_WORLD_VIEWER_AUTH_ENV`。
    - native 鉴权解析增加 `config.toml [node]` 回退：当 `AGENT_WORLD_VIEWER_AUTH_*` 未设置时自动读取 `private_key/public_key`。
    - nonce 同步路径统一使用同一套运行时鉴权值解析函数，避免 player_id 读取口径分叉。
  - 更新 `crates/agent_world_viewer/Cargo.toml`：
    - wasm 目标新增 `js-sys`。
    - native 目标新增 `toml`。
  - 更新 `crates/agent_world_viewer/src/egui_right_panel_chat_tests.rs`：
    - 补充 `config.toml [node]` 回退读取与缺失 `node` 表拒绝用例。
  - 更新项目管理文档：`doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.project.md`，标记 T2 完成并推进到 T3。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_viewer egui_right_panel_chat::tests:: -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_viewer`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_viewer --target wasm32-unknown-unknown`。
- 遗留事项：
  - 执行 T3：完成收口回归、更新设计文档完成态并结项。

- 时刻：2026-03-02 15:45:10 CST
- 完成内容：完成“启动器 Viewer 鉴权自动继承 Node 配置（2026-03-02）”T3（回归与收口）。
  - 更新设计文档：`doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.md`，补充“完成态（2026-03-02）”。
  - 更新项目管理文档：`doc/testing/launcher-viewer-auth-node-config-autowire-2026-03-02.project.md`，标记 T3 完成并结项（T0~T3 全部完成）。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_game_launcher world_game_launcher_tests -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_viewer`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_viewer --target wasm32-unknown-unknown`。
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。
- 时刻：2026-03-02 15:40:24 CST
- 完成内容：完成“客户端启动器反馈分布式提交迁移（2026-03-02）”T0~T3（链路实现 + 回归 + 文档收口）。
  - 新建设计文档：`doc/world-simulator/game-client-launcher-feedback-distributed-submit-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/game-client-launcher-feedback-distributed-submit-2026-03-02.project.md`（状态更新为已完成）。
  - 更新 `crates/agent_world/src/bin/world_chain_runtime.rs`：
    - 默认启用 `feedback_p2p`；
    - 新增 `POST /v1/chain/feedback/submit`；
    - 接口请求解析与错误响应接入；
    - 状态打印新增 feedback submit 地址。
  - 新增 `crates/agent_world/src/bin/world_chain_runtime/feedback_submit_api.rs`：
    - 反馈提交 payload 校验；
    - FeedbackCreateRequest 构造与签名；
    - HTTP body 抽取与响应结构。
  - 新增 `crates/agent_world/src/bin/world_chain_runtime/balances_api.rs`，并新增 `world_chain_runtime/world_chain_runtime_tests.rs`，将部分逻辑/测试外置，确保主文件行数约束。
  - 更新 `crates/agent_world_client_launcher/src/feedback_entry.rs`：
    - 新增远端提交（HTTP POST）与解析；
    - 新增 `submit_feedback_with_fallback`（远端优先，失败回落本地）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：反馈提交流程接入分布式优先策略并区分成功文案。
  - 新增 `crates/agent_world_client_launcher/src/platform_ops.rs`：抽离平台相关函数以控制 `main.rs` 行数。
  - 更新设计文档完成态与项目管理文档状态。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_chain_runtime`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world --bin world_chain_runtime`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs crates/agent_world/src/bin/world_chain_runtime.rs`（分别 1149 / 1173，满足单文件不超过 1200 行约束）。
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。

- 时刻：2026-03-02 15:53:08 CST
- 完成内容：完成“P2P 长跑反馈事件注入（2026-03-02）”F1~F3（脚本接线 + 手册更新 + 回归验证 + 项目文档收口）。
  - 更新 `scripts/p2p-longrun-soak.sh`：补齐 `summary.json` 的 feedback 配置/产物/totals 字段，`summary.md` 的 feedback 统计与 Gate 表列，结束输出新增 `feedback_events_log`。
  - 更新 `testing-manual.md`：S9 增加 feedback 事件注入命令示例，并补充 feedback totals/log 一致性通过标准。
  - 更新 `doc/testing/p2p-longrun-feedback-event-injection-2026-03-02.project.md`：F1/F2/F3 标记完成，状态收口为“已完成（F0~F3）”。
- 测试：
  - test_tier_required：`bash -n scripts/p2p-longrun-soak.sh`。
  - test_tier_required：`./scripts/p2p-longrun-soak.sh --profile soak_smoke --topologies triad --duration-secs 60 --feedback-events-enable --feedback-events-start-sec 5 --feedback-events-interval-secs 10 --feedback-events-max-events 2 --dry-run`。
  - test_tier_full：`./scripts/p2p-longrun-soak.sh --profile soak_smoke --topologies triad --duration-secs 45 --no-prewarm --feedback-events-enable --feedback-events-start-sec 5 --feedback-events-interval-secs 10 --feedback-events-max-events 2`。
  - test_tier_required：`jq '{overall_status, feedback_events: .feedback_events, totals: .totals, topology: .topologies[0] | {topology, status, feedback_events, feedback_events_success, feedback_events_failed}}' .tmp/p2p_longrun/20260302-155207/summary.json` 与 `awk -F'|' 'NR>1 && ($4=="completed" || $4=="failed") {c++} END{print c+0}' .tmp/p2p_longrun/20260302-155207/feedback_events.log` 一致性核对。
- 遗留事项：
  - 当前短跑样例中反馈提交返回 `HTTP 405 method not allowed`（`feedback_events_failed=2`），注入与统计链路正常但接口可用性需后续联调确认。

- 时刻：2026-03-02 15:53:35 CST
- 完成内容：补充“P2P 长跑反馈事件注入（2026-03-02）”设计文档完成态。
  - 更新 `doc/testing/p2p-longrun-feedback-event-injection-2026-03-02.md`：新增“完成态（2026-03-02）”章节。
- 测试：
  - test_tier_required：`rg -n "## 完成态（2026-03-02）" doc/testing/p2p-longrun-feedback-event-injection-2026-03-02.md`。
- 遗留事项：
  - 无新增。

- 时刻：2026-03-02 16:29:43 CST
- 完成内容：完成“LLM 配置 TOML 风格统一（2026-03-02）”T0（建档）。
  - 新建设计文档：`doc/world-simulator/llm-config-toml-style-unification-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/llm-config-toml-style-unification-2026-03-02.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/llm-config-toml-style-unification-2026-03-02.md doc/world-simulator/llm-config-toml-style-unification-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/world-simulator/llm-config-toml-style-unification-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/llm-config-toml-style-unification-2026-03-02.project.md`。
- 遗留事项：
  - 执行 T1：完成 LLM 小写 TOML 配置解析改造与测试。

- 时刻：2026-03-02 16:33:47 CST
- 完成内容：完成“LLM 配置 TOML 风格统一（2026-03-02）”T1（代码重构 + 单测）。
  - 更新 `crates/agent_world/src/simulator/llm_agent.rs`：
    - `from_config_file*` 改为读取小写 TOML 结构（`[llm]` / `profile` / `[profiles.*]` / `[model_providers.*]`）。
    - 增加配置选择优先级解析：`model/base_url/api_key` 支持从 profile/provider 组装。
    - 增加 agent 目标覆盖读取：`[llm.agent_overrides.<AGENT_ID_NORMALIZED>]`。
  - 更新 `crates/agent_world/src/simulator/llm_agent/tests_split_part1.rs`：
    - 将 `llm_config_reads_from_config_file` 改为小写 TOML 用例。
    - 新增 `llm_config_prefers_llm_table_core_fields_over_profile_defaults`。
    - 新增 `llm_config_reads_agent_scoped_goal_overrides_from_llm_agent_overrides_table`。
  - 更新项目管理文档：`doc/world-simulator/llm-config-toml-style-unification-2026-03-02.project.md`，标记 T1 完成并推进到 T2。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world simulator::llm_agent::tests::llm_config_ -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world`。
  - test_tier_required：`wc -l crates/agent_world/src/simulator/llm_agent.rs crates/agent_world/src/simulator/llm_agent/tests_split_part1.rs`（分别 1073 / 1060 行，满足单文件不超过 1200 行约束）。
- 遗留事项：
  - 执行 T2：迁移 `config.toml` / `config.example.toml` 与配置说明文档到小写 TOML 风格。

- 时刻：2026-03-02 16:38:19 CST
- 完成内容：完成“LLM 配置 TOML 风格统一（2026-03-02）”T2（配置与文档迁移）。
  - 更新 `config.toml`：移除 `AGENT_WORLD_LLM_*` 顶层键，改为 `[llm]` 小写配置。
  - 更新 `config.example.toml`：
    - LLM 参数全部迁移到 `[llm]`（小写字段）；
    - 保留 `model/profile/model_providers/profiles` 结构；
    - agent 级目标覆盖改为 `[llm.agent_overrides.<AGENT_ID_NORMALIZED>]` 示例。
  - 更新 `doc/world-simulator/llm-agent-behavior.md` 与 `doc/world-simulator/llm-prompt-system.md`：配置说明同步为小写 TOML 风格，并明确环境变量仅作回退/注入。
  - 更新项目管理文档：`doc/world-simulator/llm-config-toml-style-unification-2026-03-02.project.md`，标记 T2 完成并推进到 T3。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world simulator::llm_agent::tests::llm_config_reads_from_config_file -- --nocapture`。
  - test_tier_required：`if rg -n "AGENT_WORLD_LLM_" config.toml config.example.toml; then echo "unexpected AGENT_WORLD_LLM_ keys remain"; else echo "no AGENT_WORLD_LLM_ keys in config files"; fi`。
  - test_tier_required：`wc -l config.toml config.example.toml doc/world-simulator/llm-agent-behavior.md doc/world-simulator/llm-prompt-system.md`（均满足单 md 不超过 500 行约束）。
- 遗留事项：
  - 执行 T3：完成最终回归、更新设计文档完成态与项目结项。

- 时刻：2026-03-02 16:41:57 CST
- 完成内容：完成“LLM 配置 TOML 风格统一（2026-03-02）”T3（回归与收口）。
  - 更新项目管理文档：`doc/world-simulator/llm-config-toml-style-unification-2026-03-02.project.md`，标记 T3 完成并收口为“已完成（T0~T3）”。
  - 更新设计文档：`doc/world-simulator/llm-config-toml-style-unification-2026-03-02.md`，补充“完成态（2026-03-02）”交付结论。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world simulator::llm_agent::tests::llm_config_ -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world`。
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。

- 时刻：2026-03-02 17:19:39 CST
- 完成内容：完成“客户端启动器 LLM 设置入口（2026-03-02）”T0（建档）。
  - 新建设计文档：`doc/world-simulator/game-client-launcher-llm-settings-panel-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/game-client-launcher-llm-settings-panel-2026-03-02.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/game-client-launcher-llm-settings-panel-2026-03-02.md doc/world-simulator/game-client-launcher-llm-settings-panel-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/world-simulator/game-client-launcher-llm-settings-panel-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/game-client-launcher-llm-settings-panel-2026-03-02.project.md`。
- 遗留事项：
  - 执行 T1：实现设置按钮/窗口与 LLM 三字段配置读写。

- 时刻：2026-03-02 17:24:21 CST
- 完成内容：完成“客户端启动器 LLM 设置入口（2026-03-02）”T1（设置按钮/窗口 + 配置读写 + 单测）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：新增“设置 / Settings”按钮，点击后打开 LLM 设置窗口。
  - 新增 `crates/agent_world_client_launcher/src/llm_settings.rs`：
    - 新增 `LlmSettingsPanel`，提供 `config.toml` 的 `[llm]` 三字段编辑窗口（`api_key/base_url/model`）。
    - 支持从 `config.toml` 读取当前值并回填编辑框。
    - 支持保存到小写 TOML 键；当输入为空时移除对应键。
    - 补充模块单测：读取、写入保留其他表、空值移除、非法 TOML 报错。
  - 更新 `crates/agent_world_client_launcher/Cargo.toml`：新增 `toml = "0.8"`。
  - 更新项目管理文档：`doc/world-simulator/game-client-launcher-llm-settings-panel-2026-03-02.project.md`，标记 T1 完成并推进到 T2。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs crates/agent_world_client_launcher/src/llm_settings.rs`（`main.rs` 1157 行，满足单文件不超过 1200 行约束）。
- 遗留事项：
  - 执行 T2：回归收口并更新设计文档完成态。

- 时刻：2026-03-02 17:26:48 CST
- 完成内容：完成“客户端启动器 LLM 设置入口（2026-03-02）”T2（回归与收口）。
  - 更新设计文档：`doc/world-simulator/game-client-launcher-llm-settings-panel-2026-03-02.md`，补充“完成态（2026-03-02）”。
  - 更新项目管理文档：`doc/world-simulator/game-client-launcher-llm-settings-panel-2026-03-02.project.md`，标记 T2 完成并收口为“已完成（T0~T2）”。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
- 遗留事项：
  - 无（本项目 T0~T2 已全部完成）。

- 时刻：2026-03-02 17:35:57 CST
- 完成内容：完成“客户端启动器反馈窗口化（2026-03-02）”T0（建档）。
  - 新建设计文档：`doc/world-simulator/game-client-launcher-feedback-window-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/game-client-launcher-feedback-window-2026-03-02.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/game-client-launcher-feedback-window-2026-03-02.md doc/world-simulator/game-client-launcher-feedback-window-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/world-simulator/game-client-launcher-feedback-window-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/game-client-launcher-feedback-window-2026-03-02.project.md`。
- 遗留事项：
  - 执行 T1：实现反馈入口按钮 + 反馈窗口。

- 时刻：2026-03-02 17:39:06 CST
- 完成内容：完成“客户端启动器反馈窗口化（2026-03-02）”T1（反馈按钮 + 反馈窗口）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：
    - 新增反馈窗口状态 `feedback_window_open` 与 `show_feedback_window` 渲染方法。
    - 操作区新增“反馈 / Feedback”按钮，点击后打开反馈弹窗。
    - 主面板移除原内嵌反馈区域，反馈表单迁移到弹窗，字段/校验/提交结果展示保持一致。
  - 更新项目管理文档：`doc/world-simulator/game-client-launcher-feedback-window-2026-03-02.project.md`，标记 T1 完成并推进到 T2。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs`（1180 行，满足单文件不超过 1200 行约束）。
- 遗留事项：
  - 执行 T2：文档完成态与结项提交。

- 时刻：2026-03-02 17:41:36 CST
- 完成内容：完成“客户端启动器反馈窗口化（2026-03-02）”T2（回归与收口）。
  - 更新设计文档：`doc/world-simulator/game-client-launcher-feedback-window-2026-03-02.md`，补充“完成态（2026-03-02）”。
  - 更新项目管理文档：`doc/world-simulator/game-client-launcher-feedback-window-2026-03-02.project.md`，标记 T2 完成并收口为“已完成（T0~T2）”。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
- 时刻：2026-03-02 16:26:20 CST
- 完成内容：完成“Chain Runtime 反馈网络复制层自动挂载修复（2026-03-02）”T0~T2（根因修复 + 测试回归 + 文档收口）。
  - 新建设计文档：`doc/testing/chain-runtime-feedback-replication-network-autowire-2026-03-02.md`。
  - 新建项目管理文档：`doc/testing/chain-runtime-feedback-replication-network-autowire-2026-03-02.project.md`（状态已收口为完成）。
  - 更新 `crates/agent_world/src/bin/world_chain_runtime.rs`：
    - 新增默认 replication network 配置与挂载逻辑；
    - 启动前自动挂载 `NodeReplicationNetworkHandle`，修复 `feedback_p2p requires replication network` 启动失败；
    - 启用 `allow_local_handler_fallback_when_no_peers`，保证单机反馈链路可闭环。
  - 更新 `crates/agent_world/src/bin/world_chain_runtime/world_chain_runtime_tests.rs`：新增默认 replication 网络配置测试。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_chain_runtime -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world --bin world_chain_runtime`。
  - test_tier_required：`timeout 12s env -u RUSTC_WRAPPER cargo run -p agent_world --bin world_chain_runtime -- --status-bind 127.0.0.1:6121`（观察到 `world_chain_runtime ready.`）。
  - test_tier_required：`wc -l crates/agent_world/src/bin/world_chain_runtime.rs crates/agent_world/src/bin/world_chain_runtime/world_chain_runtime_tests.rs`（`world_chain_runtime.rs` 1198 行，满足单文件不超过 1200 行约束）。
- 遗留事项：
  - 无（本项目 T0~T2 已全部完成）。

- 时刻：2026-03-02 16:57:26 CST
- 完成内容：完成“Viewer Web 构建体积裁剪（2026-03-02）”T0~T3（建档 + wasm 路径裁剪 + 体积对比 + 收口）。
  - 新建设计文档：`doc/world-simulator/viewer-web-build-pruning-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/viewer-web-build-pruning-2026-03-02.project.md`（状态已收口为“已完成（T0~T3）”）。
  - 代码裁剪：
    - `crates/agent_world/src/lib.rs`：wasm 下剥离 `runtime`/`consensus_action_payload`。
    - `crates/agent_world/src/viewer/mod.rs`：wasm 下剥离 `demo/live/server/web_bridge` 导出。
    - `crates/agent_world/src/simulator/mod.rs` + `crates/agent_world/src/simulator/llm_defaults.rs`：wasm 保留 LLM 默认常量导出，`llm_agent` 改为 native-only 编译。
    - `crates/agent_world/src/simulator/kernel/module_lifecycle.rs`：wasm 下 `compile_module_artifact_from_source` 显式拒绝，避免 runtime 依赖回流。
    - `crates/agent_world/Cargo.toml`：native-only 依赖下沉到 `cfg(not(target_arch = "wasm32"))`，并补 wasm `getrandom(js)`。
  - 结果：wasm 依赖树从 1838 行降到 1375 行，`agent_world_node/libp2p/async-openai/reqwest/tokio/tungstenite` 不再出现在 viewer wasm 依赖树；wasm 体积由 `70,761,540` bytes 降至 `70,754,079` bytes（-7,461 bytes，-0.0105%）。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/viewer-web-build-pruning-2026-03-02.md doc/world-simulator/viewer-web-build-pruning-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/world-simulator/viewer-web-build-pruning-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/viewer-web-build-pruning-2026-03-02.project.md`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_viewer --target wasm32-unknown-unknown`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_viewer`。
  - test_tier_required：`cd crates/agent_world_viewer && env -u NO_COLOR trunk build --release --dist ../../output/web-size-audit-20260302-pruned`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo tree -p agent_world_viewer --target wasm32-unknown-unknown -e normal`（对比 `/tmp/aw_viewer_tree.txt` 与 `/tmp/aw_viewer_tree_pruned.txt`）。
- 遗留事项：
  - 当前 wasm 体积大头仍在 Bevy 默认功能集与嵌入字体（`ms-yahei.ttf`）路径，建议下一任务优先做 `bevy` feature 精细化和字体策略拆分。

- 时刻：2026-03-02 17:32:10 CST
- 完成内容：完成“Viewer Web 构建体积裁剪 Phase 2（2026-03-02）”T2（字体改为运行时资源加载并回归）。
  - 更新 `crates/agent_world_viewer/src/copyable_text.rs`：移除 `include_bytes!` 大字体内联，改为 `AssetServer` 运行时加载 `fonts/ms-yahei.ttf`；egui CJK 字体改为等待 `Assets<Font>` 可用后一次性注入。
  - 更新 `crates/agent_world_viewer/src/main_ui_runtime.rs`：`setup_3d_scene/setup_ui` 统一改用运行时字体句柄，避免把字体字节打进 wasm 数据段。
  - 更新 `crates/agent_world_viewer/src/egui_right_panel.rs`：接入 `Viewer3dAssets` 与 `Assets<Font>`，只在字体资源就绪后初始化 egui CJK 字体。
  - 更新 `crates/agent_world_viewer/src/main.rs`：同步字体加载函数导入。
  - 更新项目管理文档：`doc/world-simulator/viewer-web-build-pruning-phase2-2026-03-02.project.md`，标记 T2 完成并推进到 T3。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_viewer`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_viewer --target wasm32-unknown-unknown`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_viewer copyable_text::tests:: -- --nocapture`。
- 遗留事项：
  - 执行 T3：运行 `trunk build --release` 对比 wasm/dist 体积并完成文档收口。

- 时刻：2026-03-02 17:45:39 CST
- 完成内容：完成“Viewer Web 构建体积裁剪 Phase 2（2026-03-02）”T3（体积验证 + 文档收口 + 结项）。
  - 更新 `crates/agent_world_viewer/index.html`：新增 trunk `copy-file`，仅拷贝 `assets/fonts/ms-yahei.ttf` 到 dist 的 `assets/fonts/ms-yahei.ttf`，保证运行时字体路径可用且避免整包 assets 入包。
  - 执行 `trunk build --release`（phase2）并完成体积对比：
    - wasm：`70,754,079`（phase1）-> `48,934,067`（phase2），减少 `21,820,012` bytes（`-30.8392%`）。
    - dist：`70,881,376`（phase1）-> `64,104,437`（phase2），减少 `6,776,939` bytes（`-9.5610%`）。
    - 字体资产：`output/web-size-audit-20260302-phase2/assets/fonts/ms-yahei.ttf`，`15,044,440` bytes。
  - 更新设计文档：`doc/world-simulator/viewer-web-build-pruning-phase2-2026-03-02.md`，补充“完成态（2026-03-02）”。
  - 更新项目管理文档：`doc/world-simulator/viewer-web-build-pruning-phase2-2026-03-02.project.md`，标记 T3 完成并结项（T0~T3）。
- 测试：
  - test_tier_required：`cd crates/agent_world_viewer && env -u NO_COLOR trunk build --release --dist ../../output/web-size-audit-20260302-phase2`。
  - test_tier_required：`stat -c '%n %s' output/web-size-audit-20260302-baseline/*_bg.wasm output/web-size-audit-20260302-pruned/*_bg.wasm output/web-size-audit-20260302-phase2/*_bg.wasm`。
  - test_tier_required：`du -sb output/web-size-audit-20260302-pruned output/web-size-audit-20260302-phase2`。
  - test_tier_required：`find output/web-size-audit-20260302-phase2 -maxdepth 5 -type f | sort`（确认字体落盘路径）。
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。

- 时刻：2026-03-02 20:36:04 CST
- 完成内容：完成“全仓文档 PRD 体系迁移（模块入口统一）”任务，建立模块级统一入口并登记 legacy 来源。
  - 已提交并生效流程基线：`AGENTS.md`（PRD 硬切换，模块固定 `doc/<module>/prd.md` 与 `doc/<module>/prd.project.md`）。
  - 新增模块 PRD 文档与项目管理文档（共 12 个模块，24 个文件）：`core`、`engineering`、`game`、`nonviewer`、`p2p`、`playability_test_result`、`readme`、`scripts`、`site`、`testing`、`world-runtime`、`world-simulator`。
  - 每个模块 `prd.md` 已包含 PRD 标准章节（Executive Summary、User Experience & Functionality、AI System Requirements、Technical Specifications、Risks & Roadmap）与 Legacy Sources 清单。
  - 每个模块 `prd.project.md` 已包含任务拆解、依赖、状态，并映射 PRD-ID。
  - 校验通过：所有有历史 md 文档的模块均存在 `prd.md` 与 `prd.project.md`；新增 PRD 文档行数最大为 `doc/world-simulator/prd.md` 的 333 行，未超过 500 行约束。
- 测试：
  - test_tier_required：模块 PRD 覆盖校验脚本（检查每个有历史 md 的模块均具备 `prd.md` 与 `prd.project.md`）。
  - test_tier_required：`wc -l` 全量校验新增 PRD 文档行数上限（最大 333 行）。
- 遗留事项：
  - 后续迭代需持续把高频变更从 legacy 文档沉淀到各模块 `prd.md` 主体章节（已在各模块 `prd.project.md` 的 TASK-004 记录）。
