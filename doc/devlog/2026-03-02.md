- 时刻：2026-03-02 13:26:45 CST
- 完成内容：完成“客户端启动器中英文切换与必填配置校验（2026-03-02）”T0（建档）。
  - 新建设计文档：`doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.md doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.project.md`。
- 遗留事项：
  - 执行 T1：实现启动器中英文切换。

- 时刻：2026-03-02 13:31:19 CST
- 完成内容：完成“客户端启动器中英文切换与必填配置校验（2026-03-02）”T1（启动器 UI 中英文切换）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：新增 `UiLanguage` 与环境变量语言解析（`AGENT_WORLD_CLIENT_LAUNCHER_LANG` / `LANG`），默认回落中文。
  - 新增语言切换控件（中文/English），并将标题、字段标签、按钮、状态文案切到双语渲染。
  - 新增 `LauncherStatus` 状态枚举，状态文本按当前语言动态显示，避免切换语言后状态文案不一致。
  - 补充单测：语言标签解析与状态本地化断言。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs`（968 行，满足单文件不超过 1200 行约束）。
- 遗留事项：
  - 执行 T2：新增必填配置阻断校验与提示清单。

- 时刻：2026-03-02 13:35:41 CST
- 完成内容：完成“客户端启动器中英文切换与必填配置校验（2026-03-02）”T2（必填配置阻断校验 + 提示清单 + 单测）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：新增 `ConfigIssue` 与 `collect_required_config_issues`，覆盖 scenario/live/web/viewer/launcher 与 chain 相关必填与格式校验。
  - 启动器 UI 新增启动前校验提示区：
    - 当校验通过时显示“可启动”；
    - 当存在阻断项时逐条列出问题（中英文），并禁用“启动”按钮。
  - `start_process` 增加防线：即使按钮层误触发，也会先做 preflight 校验并写入日志后拒绝启动。
  - 补充单测：缺失/非法配置项收集与合法配置放行；并整理文件空行使 `main.rs` 回落到 1190 行（满足单文件约束）。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs`（1190 行）。
- 遗留事项：
  - 执行 T3：回归测试、文档收口与项目结项。

- 时刻：2026-03-02 13:38:05 CST
- 完成内容：完成“客户端启动器中英文切换与必填配置校验（2026-03-02）”T3（回归测试 + 文档收口 + 结项）。
  - 更新项目管理文档：`doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.project.md`，T3 勾选完成，状态更新为“已完成（T0~T3）”。
  - 更新设计文档：`doc/world-simulator/game-client-launcher-i18n-required-config-2026-03-02.md`，补充“完成态（2026-03-02）”。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p agent_world_client_launcher`。
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。

- 时刻：2026-03-02 14:58:16 CST
- 完成内容：完成“客户端启动器优雅退出与级联进程关闭（2026-03-02）”T0（建档）。
  - 新建设计文档：`doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.md`。
  - 新建项目管理文档：`doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.project.md`。
  - 项目状态置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.md doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.project.md`。
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.md`。
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/game-client-launcher-graceful-stop-2026-03-02.project.md`。
- 遗留事项：
  - 执行 T1：实现优雅中断 + 超时强杀的停止流程。

- 时刻：2026-03-02 15:01:04 CST
- 完成内容：完成“客户端启动器优雅退出与级联进程关闭（2026-03-02）”T1（停止流程改造）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：`stop_child_process` 由“直接 kill”改为“优雅中断（Unix 下发送 SIGINT）-> 超时轮询等待 -> 强杀兜底”。
  - 新增停止策略常量：`GRACEFUL_STOP_TIMEOUT_MS`、`STOP_POLL_INTERVAL_MS`。
  - 新增 `send_interrupt_signal`（Unix: `libc::kill(pid, SIGINT)`；非 Unix: no-op fallback）。
  - 窗口关闭路径（`Drop`）与“停止”按钮均复用 `stop_child_process`，统一优雅退出语义。
  - 更新 `crates/agent_world_client_launcher/Cargo.toml`：新增 Unix 目标依赖 `libc`。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
- 遗留事项：
  - 执行 T2：补充停止逻辑测试，并处理 `main.rs` 1200 行约束（当前 `wc -l` 为 1229）。

- 时刻：2026-03-02 15:04:58 CST
- 完成内容：完成“客户端启动器优雅退出与级联进程关闭（2026-03-02）”T2（测试补充 + 行数约束收敛）。
  - 将启动器测试从 `main.rs` 拆分至 `crates/agent_world_client_launcher/src/tests.rs`，`main.rs` 仅保留 `#[cfg(test)] mod tests;`。
  - 新增停止逻辑测试：
    - `stop_child_process_handles_already_exited_child`
    - `stop_child_process_sends_interrupt_for_graceful_exit`（Unix）
  - 行数约束收敛：`crates/agent_world_client_launcher/src/main.rs` 由 1232 行降至 1016 行，满足单文件不超过 1200 行约束。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs crates/agent_world_client_launcher/src/tests.rs`。
- 遗留事项：
  - 执行 T3：回归测试、文档收口与项目结项。
