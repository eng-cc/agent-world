- 时刻：2026-03-03 00:12:28 CST
- 完成内容：完成“模块 PRD 从迁移模板切换为设计文档（2026-03-03）”任务。
  - 重写 `doc/README.md`，改为模块设计导航入口。
  - 重写 12 个模块 `doc/<module>/prd.md`，移除迁移导向语义，统一改为模块设计 PRD。
  - 重写 12 个模块 `doc/<module>/prd.project.md`，任务拆解改为设计落地任务并保留 PRD-ID 映射。
  - 按要求将迁移过程信息仅保留在 devlog，不再作为模块 PRD 主体内容。
- 测试：
  - test_tier_required：`rg -n "## 1\. Executive Summary|## 2\. User Experience & Functionality|## 3\. AI System Requirements \(If Applicable\)|## 4\. Technical Specifications|## 5\. Risks & Roadmap" doc/*/prd.md`。
  - test_tier_required：`rg -n "迁移|legacy|Legacy Sources" doc/*/prd.md doc/*/prd.project.md doc/README.md`（期望无结果）。
  - test_tier_required：`for f in doc/*/prd.md doc/*/prd.project.md doc/README.md; do wc -l "$f"; done`（校验单文档 <= 500 行）。
- 遗留事项：
  - 继续执行各模块 `prd.project.md` 中下一任务（TASK-*-002 起）。

- 时刻：2026-03-03 10:39:10 CST
- 完成内容：完成“core 作为项目全局总 PRD 入口”任务。
  - 更新 `doc/core/prd.md`：新增项目模块地图、关键端到端链路、关键分册导航与全局术语。
  - 更新 `doc/core/prd.project.md`：新增并完成 TASK-CORE-002，聚焦全局总览入口落地。
  - 更新 `doc/README.md`：将 `doc/core/prd.md` 上移为推荐阅读第二步（总览优先）。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`。
  - test_tier_required：`for f in doc/core/prd.md doc/core/prd.project.md doc/README.md; do wc -l "$f"; done`。
- 遗留事项：
  - 执行 `doc/core/prd.project.md` 的 TASK-CORE-003（跨模块变更影响检查清单）。

- 时刻：2026-03-03 10:49:09 CST
- 完成内容：完成“doc 目录组织重构（归档下沉 + 主题子目录 + 索引补齐）”任务。
  - 根目录历史治理文档迁入 `doc/archive/root-history/`，并新增 `doc/archive/README.md`。
  - `doc/world-simulator/` 增加主题子目录：`viewer/`、`llm/`、`launcher/`、`scenario/`、`kernel/`，并按前缀完成迁移。
  - `doc/p2p/` 增加主题子目录：`distfs/`、`node/`、`observer/`、`blockchain/`、`token/`、`viewer-live/`、`consensus/`、`distributed/`、`network/`，并按前缀完成迁移。
  - 新增模块目录索引：`doc/world-simulator/README.md`、`doc/p2p/README.md`。
  - 更新文档内路径引用（排除 `doc/devlog/` 快照），对齐迁移后的新路径。
- 测试：
  - test_tier_required：`for d in doc/world-simulator doc/p2p; do find "$d" -maxdepth 3 -type f -name '*.project.md' | while read -r pf; do mf="${pf%.project.md}.md"; [ -f "$mf" ] || echo "missing pair: $pf"; done; done`（无缺失）。
  - test_tier_required：`rg -n "doc/world-simulator/(viewer-|llm-|game-client-launcher-|game-unified-launcher-|launcher-chain-runtime-decouple-|scenario-|kernel-)" --glob '!doc/devlog/**' doc README.md testing-manual.md world-rule.md AGENTS.md`（无残留）。
  - test_tier_required：`rg -n "doc/p2p/(distfs-|node-|observer-|blockchain-|mainchain-token-|world-viewer-live-|consensus-|distributed-|net-runtime-bridge-)" --glob '!doc/devlog/**' doc README.md testing-manual.md world-rule.md AGENTS.md`（无残留）。
  - test_tier_required：`./scripts/doc-governance-check.sh`。
- 遗留事项：
  - 后续可继续将 `doc/world-simulator/` 与 `doc/p2p/` 根目录未分组专题逐步下沉到主题子目录。

- 时刻：2026-03-03 10:54:40 CST
- 完成内容：完成“doc 重组收口检查与误链修复”任务。
  - 修复 `doc/world-simulator/viewer/viewer-live-tick-driven-doc-archive-2026-02-27.project.md` 中 Phase1~7 依赖路径，改回 `doc/world-simulator/archive/`。
  - 修复 5 份 viewer 项目文档中的 playability 卡片依赖路径，统一改为 `doc/playability_test_result/archive/2026-02/`。
  - 复核 `doc/README.md`、`doc/core/prd.md`、`doc/world-simulator/prd*.md`、`doc/p2p/prd*.md`、`testing-manual.md`、`doc/world-runtime*.md` 的关键引用可达性。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`。
  - test_tier_required：关键入口文档引用可达扫描（`doc/README.md`、`doc/core/prd.md`、`doc/world-simulator/prd*.md`、`doc/p2p/prd*.md`、`doc/world-runtime*.md`、`doc/viewer-manual.md`、`testing-manual.md`、模块 README）。
  - test_tier_required：主题子目录（`doc/world-simulator/*`、`doc/p2p/*`）内 `doc/...` 依赖路径可达扫描（仅保留 `doc/devlog/YYYY-MM-DD.md` 占位例外）。
- 遗留事项：
  - 归档历史文档中的部分旧路径引用仍含历史断链，后续可按“读到再修”策略增量治理，不阻塞当前目录重组落地。

- 时刻：2026-03-03 11:14:52 CST
- 完成内容：完成“启动器链上转账 PRD 建模与任务拆解”文档任务。
  - 更新 `doc/world-simulator/prd.md`，新增 `PRD-WORLD_SIMULATOR-004/005`，补齐启动器链上转账的 Problem/Solution、用户故事、验收标准、技术集成点与安全约束。
  - 更新 `doc/world-simulator/prd.project.md`，新增并拆解 `TASK-WORLD_SIMULATOR-005~009`，明确转账 API、运行时账本动作、启动器 UI 与闭环测试任务。
  - 同步项目状态：将转账实现入口优先任务标记为 `TASK-WORLD_SIMULATOR-006`。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`（通过）。
  - test_tier_required：`rg -n "## 1\\. Executive Summary|## 2\\. User Experience & Functionality|## 3\\. AI System Requirements \\(If Applicable\\)|## 4\\. Technical Specifications|## 5\\. Risks & Roadmap" doc/world-simulator/prd.md`（结构完整）。
  - test_tier_required：`for f in doc/world-simulator/prd.md doc/world-simulator/prd.project.md; do wc -l "$f"; done`（均 <= 500 行）。
- 遗留事项：
  - 开始执行 `TASK-WORLD_SIMULATOR-006`：在 `world_chain_runtime` 落地转账提交接口与对应单元测试。

- 时刻：2026-03-03 11:28:31 CST
- 完成内容：完成“world-simulator PRD 分册化（启动器链上转账专题）”文档任务。
  - 将 `doc/world-simulator/prd.md` 调整为主入口总览，保留模块级边界与分册索引。
  - 新增分册 `doc/world-simulator/prd/launcher/blockchain-transfer.md`，承载 `PRD-WORLD_SIMULATOR-004/005` 的完整条款（接口、安全、验收、路线图）。
  - 更新 `doc/world-simulator/prd.project.md` 依赖，挂载新分册路径，保持任务追踪闭环。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`。
  - test_tier_required：`for f in doc/world-simulator/prd.md doc/world-simulator/prd.project.md doc/world-simulator/prd/launcher/blockchain-transfer.md; do wc -l "$f"; done`（均 <= 500 行）。
  - test_tier_required：`rg -n "## 1\\. Executive Summary|## 2\\. User Experience & Functionality|## 3\\. AI System Requirements \\(If Applicable\\)|## 4\\. Technical Specifications|## 5\\. Risks & Roadmap" doc/world-simulator/prd.md doc/world-simulator/prd/launcher/blockchain-transfer.md`。
- 遗留事项：
  - 继续执行 `TASK-WORLD_SIMULATOR-006`：实现链运行时转账提交接口并补单元测试。

- 时刻：2026-03-03 11:39:58 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-006`（`world_chain_runtime` 转账提交接口）。
  - 新增 `crates/agent_world/src/bin/world_chain_runtime/transfer_submit_api.rs`，实现 `POST /v1/chain/transfer/submit` 的请求解析、字段校验、结构化响应、共识 payload 构建与提交流程。
  - 在 `crates/agent_world/src/bin/world_chain_runtime.rs` 接入转账提交路由，不影响现有 `status/balances/feedback` 接口。
  - runtime 新增 `Action::TransferMainToken` 变体，并在事件路由中临时返回结构化拒绝（占位，避免执行期 panic，待 `TASK-WORLD_SIMULATOR-007` 落地真实账本语义）。
  - 更新 `doc/world-simulator/prd.project.md` 与 `doc/world-simulator/prd/launcher/blockchain-transfer.md`，同步任务状态与实施进展。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_chain_runtime transfer_submit_api`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_chain_runtime`。
  - test_tier_required：`wc -l crates/agent_world/src/bin/world_chain_runtime.rs`（1200 行，满足单文件上限约束）。
- 遗留事项：
  - 开始执行 `TASK-WORLD_SIMULATOR-007`：落地 main token 转账事件与账本状态更新（余额约束、nonce anti-replay、回归测试）。

- 时刻：2026-03-03 11:50:18 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-007`（runtime 主 token 转账语义）。
  - `runtime::Action` / `DomainEvent` 新增主 token 转账链路：`TransferMainToken` 与 `MainTokenTransferred`，并接入 action label / event label / event routing。
  - `action_to_event_core` 新增转账动作评估：字段约束、preview apply 校验、结构化拒绝原因。
  - `apply_domain_event_main_token` 新增主 token 转账账本更新：余额扣加、来源账户 nonce anti-replay（`main_token_transfer_nonces`）、不足余额拒绝、溢出保护。
  - `WorldState` 新增 `main_token_transfer_nonces` 持久字段，`World` 新增查询接口 `main_token_last_transfer_nonce`。
  - `runtime/tests/main_token.rs` 新增 3 条回归：成功转账、余额不足拒绝、nonce 重放拒绝。
  - 顺带整理 `world_chain_runtime`：将 `feedback submit` 错误响应写回 helper 下沉到子模块，主文件行数回落到 1200 以内约束。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world main_token_transfer_action`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world runtime::tests::main_token::`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_chain_runtime`。
  - test_tier_required：`wc -l crates/agent_world/src/bin/world_chain_runtime.rs`（1194 行，满足单文件上限约束）。
- 遗留事项：
  - 开始执行 `TASK-WORLD_SIMULATOR-008`：在 `agent_world_client_launcher` 落地转账 UI 与提交流程（输入校验、状态提示、错误展示）。

- 时刻：2026-03-03 12:00:30 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-008`（启动器转账 UI 与提交流程）。
  - 新增 `transfer_entry.rs`：封装转账草稿、输入校验、HTTP 提交与响应解析（`/v1/chain/transfer/submit`）。
  - 新增 `transfer_window.rs`：转账窗口 UI（`from/to/amount/nonce` 输入、错误提示、成功/失败状态展示）。
  - 将反馈窗口逻辑拆分至 `feedback_window.rs`，主界面新增“转账”按钮与窗口入口，避免 `main.rs` 继续膨胀。
  - `main.rs` 接入转账窗口状态机（`transfer_draft` / `transfer_submit_state` / `transfer_window_open`）。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo fmt --all -- --check`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`wc -l crates/agent_world_client_launcher/src/main.rs`（992 行，满足单文件上限约束）。
- 遗留事项：
  - 开始执行 `TASK-WORLD_SIMULATOR-009`：完成启动器-链运行时转账闭环测试（`test_tier_required`）并沉淀测试证据。

- 时刻：2026-03-03 12:11:44 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-009`（启动器-链运行时转账闭环测试与证据沉淀）。
  - `crates/agent_world_client_launcher/src/transfer_entry.rs` 新增 TCP 闭环测试，验证启动器发起转账请求的 HTTP 路径/载荷以及成功/失败响应解析。
  - `crates/agent_world/src/bin/world_chain_runtime/transfer_submit_api.rs` 新增接口联动测试，覆盖 `POST /v1/chain/transfer/submit` 成功提交到 runtime 共识动作与非法请求拒绝分支。
  - 同步更新 `doc/world-simulator/prd.project.md` 与 `doc/world-simulator/prd/launcher/blockchain-transfer.md`，将 TASK-009 状态与测试证据归档到 PRD 追踪链路。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher transfer_entry::tests:: -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --tests --features test_tier_required transfer_submit_api::tests:: -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world --tests --features test_tier_required main_token_transfer_action_ -- --nocapture`。
- 遗留事项：
  - 开始执行 `TASK-WORLD_SIMULATOR-002`：对齐场景系统、Viewer、启动器统一验收清单。

- 时刻：2026-03-03 12:15:45 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-002`（场景系统 / Viewer / 启动器统一验收清单）。
  - 新增 `doc/world-simulator/prd/acceptance/unified-checklist.md`，统一 G1~G5 验收门禁（场景、Viewer Web 闭环、launcher、文档治理）及证据模板。
  - 更新 `doc/world-simulator/prd.md`：挂载统一清单分册，并补充 SC/AC 对统一验收口径的要求。
  - 更新 `doc/world-simulator/prd.project.md`：标记 TASK-002 完成，优先任务推进为 `TASK-WORLD_SIMULATOR-003`。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`。
  - test_tier_required：`for f in doc/world-simulator/prd.md doc/world-simulator/prd.project.md doc/world-simulator/prd/acceptance/unified-checklist.md; do wc -l "$f"; done`（单文档 <= 500 行）。
  - test_tier_required：`rg -n "unified-checklist|TASK-WORLD_SIMULATOR-002|当前优先任务" doc/world-simulator/prd.md doc/world-simulator/prd.project.md`。
- 遗留事项：
  - 开始执行 `TASK-WORLD_SIMULATOR-003`：固化 Web-first 闭环与 LLM 链路的测试证据模板。

- 时刻：2026-03-03 12:18:47 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-003`（Web-first + LLM 测试证据模板固化）。
  - 新增 `doc/world-simulator/prd/acceptance/web-llm-evidence-template.md`，提供 S6（Web-first）与 S8（LLM 链路）证据卡模板及合并发布结论模板。
  - 更新 `doc/world-simulator/prd.md`：将证据模板分册纳入索引，并补充 SC-8 / AC-7。
  - 更新 `doc/world-simulator/prd/acceptance/unified-checklist.md` 与 `doc/world-simulator/prd.project.md`：关联模板入口并标记 TASK-003 完成，下一优先任务推进为 `TASK-WORLD_SIMULATOR-004`。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`。
  - test_tier_required：`for f in doc/world-simulator/prd.md doc/world-simulator/prd.project.md doc/world-simulator/prd/acceptance/unified-checklist.md doc/world-simulator/prd/acceptance/web-llm-evidence-template.md; do wc -l "$f"; done`（单文档 <= 500 行）。
  - test_tier_required：`rg -n "web-llm-evidence-template|TASK-WORLD_SIMULATOR-003|当前优先任务" doc/world-simulator/prd.md doc/world-simulator/prd.project.md doc/world-simulator/prd/acceptance/unified-checklist.md`。
- 遗留事项：
  - 开始执行 `TASK-WORLD_SIMULATOR-004`：建立 simulator 体验质量趋势跟踪。

- 时刻：2026-03-03 12:21:42 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-004`（simulator 体验质量趋势跟踪）。
  - 新增 `doc/world-simulator/prd/quality/experience-trend-tracking.md`，定义体验质量趋势面板（Web 启动、闭环稳定性、LLM 指标、玩法覆盖）。
  - 更新 `doc/world-simulator/prd.md`：挂载趋势跟踪分册并补充 SC-9 / AC-8。
  - 更新 `doc/world-simulator/prd.project.md`：标记 TASK-004 完成，当前任务清单收口为“无待办”。
  - 更新 `doc/world-simulator/prd/acceptance/unified-checklist.md`：补充趋势跟踪入口引用，形成“清单 -> 证据模板 -> 趋势追踪”链路。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`。
  - test_tier_required：`for f in doc/world-simulator/prd.md doc/world-simulator/prd.project.md doc/world-simulator/prd/acceptance/unified-checklist.md doc/world-simulator/prd/quality/experience-trend-tracking.md; do wc -l "$f"; done`（单文档 <= 500 行）。
  - test_tier_required：`rg -n "experience-trend-tracking|TASK-WORLD_SIMULATOR-004|当前优先任务|并行待办" doc/world-simulator/prd.md doc/world-simulator/prd.project.md doc/world-simulator/prd/acceptance/unified-checklist.md`。
- 遗留事项：
  - 当前 `doc/world-simulator/prd.project.md` 任务清单已完成，等待新增需求任务。
- 时刻：2026-03-03 12:01:42 CST
- 完成内容：完成“PRD 技能补强并整理当前工作区技能改动提交”任务。
  - 更新 `.agents/skills/prd/SKILL.md`，补充 Developer-Ready Completeness、扩展 Strict PRD Schema（功能规格/边界异常/NFR/验证与决策记录）以及 Review Gate 强制自检门禁。
  - 纳入并提交 `.agents/skills/prd/check.md` 审查清单模板。
  - 纳入并提交 `.agents/skills/game-changing-features/` 新技能文档（`SKILL.md`、`README.md`）。
  - 按当前工作区状态提交 `skills-lock.json` 删除。
- 测试：
  - test_tier_required：`for f in .agents/skills/prd/SKILL.md .agents/skills/prd/check.md .agents/skills/game-changing-features/SKILL.md .agents/skills/game-changing-features/README.md doc/devlog/2026-03-03.md; do wc -l "$f"; done`（单文档均 <= 500 行）。
  - test_tier_required：`rg -n "^# |^## |Review Gate|Strict PRD Schema|Developer-Ready Completeness|10x Mode|Game-Changing Features" .agents/skills/prd/SKILL.md .agents/skills/prd/check.md .agents/skills/game-changing-features/SKILL.md .agents/skills/game-changing-features/README.md`（关键章节与门禁条目存在）。
- 遗留事项：
  - 后续如需发布到团队技能库，可补充 `game-changing-features` 与仓库治理规则（PRD 流程）的一致性约束说明。
- 时刻：2026-03-03 11:11:54 CST
- 完成内容：完成“doc 目录语义收敛（nonviewer 重命名 + README 补齐 + 根入口去歧义）”任务。
  - 模块目录重命名：`doc/nonviewer/` -> `doc/headless-runtime/`，并同步更新 `doc/README.md`、`doc/core/prd.md` 的模块矩阵命名。
  - 更新 `doc/headless-runtime/prd.md`、`doc/headless-runtime/prd.project.md`、`doc/headless-runtime/README.md`：统一为 `headless-runtime（原 nonviewer）` 口径，并保留 `PRD-NONVIEWER-*` 追踪连续性。
  - 补齐缺失模块索引：新增 `doc/core/README.md`、`doc/game/README.md`、`doc/readme/README.md`、`doc/scripts/README.md`、`doc/site/README.md`、`doc/testing/README.md`、`doc/world-runtime/README.md`。
  - 根目录入口去歧义：在 `doc/world-runtime.md`、`doc/world-runtime.project.md`、`doc/world-simulator.md`、`doc/world-simulator.project.md` 增加 legacy 说明，并在 `doc/README.md` 明确主入口优先级。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`。
  - test_tier_required：`for d in doc/*; do [ -d "$d" ] || continue; b=$(basename "$d"); case "$b" in archive|devlog) continue;; esac; [ -f "$d/README.md" ] || echo "$d"; done`（校验模块 README 覆盖）。
  - test_tier_required：`rg -n "doc/nonviewer/" doc --glob '!doc/devlog/**' --glob '!doc/archive/root-history/**'`（期望无结果）。
  - test_tier_required：变更文件内 `doc/...` 引用可达性扫描（无断链）。
- 遗留事项：
  - `headless-runtime` 下仍有历史文件名 `nonviewer-*`，后续可在不影响追溯的前提下分批重命名为 `headless-runtime-*`。

- 时刻：2026-03-03 11:34:16 CST
- 完成内容：完成“PRD 硬切换合规修复（问题1）”任务。
  - 补齐 12 份 `2026-03-02` 在研/新增 `.project.md` 任务项的 PRD-ID 映射，覆盖 `testing` 与 `world-simulator` 两个模块。
  - 统一任务项写法为 `任务编号 (PRD-xxx)`，确保任务到模块 PRD 可直接追溯。
- 测试：
  - test_tier_required：`find doc -name '*2026-03-02*.project.md' ! -path '*/archive/*' | sort | while read -r f; do if ! rg -q 'PRD-' "$f"; then echo "$f"; fi; done`（无输出）。
  - test_tier_required：`./scripts/doc-governance-check.sh`（OK）。
- 遗留事项：
  - 继续执行问题2：将模块级 `prd.project.md` 与专题项目文档建立显式映射，减少“模块总任务”与“专题实做”脱节。

- 时刻：2026-03-03 11:37:08 CST
- 完成内容：完成“模块主项目文档与专题任务映射收敛（问题2）”任务。
  - 更新 `doc/testing/prd.project.md`：新增 TASK-TESTING-005 与“2026-03-02 批次”专题任务映射（3/3）。
  - 更新 `doc/world-simulator/prd.project.md`：新增 TASK-WORLD_SIMULATOR-005 与“2026-03-02 批次”专题任务映射（9/9）。
  - 在两个模块状态区增加“专题映射状态”，将模块总任务与专题项目文档建立显式追踪关系。
- 测试：
  - test_tier_required：`rg -n "TASK-(TESTING|WORLD_SIMULATOR)-005|SUBTASK-(TESTING|WORLD_SIMULATOR)-20260302" doc/testing/prd.project.md doc/world-simulator/prd.project.md`。
  - test_tier_required：`./scripts/doc-governance-check.sh`（OK）。
- 遗留事项：
  - 可按同样模式继续把其他模块的“近期专题任务”补到对应 `doc/<module>/prd.project.md`，形成全模块统一索引。

- 时刻：2026-03-03 12:09:24 CST
- 完成内容：完成“doc 根目录入口终版收敛（问题1/2）”任务。
  - 将根目录活跃文档迁入所属模块：
    - `doc/viewer-manual.md` -> `doc/world-simulator/viewer/viewer-manual.md`
    - `doc/game-test.md` -> `doc/playability_test_result/game-test.md`
    - `doc/game-test.project.md` -> `doc/playability_test_result/game-test.project.md`
    - `doc/playability_test_card.md` -> `doc/playability_test_result/playability_test_card.md`
    - `doc/playability_test_manual.md` -> `doc/playability_test_result/playability_test_manual.md`
  - 根目录保留 5 个 legacy redirect 文件，仅做兼容跳转，不再承载活跃正文。
  - 更新 `doc/README.md` 根目录入口说明，显式声明 legacy redirect 到模块主入口。
  - 批量更新活跃文档、脚本与站点引用路径（含 `README.md`、`testing-manual.md`、`scripts/site-manual-sync-check.sh`、`pages.yml`）。
  - 更新模块项目管理文档追踪：`doc/core/prd.project.md`、`doc/playability_test_result/prd.project.md`、`doc/world-simulator/prd.project.md`。
  - 更新 `scripts/doc-governance-check.sh`，对白名单保留新旧 `game-test.project` 路径兼容。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`（OK）。
  - test_tier_required：`./scripts/site-manual-sync-check.sh`（OK）。
  - test_tier_required：`rg -n "doc/viewer-manual\.md|doc/game-test\.md|doc/game-test\.project\.md|doc/playability_test_card\.md|doc/playability_test_manual\.md" README.md testing-manual.md scripts .github site doc --glob '!doc/devlog/**' --glob '!doc/**/archive/**'`（仅 legacy redirect/导航声明命中）。
- 遗留事项：
  - 后续可继续将 `testing/site/world-runtime/readme` 等模块根目录平铺专题下沉到主题子目录，进一步统一层级结构。

- 时刻：2026-03-03 13:32:10 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-012`（反馈分布式提交连接拒绝回归约束测试）。
  - 更新 `crates/agent_world_client_launcher/src/feedback_entry.rs`：强化 `submit_feedback_with_fallback_writes_local_on_remote_failure` 断言，除本地落盘外，必须保留远端错误并包含 `connect chain status server failed` 与 `refused` 诊断签名。
  - 更新 `doc/world-simulator/prd.md`：新增 `AC-9`，将“链状态服务 `Connection refused` 时必须回落本地并保留错误签名”纳入验收条款。
  - 更新 `doc/world-simulator/prd.project.md`：新增并完成 `TASK-WORLD_SIMULATOR-012`，补充 `2026-03-03` 专题映射批次。
  - 更新 `doc/world-simulator/launcher/game-client-launcher-feedback-distributed-submit-2026-03-02.project.md`：新增并完成 `T4` 回归测试任务。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher submit_feedback_with_fallback_writes_local_on_remote_failure -- --nocapture`。
  - test_tier_required：`./scripts/doc-governance-check.sh`。
- 遗留事项：
  - 可选增强：在反馈窗口层增加“链服务不可达”显式 UI 提示（当前已通过日志与本地落盘保底）。

- 时刻：2026-03-03 14:18:16 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-013`（启动器顶部区块链启动状态可视化）。
  - 更新 `crates/agent_world_client_launcher/src/main.rs`：新增区块链状态机与探针逻辑（`已禁用/未启动/启动中/已就绪/不可达/配置错误`），并在顶部状态栏展示区块链状态；不可达与配置错误支持 hover 错误详情。
  - 新增链状态探针：按 1s 周期探测 `chain_status_bind` 的 `/v1/chain/status`，短超时校验，避免阻塞 UI 主循环。
  - 更新 `crates/agent_world_client_launcher/src/main_tests.rs`：新增链状态文案本地化测试、探针 HTTP 成功测试、探针连接失败测试。
  - 更新 PRD 追踪：`doc/world-simulator/prd.md` 新增 `AC-10`；`doc/world-simulator/prd.project.md` 新增并完成 `TASK-WORLD_SIMULATOR-013` 与专题映射；同步更新 `doc/world-simulator/launcher/game-client-launcher-desktop-2026-02-27(.project).md`。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world_client_launcher`。
  - test_tier_required：`./scripts/doc-governance-check.sh`。
- 遗留事项：
  - 当前探针按单地址探测（`to_socket_addrs()` 首地址），若后续需要多地址高可用，可扩展为逐地址尝试并汇总诊断信息。
- 时刻：2026-03-03 14:01:47 CST
- 完成内容：完成“doc 目录组织治理加固（问题1/2）”任务。
  - 新增文档组织门禁基线：`doc/.governance/doc-root-md-allowlist.txt` 与 `doc/.governance/module-root-md-allowlist.txt`，冻结当前根目录与模块根目录平铺 `.md` 集合。
  - 更新 `scripts/doc-governance-check.sh`：新增 allowlist 一致性校验，阻止 `doc/*.md` 与 `doc/<module>/*.md` 非预期新增。
  - 根目录历史入口收口：将 `doc/world-runtime*.md` 与 `doc/world-simulator*.md` 正文归档到 `doc/archive/root-history/*-legacy-2026-03-03*.md`，原路径改为兼容跳转入口。
  - 更新导航与追踪文档：`doc/README.md`、`doc/world-runtime/README.md`、`doc/world-simulator/README.md`、`doc/engineering/prd.md`、`doc/engineering/prd.project.md`。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`（OK）。
  - test_tier_required：`bash -n scripts/doc-governance-check.sh`（OK）。
  - test_tier_required：`for f in doc/world-runtime.md doc/world-runtime.project.md doc/world-simulator.md doc/world-simulator.project.md doc/README.md doc/engineering/prd.md doc/engineering/prd.project.md; do wc -l "$f"; done`（非 devlog 文档均 <= 500 行）。
- 遗留事项：
  - 现有模块根目录平铺文档通过 allowlist 机制“冻结”，后续可按模块分批迁移到主题子目录并同步收缩 allowlist。

- 时刻：2026-03-03 14:16:18 CST
- 完成内容：完成“模块根目录平铺文档迁移批次（world-simulator/p2p）”任务。
  - 将 `doc/world-simulator` 根目录专题文档批量下沉到主题目录：`scenario/`、`viewer/`、`llm/`、`kernel/`、`m4/`，并保持 `.md/.project.md` 成对迁移。
  - 将 `doc/p2p` 根目录专题文档批量下沉到主题目录：`distfs/`、`node/`、`consensus/`、`blockchain/`、`distributed/`、`network/`，历史治理文档下沉到 `archive/`。
  - 更新跨模块引用路径，修复 `core/headless-runtime/p2p/world-simulator` 等活跃文档中的旧路径引用。
  - 更新模块索引：`doc/world-simulator/README.md`、`doc/p2p/README.md`，明确模块根目录仅保留 `README.md`/`prd.md`/`prd.project.md`。
  - 重建文档治理基线：`doc/.governance/doc-root-md-allowlist.txt`、`doc/.governance/module-root-md-allowlist.txt`。
  - 更新工程治理追踪：`doc/engineering/prd.md`（SC-6/AC-6）与 `doc/engineering/prd.project.md`（TASK-ENGINEERING-005 完成）。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`（OK）。
  - test_tier_required：`for d in doc/world-simulator doc/p2p; do find "$d" -maxdepth 1 -type f -name '*.md' | sort; done`（根目录仅剩 `README/prd/prd.project`）。
  - test_tier_required：`rg -n 'doc/world-simulator/(agent-frag-initial-spawn-position|asteroid-fragment-renaming|chunked-fragment-generation|frag-resource-balance-onboarding|fragment-spacing|world-initialization|visualization|visualization-3d|rendering-physical-accuracy|module-visual-entities|minimal-system|material-multi-ledger-logistics|indirect-control-tick-lifecycle-long-term-memory|intent-distributed-runtime-closure-2026-02-27|location-electricity-pool-removal-and-radiation-plant|resource-kind-compound-hardware-hard-migration|rust-wasm-build-suite|social-fact-ledger-declarative-reputation)\\.md' --glob '!doc/**/archive/**' --glob '!doc/devlog/**'`（无命中）。
  - test_tier_required：`rg -n 'doc/p2p/(builtin-wasm-distfs-api-closure|builtin-wasm-distfs-storage|builtin-wasm-fetch-fallback-compile|builtin-wasm-identity-consensus|p2p-blockchain-security-hardening-2026-02-23|production-grade-blockchain-p2pfs-roadmap|production-grade-blockchain-p2pfs-phaseb-consensus-execution|production-grade-blockchain-p2pfs-phasec-distfs-proof-network|production-runtime-gap1234568-closure|readme-p1-network-production-hardening|readme-p2-node-net-stack-unification|p2p-doc-consolidation|pos-doc-archive-cleanup-2026-02-20)\\.md' --glob '!doc/**/archive/**' --glob '!doc/devlog/**'`（无命中）。
- 遗留事项：
  - `world-runtime/testing/site/readme` 等模块仍有大量历史平铺专题文档，需按同策略继续分批迁移并收缩 allowlist。

- 时刻：2026-03-03 14:43:32 CST
- 完成内容：完成“模块根目录平铺文档迁移批次（world-runtime/testing/site/readme/scripts/game/headless-runtime）”收口任务。
  - 完成模块根目录收口：`doc/world-runtime`、`doc/testing`、`doc/site`、`doc/readme`、`doc/scripts`、`doc/game`、`doc/headless-runtime` 根目录均仅保留 `README.md`、`prd.md`、`prd.project.md`。
  - 更新模块索引文档：`doc/testing/README.md`、`doc/site/README.md`、`doc/readme/README.md`、`doc/scripts/README.md`、`doc/game/README.md`，统一改为按主题子目录导航并声明根目录收口规则。
  - 回写 PRD/项目管理依赖路径：`doc/world-runtime/prd.md`、`doc/site/prd.md`、`doc/site/prd.project.md`、`doc/scripts/prd.md`、`doc/readme/prd.project.md`，去除已失效的根目录通配依赖。
  - 更新工程治理追踪：`doc/engineering/prd.md`、`doc/engineering/prd.project.md`，新增并完成 `TASK-ENGINEERING-006`（本批次迁移）。
  - 重建文档治理基线：`doc/.governance/doc-root-md-allowlist.txt`、`doc/.governance/module-root-md-allowlist.txt`。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`（OK）。
  - test_tier_required：`for d in doc/world-runtime doc/testing doc/site doc/readme doc/scripts doc/game doc/headless-runtime; do find "$d" -maxdepth 1 -type f -name '*.md'; done`（各模块根目录均为 3 个 `.md`）。
  - test_tier_required：基于 `/tmp/doc-move-map-batch2.txt` 的旧路径残留扫描（排除 `archive/devlog`）结果为 0。
- 遗留事项：
  - 现有 `archive/devlog` 历史材料中保留旧路径记录，按“历史快照不改写”策略维持，仅在需要恢复可执行链路时再做定向修订。

- 时刻：2026-03-03 15:32:52 CST
- 完成内容：完成“文档规则口径对齐（README 与 AGENTS 一致）”任务。
  - 更新 `doc/README.md` 维护约定：将“500 行上限”限定为仅作用于 `prd.md`，并保留 `prd.md` 超限拆分规则（`doc/<module>/prd/*.md`）。
  - 对齐 `AGENTS.md` 当前流程口径，避免把 `devlog` 等非 PRD 文档错误纳入 500 行限制。
- 测试：
  - test_tier_required：`./scripts/doc-governance-check.sh`（OK）。
- 遗留事项：
  - 无。

- 时刻：2026-03-03 16:31:51 CST
- 完成内容：完成 `TASK-WORLD_SIMULATOR-014`（启动器链/游戏独立启动 + 反馈链就绪门控建模与拆解）。
  - 更新 `doc/world-simulator/prd.md`：新增 `PRD-WORLD_SIMULATOR-006`，并补齐 `SC-10`、`AC-11`、`AC-12`，明确“链/游戏分离启动、启动器打开默认拉起链、反馈仅链就绪可用”验收边界。
  - 更新 `doc/world-simulator/prd.project.md`：新增并完成 `TASK-WORLD_SIMULATOR-014`，新增待执行 `TASK-WORLD_SIMULATOR-015`（代码实现与回归测试）。
- 测试：
  - test_tier_required：`wc -l doc/world-simulator/prd.md doc/world-simulator/prd.project.md`（均 <= 500 行）。
  - test_tier_required：`rg -n "PRD-WORLD_SIMULATOR-006|AC-11|AC-12|TASK-WORLD_SIMULATOR-014|TASK-WORLD_SIMULATOR-015" doc/world-simulator/prd.md doc/world-simulator/prd.project.md`。
  - test_tier_required：`./scripts/doc-governance-check.sh`（OK）。
- 遗留事项：
  - 执行 `TASK-WORLD_SIMULATOR-015`：在 `agent_world_client_launcher` 实现链/游戏独立按钮、默认自动拉起链、反馈入口链就绪门控与回归测试。
