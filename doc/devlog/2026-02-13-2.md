- 日期：2026-02-13
- 完成内容：完成 BMS-59（规则域按 `module_id` 独立工件装载切换）。
  - `crates/agent_world/src/runtime/m1_builtin_wasm_artifact.rs`：
    - 新增 `m1_builtin_wasm_module_artifact_bytes(module_id)`，按模块 ID 返回独立工件字节。
    - 新增 `register_m1_builtin_wasm_module_artifact(world, module_id)`，按模块 ID 注册工件并返回哈希。
  - `crates/agent_world/src/runtime/mod.rs`：
    - 导出 `register_m1_builtin_wasm_module_artifact` 供测试安装链路复用。
  - `crates/agent_world/src/runtime/tests/rules.rs`：
    - `install_m1_move_rule` / `install_m1_visibility_rule` / `install_m1_transfer_rule` 改为按对应 `module_id` 注册独立工件，不再复用单聚合工件注册入口。
  - 验证通过：
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::rules::`
- 遗留事项：执行 BMS-60，完成阶段五首轮回归收口、文档与 devlog 闭环。

- 日期：2026-02-13
- 完成内容：完成 BMS-60（阶段五首轮回归收口、文档与 devlog 闭环）。
  - 回归与校验通过：
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::rules::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::body::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::power_bootstrap::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::agent_default_modules::`
  - 文档更新：
    - `doc/world-runtime/builtin-wasm-crate-split.md`：补充 BMS-60 回归结果与阶段五首轮完成状态。
    - `doc/world-runtime/builtin-wasm-crate-split.project.md`：勾选 BMS-60，更新阶段状态/最近更新/下一步。
    - `doc/world-runtime.project.md`：勾选 BMS-60，并同步总项目状态。
- 遗留事项：阶段五下一批（`body/sensor/mobility` 按 `module_id` 独立工件装载）尚未拆解 BMS-61+ 任务。

- 日期：2026-02-13
- 完成内容：完成 BMS-61（阶段五第二轮设计与任务拆解）。
  - 文档更新：
    - `doc/world-runtime/builtin-wasm-crate-split.md`：新增“阶段五第二轮路线（BMS-61~BMS-63）”与本轮实施约束。
    - `doc/world-runtime/builtin-wasm-crate-split.project.md`：新增并勾选 BMS-61，补充 BMS-62/BMS-63 待办。
    - `doc/world-runtime.project.md`：同步新增 BMS-61~BMS-63 任务与阶段状态。
- 遗留事项：执行 BMS-62（`body/sensor/mobility` 按 `module_id` 独立工件装载切换）与 BMS-63 回归收口。

- 日期：2026-02-13
- 完成内容：完成 BMS-62（`body/sensor/mobility` 按 `module_id` 独立工件装载切换）。
  - 代码改动：
    - `crates/agent_world/src/runtime/world/bootstrap_power.rs`：
      - `install_m1_agent_default_modules` 中 `m1.sensor.basic` / `m1.mobility.basic` 改为按 `module_id` 装载独立工件字节。
      - 新增模块工件查找辅助，缺失时返回显式 `ModuleChangeInvalid`。
    - `crates/agent_world/src/runtime/tests/body.rs`：
      - `install_m1_body_module` 改为按 `M1_BODY_MODULE_ID` 注册独立工件。
    - `crates/agent_world/src/runtime/m1_builtin_wasm_artifact.rs` / `crates/agent_world/src/runtime/mod.rs`：
      - 暴露 `m1_builtin_wasm_module_artifact_bytes` 供 runtime 装载链路复用。
      - 清理不再使用的聚合工件注册测试辅助导出。
  - 验证通过：
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::body::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::agent_default_modules::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::power_bootstrap::`
- 遗留事项：执行 BMS-63，完成阶段五第二轮回归收口与文档闭环。

- 日期：2026-02-13
- 完成内容：完成 BMS-63（阶段五第二轮回归收口、文档与 devlog 闭环）。
  - 回归与校验通过：
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::rules::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::body::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::agent_default_modules::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::power_bootstrap::`
  - 文档更新：
    - `doc/world-runtime/builtin-wasm-crate-split.md`：补充 BMS-63 回归结果与阶段五第二轮完成状态。
    - `doc/world-runtime/builtin-wasm-crate-split.project.md`：勾选 BMS-63，更新阶段状态/最近更新/下一步。
    - `doc/world-runtime.project.md`：勾选 BMS-63，并同步总项目状态。
- 遗留事项：阶段五下一批（`memory/storage_cargo` 与 power 域按 `module_id` 独立工件装载）尚未拆解 BMS-64+ 任务。

- 日期：2026-02-13
- 完成内容：完成 BMS-64（阶段五第三轮设计与任务拆解）。
  - 文档更新：
    - `doc/world-runtime/builtin-wasm-crate-split.md`：新增“阶段五第三轮路线（BMS-64~BMS-66）”与实施约束。
    - `doc/world-runtime/builtin-wasm-crate-split.project.md`：新增并勾选 BMS-64，补充 BMS-65/BMS-66 待办。
    - `doc/world-runtime.project.md`：同步新增 BMS-64~BMS-66 任务与阶段状态。
- 遗留事项：执行 BMS-65（`memory/storage_cargo + power` 按 `module_id` 独立工件装载切换）与 BMS-66 回归收口。

- 日期：2026-02-13
- 完成内容：完成 BMS-65（`memory/storage_cargo + power` 按 `module_id` 独立工件装载切换）。
  - 代码改动：
    - `crates/agent_world/src/runtime/world/bootstrap_power.rs`：
      - `install_m1_power_bootstrap_modules` 中 `m1.power.radiation_harvest` / `m1.power.storage` 改为按 `module_id` 装载独立工件字节。
      - `install_m1_agent_default_modules` 中 `m1.memory.core` / `m1.storage.cargo` 改为按 `module_id` 装载独立工件字节。
      - 删除上述模块复用单聚合工件常量的硬编码，保持装载入口与 `module_id` 对齐。
  - 验证通过：
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::power_bootstrap::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::agent_default_modules::`
- 遗留事项：执行 BMS-66，完成阶段五第三轮回归收口、文档与 devlog 闭环。

- 日期：2026-02-13
- 完成内容：完成 BMS-66（阶段五第三轮回归收口、文档与 devlog 闭环）。
  - 回归与校验通过：
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::rules::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::body::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::agent_default_modules::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::power_bootstrap::`
  - 文档更新：
    - `doc/world-runtime/builtin-wasm-crate-split.md`：补充 BMS-66 回归结果与阶段五第三轮完成状态。
    - `doc/world-runtime/builtin-wasm-crate-split.project.md`：勾选 BMS-66，更新阶段状态/最近更新/下一步。
    - `doc/world-runtime.project.md`：勾选 BMS-66，并同步总项目状态。
- 遗留事项：阶段五第三轮已收口；若继续推进，需先拆解 BMS-67+ 任务。

- 日期：2026-02-13
- 完成内容：完成 BMS-67（阶段五第四轮设计与任务拆解）。
  - 文档更新：
    - `doc/world-runtime/builtin-wasm-crate-split.md`：新增“阶段五第四轮路线（BMS-67~BMS-69）”与实施约束。
    - `doc/world-runtime/builtin-wasm-crate-split.project.md`：新增并勾选 BMS-67，补充 BMS-68/BMS-69 待办。
    - `doc/world-runtime.project.md`：同步新增 BMS-67~BMS-69 任务与阶段状态。
- 遗留事项：执行 BMS-68（删除单聚合工件兼容入口）与 BMS-69 回归收口。

- 日期：2026-02-13
- 完成内容：完成 BMS-68（删除单聚合工件兼容入口，切换到 per-module-only）。
  - 代码改动：
    - `crates/agent_world/src/runtime/world/bootstrap_power.rs`：
      - 删除 `m1_builtin_modules.wasm` 单聚合工件 hash 校验逻辑；
      - 保留并收敛为按 `module_id` 装载独立工件。
    - `crates/agent_world/src/runtime/m1_builtin_wasm_artifact.rs` / `crates/agent_world/src/runtime/mod.rs`：
      - 删除单聚合工件常量与 hash 辅助函数导出；
      - 测试辅助导出改为 `#[cfg(all(test, feature = "wasmtime"))]`，消除无 wasmtime 目标警告。
    - `crates/agent_world/src/runtime/tests/power_bootstrap.rs`：
      - 删除单聚合工件 hash 对齐测试，保留模块清单一致性与 bootstrap 行为测试。
    - `scripts/ci-tests.sh`：
      - 删除 `./scripts/sync-m1-builtin-wasm-artifact.sh --check` 调用。
    - 删除文件：
      - `scripts/sync-m1-builtin-wasm-artifact.sh`
      - `crates/agent_world/src/runtime/world/artifacts/m1_builtin_modules.wasm`
      - `crates/agent_world/src/runtime/world/artifacts/m1_builtin_modules.wasm.sha256`
  - 验证通过：
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check`
    - `./scripts/check-include-warning-baseline.sh`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::power_bootstrap::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::agent_default_modules::`
- 遗留事项：执行 BMS-69，完成阶段五第四轮回归收口、文档与 devlog 闭环。
