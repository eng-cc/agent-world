- 日期：2026-02-13
- 完成内容：完成 BMS-59（规则域按 `module_id` 独立工件装载切换）。
  - `crates/agent_world/src/runtime/m1_builtin_wasm_artifact.rs`：
    - 新增 `m1_builtin_wasm_module_artifact_bytes(module_id)`，按模块 ID 返回独立工件字节。
    - 新增 `register_m1_builtin_wasm_module_artifact(world, module_id)`，按模块 ID 注册工件并返回哈希。
  - `crates/agent_world/src/runtime/mod.rs`：
    - 导出 `register_m1_builtin_wasm_module_artifact` 供测试安装链路复用。
  - `crates/agent_world/src/runtime/tests/rules.rs`：
    - `install_m1_move_rule` / `install_m1_visibility_rule` / `install_m1_transfer_rule` 改为按对应 `module_id` 注册独立工件，不再复用单聚合工件注册入口。
  - 验证通过：
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::rules::`
- 遗留事项：执行 BMS-60，完成阶段五首轮回归收口、文档与 devlog 闭环。

- 日期：2026-02-13
- 完成内容：完成 BMS-60（阶段五首轮回归收口、文档与 devlog 闭环）。
  - 回归与校验通过：
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::rules::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::body::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::power_bootstrap::`
    - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime runtime::tests::agent_default_modules::`
  - 文档更新：
    - `doc/world-runtime/builtin-wasm-crate-split.md`：补充 BMS-60 回归结果与阶段五首轮完成状态。
    - `doc/world-runtime/builtin-wasm-crate-split.project.md`：勾选 BMS-60，更新阶段状态/最近更新/下一步。
    - `doc/world-runtime.project.md`：勾选 BMS-60，并同步总项目状态。
- 遗留事项：阶段五下一批（`body/sensor/mobility` 按 `module_id` 独立工件装载）尚未拆解 BMS-61+ 任务。
