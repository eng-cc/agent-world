# 2026-03-24 Devlog

## 14:05:00 CST / producer_system_designer
- 完成内容:
  - 新建 `doc/p2p/blockchain/p2p-mainstream-public-chain-testing-benchmark-2026-03-24.{prd,design,project}.md`，把“主流公链测试体系对标”正式收成六层 benchmark，并映射 oasis7 当前 `S0/S1/S2/S4/S6/S9/S10 + governance audit/runbook` 的覆盖现状。
  - 回写 `doc/p2p/prd.md`、`doc/p2p/project.md`、`doc/p2p/prd.index.md`、`doc/p2p/README.md` 与 `testing-manual.md`，新增 `PRD-P2P-021 / TASK-P2P-039`，冻结当前高优先级缺口为：真实 governance drill 证据、`fuzz/property-based gate`、`shared network / release train`。
  - 当前 producer 结论保持不变：项目仍是 `limited playable technical preview` + `crypto-hardened preview`，不得对外宣称“主流公链级测试成熟度”。
- 遗留事项:
  - 先按 benchmark 的 `BENCH-G1` 补首轮真实 governance drill / negative drill / QA evidence。
  - 后续再定义 `shared network / release train` 的最小执行形态。

## 14:18:00 CST / runtime_engineer
- 完成内容:
  - 新增 `scripts/governance-registry-drill.sh`，把 clone-world governance drill 固化为一条可复用命令链：自动生成 pass/block manifests、复制 execution world、执行 import/audit，并沉淀 `run_config.json`、`summary.json`、`summary.md` 与完整日志。
  - 回写 `testing-manual.md`，新增 `S4B Governance Registry Drill` 入口，明确 baseline/pass/block 的判定口径与 clone-world 限制。
- 遗留事项:
  - 同一脚本流程仍需在 default/live execution world 上留正式证据。

## 14:24:00 CST / qa_engineer
- 完成内容:
  - 基于 `output/chain-runtime/viewer-live-node/reward-runtime-execution-world`，对 `msig.foundation_ops.v1` 完成首轮 clone-world `pass + block` drill，证据落在 `output/governance-drills/20260324-foundation-ops-clone-world/`。
  - 回写 `doc/testing/evidence/governance-registry-clone-world-drill-foundation-ops-2026-03-24.md` 与 `doc/p2p/blockchain/p2p-governance-signer-externalization-2026-03-23.project.md`，固定本轮 QA 结论：
    - baseline `ready_for_ops_drill`
    - pass case `ready_for_ops_drill`
    - negative block case `failover_blocked`
- 遗留事项:
  - 当前只可记为 `pass_for_clone_world`，不能替代 default/live execution world 的最终治理 drill `pass/block`。

## 14:36:00 CST / runtime_engineer
- 完成内容:
  - 在默认 execution world `output/chain-runtime/viewer-live-node/reward-runtime-execution-world` 上完成首轮真实 governance drill：依次执行 baseline audit、pass import/audit、negative block import/audit、restore import/audit。
  - 在 `output/governance-drills/20260324-foundation-ops-live-world/` 落下 backup、manifests、logs、`summary.json` 与 `summary.md`；最终 restore audit 返回 `ready_for_ops_drill`，确认默认 world 已恢复回 baseline truth。
- 遗留事项:
  - 当前仍只覆盖 `msig.foundation_ops.v1` 单槽位，后续要扩到更多 controller slot / finality slot。

## 14:42:00 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/governance-registry-live-world-drill-foundation-ops-2026-03-24.md`，把默认 execution world 首轮真实 drill 固化为正式 QA 证据。
  - 回写 `doc/p2p/blockchain/p2p-governance-signer-externalization-2026-03-23.project.md` 与 `doc/p2p/blockchain/p2p-mainstream-public-chain-testing-benchmark-2026-03-24.project.md`，将状态更新为：
    - default/live execution world 首轮 low-risk governance drill 已完成
    - 但 coverage 仍有限，`shared network / release train` 仍缺
- 遗留事项:
  - 继续扩更多 slot 覆盖，再决定是否把 `BENCH-G1` 视为足够收口。

## 14:56:00 CST / runtime_engineer
- 完成内容:
  - 核对 `output/governance-drills/20260324-finality-live-world-signer04/summary.json` 与默认 world restore 审计，确认 `governance.finality.v1` 的真实 live drill 已完成 `pass + block + restore`，默认 world 当前重新回到 `ready_for_ops_drill`。
  - 更新 `scripts/governance-registry-drill.sh`，新增 finality rotation 误用防护：当 target slot 为 `governance.finality.v1` 且仍复用原 `signer_id` 时直接失败，要求显式传入新的 `--replacement-signer-id`。
  - 回写 `testing-manual.md` 与 governance project runbook，冻结当前已验证的 finality 运行规则：controller slot 可同 `signer_id` 换公钥，finality slot 必须替换 signer node identity。
- 遗留事项:
  - 继续把这套 runbook 扩到更多 controller slot / additional finality rotation-revocation 样本。

## 15:02:00 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/governance-registry-live-world-drill-finality-2026-03-24.md`，把默认 execution world 上的首轮真实 finality drill 固化为正式证据。
  - 在证据中补入失败语义对照：先前“同 `signer03` 换公钥”的 live import 会命中 `GovernancePolicyInvalid`，因此 finality rotation 必须是 `signer03 -> signer04` 这类新 signer node id 替换。
  - 回写 benchmark / governance project 文档，更新 `L4` 覆盖现状为“controller slot + finality slot 首轮真实 drill 已完成，但 coverage 仍有限”。
- 遗留事项:
  - `BENCH-G1` 仍未因本轮 finality 证据而完全关闭；shared network / release train 仍缺，更多治理槽位覆盖仍待继续。

## 15:30:07 CST / runtime_engineer
- 完成内容:
  - 新增 `scripts/governance-registry-live-drill.sh`，把 default/live execution world 的 governance drill 固化成可复用脚本，统一执行 `baseline -> pass -> block -> restore` 并自动沉淀 `world-backup-pre-drill`、`summary.json/md`、`manifests/*` 与 `logs/*`。
  - 在 operator-local 目录新增一把 additional drill key，并以 `signer02 -> signer05` 的语义运行 finality revocation recovery drill：
    - clone-world: `output/governance-drills/20260324-finality-revocation-clone-world-signer05/`
    - live-world: `output/governance-drills/20260324-finality-revocation-live-world-signer05/`
  - 复核默认 world restore 后再次审计，确认当前 world 已回到 baseline `ready_for_ops_drill`。
- 遗留事项:
  - 继续扩 finality 的 multi-signer loss / rejoin 覆盖，避免 current evidence 只停留在 single-signer recovery。

## 15:36:00 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/governance-registry-live-world-drill-finality-revocation-signer02-2026-03-24.md`，把第二条独立 finality revocation recovery 样本固化为正式 QA 证据。
  - 回写 `testing-manual.md` 与 governance / benchmark project 文档，明确 live-world 现在有正式自动化脚本入口，并把 L4 覆盖现状更新为：finality 已有两条独立 single-signer recovery 样本。
  - 本轮 additional coverage 结论：
    - `signer02 -> signer05` pass case `ready_for_ops_drill`
    - 移除 `signer02` 但不补位的 negative case `failover_blocked`
    - restore 后默认 world 回到 baseline
- 遗留事项:
  - `BENCH-G1` 仍未关闭；shared network / release train 缺口不变，finality 还缺 multi-signer failover / rejoin 证据。

## 15:48:00 CST / runtime_engineer
- 完成内容:
  - 扩展 `scripts/governance-registry-drill.sh` 与 `scripts/governance-registry-live-drill.sh`，新增 `--block-remove-signer-id`，允许 block case 显式移除多把 signer，并在 summary 中区分 `audit_failover_gate` 与 `import_policy_reject` 两种阻断阶段。
  - 基于 `signer02 -> signer05` 的 pass case，补跑 finality multi-signer loss 样本：
    - clone-world: `output/governance-drills/20260324-finality-multi-loss-clone-world-signer05/`
    - live-world: `output/governance-drills/20260324-finality-multi-loss-live-world-signer05/`
  - 本轮发现并固定了更强门禁语义：当 finality block manifest 让 `signer_count < threshold` 时，`oasis7_governance_registry_import` 会直接拒绝写入，而不是留到 audit 再判定。
- 遗留事项:
  - 继续补 finality 的 non-baseline rejoin / partial degraded-but-importable failover 覆盖。

## 15:54:00 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/governance-registry-live-world-drill-finality-multi-loss-rejoin-2026-03-24.md`，把 finality dual-loss (`signer01 + signer02`) 的 clone/live 证据固化为正式 QA 文档。
  - 回写 `testing-manual.md` 与 governance / benchmark project 文档，把 negative block 的合法结果更新为两类：
    - `audit_failover_gate`
    - `import_policy_reject`
  - 确认 restore 后默认 world 重新审计为 baseline `ready_for_ops_drill`。
- 遗留事项:
  - `BENCH-G1` 仍未关闭；当前更多是把 finality negative coverage 补深，shared network / release train 仍是大缺口。

## 16:06:00 CST / runtime_engineer
- 完成内容:
  - 继续扩展 `scripts/governance-registry-drill.sh` 与 `scripts/governance-registry-live-drill.sh`，为 `audit_failover_gate` 场景新增 `rejoin_case`，使脚本可自动执行 `block -> rejoin -> restore`。
  - 以 `signer02 -> signer05` 为样本补跑 finality non-baseline rejoin：
    - clone-world: `output/governance-drills/20260324-finality-rejoin-clone-world-signer05/`
    - live-world: `output/governance-drills/20260324-finality-rejoin-live-world-signer05/`
  - 复核结果确认：
    - `block_case=failover_blocked`
    - `rejoin_case=ready_for_ops_drill`
    - `restore=ready_for_ops_drill`
- 遗留事项:
  - 继续补 finality 的更多 rejoin 变体，而不只是一条 `2-of-2 -> 2-of-3` 路径。

## 16:12:00 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/governance-registry-live-world-drill-finality-rejoin-signer02-2026-03-24.md`，把第一条正式 non-baseline rejoin 证据固化为 QA 文档。
  - 回写 `testing-manual.md` 与 governance / benchmark project 文档，把 `rejoin_case` 纳入 S4B 正式口径。
  - 确认默认 world 在 rejoin 样本结束后重新审计为 baseline `ready_for_ops_drill`。
- 遗留事项:
  - `BENCH-G1` 仍未关闭；shared network / release train 还是主缺口，finality 这边接下来主要是补 rejoin 变体和更多 slot 覆盖。

## 16:24:00 CST / runtime_engineer
- 完成内容:
  - 扩展 `scripts/governance-registry-drill.sh` 与 `scripts/governance-registry-live-drill.sh`，新增 `--pass-manifest-mode baseline|rotate`，使脚本可以区分：
    - `baseline`: temporary offline / same-signer rejoin
    - `rotate`: replacement / revocation recovery
  - 基于 `pass_manifest_mode=baseline` 补跑 finality baseline rejoin：
    - clone-world: `output/governance-drills/20260324-finality-baseline-rejoin-clone-world-signer02/`
    - live-world: `output/governance-drills/20260324-finality-baseline-rejoin-live-world-signer02/`
  - 本轮确认 `remove signer02 -> failover_blocked -> baseline signer02 rejoin -> ready_for_ops_drill` 在 clone/live 两侧都可复现。
- 遗留事项:
  - finality 单槽位 drill 维度已比较饱和，后续更值得转去 shared network / release train 或更复杂网络抖动样本。

## 16:30:00 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/governance-registry-live-world-drill-finality-baseline-rejoin-signer02-2026-03-24.md`，把 baseline rejoin 变体固化为正式 QA 证据。
  - 回写 `testing-manual.md` 与 governance / benchmark project 文档，正式把 `pass_manifest_mode=baseline` 收进 S4B 口径。
  - 确认默认 world 在 baseline rejoin 样本结束后再次审计为 `ready_for_ops_drill`。
- 遗留事项:
  - `BENCH-G1` 仍未关闭；shared network / release train 依然是更高优先级缺口。

## 17:18:00 CST / producer_system_designer
- 完成内容:
  - 新增 `doc/p2p/blockchain/p2p-shared-network-release-train-minimum-2026-03-24.prd.md`、`.design.md`、`.project.md`，把 benchmark 中 `L5 shared network / release train` 从纯缺口描述升级成正式执行专题。
  - 在专题中冻结了三层最小轨道：
    - `shared_devnet`
    - `staging`
    - `canary`
  - 同步冻结 `release_candidate_bundle` 单一真值、promotion/freeze/rollback 规则，以及当前 shared-network 结论仍为 `specified_not_executed`、对外口径继续维持 preview。
  - 回写 `doc/p2p/prd.md`、`doc/p2p/project.md`、`doc/p2p/prd.index.md`、`doc/p2p/README.md` 与 `testing-manual.md`，正式建立 `PRD-P2P-022 / TASK-P2P-040` 追踪链。
- 遗留事项:
  - 下一步进入 `RTMIN-1~3`：补 candidate bundle schema、QA pass/partial/block 模板与 liveops release window / freeze / rollback runbook。

## 14:26:09 CST / runtime_engineer
- 完成内容:
  - 新增 `scripts/release-candidate-bundle.sh`，正式落地 `release_candidate_bundle` 的生成与校验入口，冻结以下 machine-readable 真值：
    - `candidate_id`
    - `git_commit`
    - `runtime_build`
    - `world_snapshot`
    - `governance_manifest`
    - `evidence_refs`
  - 脚本对 `runtime_build/world_snapshot/governance_manifest` 统一记录路径元数据与哈希，并在 `validate` 时做 drift blocker 校验；目录型 `world_snapshot` 使用递归文件树 hash。
  - 更新 `scripts/release-gate.sh`，新增 `--candidate-bundle <bundle.json>` 前置校验入口，使 shared-network 候选可以先经过 bundle pinning 再进入统一 gate。
  - 新增 `scripts/release-candidate-bundle-smoke.sh`，并扩展 `scripts/release-gate-smoke.sh`，确认：
    - bundle create/validate 正常
    - world snapshot 变更会触发 drift fail
    - release gate 摘要会带上 candidate bundle
  - 回写 shared-network 专题 design/project 与 `testing-manual.md`，把 `RTMIN-1` 状态改为完成并给出正式命令入口。
- 遗留事项:
  - 继续进入 `RTMIN-2` / `RTMIN-3`，把 shared track 的 QA gate 和 liveops runbook 固化下来；shared environment 本身仍未建立，整体 verdict 继续保持 `specified_not_executed`。

## 14:38:01 CST / qa_engineer
- 完成内容:
  - 新增 `scripts/shared-network-track-gate.sh`，为 `shared_devnet / staging / canary` 生成统一 QA gate `summary.json/md`，并固定 required lane 检查与 `pass / partial / block` 聚合规则。
  - 新增 `scripts/shared-network-track-gate-smoke.sh`，验证以下三种结论都可稳定产出：
    - `pass`
    - `partial`
    - `block`（缺 required lane）
  - 新增 QA 模板：
    - `doc/testing/templates/shared-network-track-gate-template.md`
    - `doc/testing/templates/shared-network-track-gate-lanes.shared_devnet.template.tsv`
    - `doc/testing/templates/shared-network-track-gate-lanes.staging.template.tsv`
    - `doc/testing/templates/shared-network-track-gate-lanes.canary.template.tsv`
  - 回写 shared-network 专题 design/project 与 `testing-manual.md`，把 `RTMIN-2` 状态更新为完成，并明确当前 shared-network 已具备 candidate bundle + QA gate scaffold，但 shared environment 仍未建立。
- 遗留事项:
  - 下一步进入 `RTMIN-3`，补 liveops 的 promotion/freeze/rollback/run window runbook；在真实 `shared_devnet` 演练前，整体 verdict 继续保持 `specified_not_executed`。

## 14:42:57 CST / liveops_community
- 完成内容:
  - 新增 `doc/p2p/blockchain/p2p-shared-network-release-train-minimum-2026-03-24.runbook.md`，正式把 `shared_devnet -> staging -> canary` 的 promotion、freeze、rollback、值班窗口与对外 preview 口径冻结为可执行 SOP。
  - 新增以下最小模板，供后续 `RTMIN-4/5` rehearsal 直接填充：
    - `doc/testing/templates/shared-network-promotion-record-template.md`
    - `doc/testing/templates/shared-network-incident-template.md`
    - `doc/testing/templates/shared-network-incident-review-template.md`
    - `doc/testing/templates/shared-network-exit-decision-template.md`
  - 回写 shared-network 专题 design/project、`testing-manual.md`、`doc/p2p/prd.md`、`doc/p2p/project.md`、`doc/p2p/prd.index.md` 与 `doc/p2p/README.md`，把 `RTMIN-3` 更新为完成，并将下一步固定为 first `shared_devnet` dry run。
- 遗留事项:
  - shared-network 总 verdict 仍是 `specified_not_executed`；接下来必须执行 `RTMIN-4`，用同一 `candidate_id` 留下 first `shared_devnet` 正式 evidence，不能把 runbook 建好误记成环境已验证。

## 15:00:47 CST / runtime_engineer
- 完成内容:
  - 生成 first `shared_devnet` dry run 的候选工件 `output/release-candidates/shared-devnet-dry-run-20260324-01.json`，把以下真值固定到同一 `candidate_id`：
    - `output/release/game-launcher-local/bin/oasis7_chain_runtime`
    - `output/chain-runtime/viewer-live-node/reward-runtime-execution-world`
    - `output/governance-drills/20260324-finality-live-world-signer04/manifests/rotated_pass_manifest.json`
  - 对同一 candidate 执行 `./scripts/release-gate.sh --dry-run --candidate-bundle ...`，在 `output/shared-network/shared-devnet-dry-run-20260324-01/release-gate/20260324-150030/` 留下 release-gate rehearsal summary。
  - 补齐 local-only dry run 的 shared-access / multi-entry / short-window / rollback-target lane 证据底稿，供后续 shared-network gate 聚合。
- 遗留事项:
  - 当前还没有真实 shared operator access，也没有为该 `candidate_id` 重跑 shared-devnet longrun 与 governance window，因此这轮只可能先到 `partial`。

## 15:02:31 CST / qa_engineer
- 完成内容:
  - 对 `output/shared-network/shared-devnet-dry-run-20260324-01/lanes.shared_devnet.tsv` 执行正式 `shared-network-track-gate`，输出：
    - `output/shared-network/shared-devnet-dry-run-20260324-01/gate/shared_devnet-20260324-150230/summary.md`
    - `output/shared-network/shared-devnet-dry-run-20260324-01/gate/shared_devnet-20260324-150230/summary.json`
  - 固化首轮 `shared_devnet` dry run QA 结论：
    - `gate_result=partial`
    - `promotion_recommendation=hold_promotion`
    - `candidate_bundle_integrity=pass`
    - 其余 required lanes 目前均为 `partial`
  - 新增 `doc/testing/evidence/shared-network-shared-devnet-dry-run-2026-03-24.md`，并同步更新 shared-network / benchmark / testing-manual 主追踪，使 shared-network 当前总 verdict 从 `specified_not_executed` 更新为 `partial`。
- 遗留事项:
  - `RTMIN-4` 已完成 first dry run 产物留档，但 `shared_devnet` 仍未到 `pass`；在补齐 shared access、multi-entry closure、shared-devnet longrun 与 rollback target 前，不得进入 `staging`。

## 15:02:31 CST / liveops_community
- 完成内容:
  - 新增 `doc/testing/evidence/shared-network-shared-devnet-promotion-record-2026-03-24.md` 与 `doc/testing/evidence/shared-network-shared-devnet-incident-2026-03-24.md`，把本轮窗口的 `hold` 结论和“无 freeze / 无 rollback”的边界正式留档。
  - 回写 shared-network runbook / project 与 `testing-manual.md`，明确当前允许的安全口径已从“只建档未执行”更新为“已有 first `shared_devnet` dry run，但 shared execution 仍是 `partial`”。
- 遗留事项:
  - 在 `shared_devnet` 真正 `pass` 前，继续保持 `limited playable technical preview` + `crypto-hardened preview`，不得把本轮 dry run 说成 shared network validated。

## 17:42:00 CST / runtime_engineer
- 完成内容:
  - 新增 `scripts/shared-devnet-rehearsal.sh`，把 shared-devnet 的 same-candidate 编排收成单命令：先创建/校验 `release_candidate_bundle`，再按 `execute/evidence/skip` 模式可选执行 `release-gate --dry-run`、`headed Web + no-ui + pure_api` 复跑、治理 drill、short-window longrun，并统一生成 `multi-entry-summary`、`access-check`、`governance-summary`、`longrun-summary`、`rollback-target`、`lanes.shared_devnet.tsv` 和 `shared-network-track-gate` 输出。
  - 新增 `scripts/shared-devnet-rehearsal-smoke.sh`，用纯本地假证据覆盖 `pass / partial` 两条最小编排路径，确保 lane 汇总与 gate 聚合稳定。
  - 回写 shared-network design/project、模块 `doc/p2p/project.md` 与 `testing-manual.md`，把 `shared-devnet-rehearsal.sh` 冻结为 `shared_devnet partial -> pass` 的正式执行入口。
- 遗留事项:
  - 当前脚本只是把 same-candidate 演练和 lane 汇总自动化；`shared_access`、`governance_live_drill`、`short_window_longrun`、`rollback_target_ready` 是否能升到 `pass` 仍取决于真实新证据，不能靠脚本本身升级 claims。
