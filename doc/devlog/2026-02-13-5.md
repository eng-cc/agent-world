- 日期：2026-02-13
- 完成内容：完成 WRS-2 三 crate 拆分并接入：新增 `crates/agent_world_wasm_abi`（`ModuleKind/ModuleLimits/ModuleCall*` 等 ABI 协议类型）、`crates/agent_world_wasm_executor`（`FixedSandbox/WasmExecutor`）、`crates/agent_world_wasm_router`（订阅匹配与过滤器校验）。
- 完成内容：`agent_world` 接入新 crate 并保持导出兼容：`runtime/sandbox.rs` 改为 re-export，`runtime/modules.rs` 改为复用 ABI 类型，`runtime/world/module_runtime.rs` 将订阅匹配/过滤器逻辑迁移至 router crate；`module_runtime.rs` 行数降至 900。
- 完成内容：更新 workspace 与依赖（根 `Cargo.toml`、`crates/agent_world/Cargo.toml`），保持 `wasmtime` feature 路径兼容。
- 完成内容：更新项目文档状态：`doc/world-runtime/wasm-runtime-crate-split.project.md`、`doc/world-runtime.project.md`。
- 完成内容：运行并通过回归：
  - `env -u RUSTC_WRAPPER cargo check -p agent_world --all-targets`
  - `env -u RUSTC_WRAPPER cargo check -p agent_world --all-targets --features wasmtime`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world runtime::tests::modules -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world runtime::tests::rules -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world kernel_wasm_sandbox_bridge -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world --test wasm_executor --features wasmtime -- --nocapture`
- 遗留事项：继续推进 WRS-3，拆分 `agent_world_builtin_wasm/src/lib.rs` 为目录化模块并保持闭环测试通过。
