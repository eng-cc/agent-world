# 2026-03-17

## 00:21:47 CST / agent_engineer
- 完成内容：为 OpenClaw 双轨模式落地真实 `player_parity` 执行 lane，新增 `ProviderExecutionMode::parse()`，把 execution mode 接到 runtime live、`world_game_launcher`、`world_openclaw_parity_bench`、`scripts/openclaw-parity-p0.sh` 与 `.agents/skills/oasis7/scripts/oasis7-run.sh`。
- 验证方式：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_game_launcher -- world_game_launcher_tests`、`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_openclaw_parity_bench -- --nocapture`、`env -u RUSTC_WRAPPER cargo test -p agent_world runtime_openclaw -- --nocapture`、`env -u RUSTC_WRAPPER cargo check -p agent_world`。
- 遗留事项：`qa_engineer` / `producer_system_designer` 仍需基于真实双模式样本完成 `PRD-WORLD_SIMULATOR-040` T4 正式对照结论。

## 00:24:31 CST / agent_engineer
- 完成内容：用 `.agents/skills/oasis7/scripts/oasis7-run.sh smoke --execution-mode <mode>` 分别完成 `headless_agent` 与 `player_parity` 的真实 OpenClaw smoke，确认两条链路都能在 `P0-001` / `4 ticks` 下通过。
- 关键产物：`output/openclaw_parity/openclaw_parity_20260317_002147/summary/P0-001.openclaw_local_http.json`、`output/openclaw_parity/openclaw_parity_20260317_002217/summary/P0-001.openclaw_local_http.json`。
- 遗留事项：需要由 `qa_engineer` / `producer_system_designer` 基于上述产物回写 T4 默认模式与偏差结论。

## 01:12:11 CST / qa_engineer
- 完成内容：基于 `output/openclaw_parity/openclaw_parity_20260317_002147` 与 `output/openclaw_parity/openclaw_parity_20260317_002217` 的真实双样本，完成 `PRD-WORLD_SIMULATOR-040` T4 的 `player_parity` vs `headless_agent` 对照采证，确认两条模式在 `P0-001` 单样本上无阻断级偏差。
- 结论：T4 `pass`；默认回归模式维持 `headless_agent`，`player_parity` 保留为体验对照 lane；本轮不替代 `PRD-WORLD_SIMULATOR-038` 的 builtin/OpenClaw parity 扩面统计。
- 遗留事项：继续推进 builtin/OpenClaw 真实 parity 双边样本与评分卡双签。

## 01:12:11 CST / producer_system_designer
- 完成内容：审阅 QA 对照结果并冻结双轨模式默认策略：`headless_agent` 作为 CI / server / 自动回归默认模式，`player_parity` 作为制作人 / QA 体验对照与准入门禁，`debug_viewer` 保持 observer-only。
- 结论：`PRD-WORLD_SIMULATOR-040` T4 收口；后续若要宣称“玩家体验等价”，仍需以 `PRD-WORLD_SIMULATOR-038` 的 builtin/OpenClaw parity 结果为准。
- 遗留事项：安排下一轮 `PRD-WORLD_SIMULATOR-038` 扩面采证与评分卡汇总。

## 01:40:00 CST / qa_engineer
- 完成内容：执行真实 builtin/OpenClaw `P0-001` parity 批次 `openclaw_builtin_parity_20260317_t4d`，输出聚合对比、失败签名与 QA 结论；确认当前 parity 不成立。
- 结论：builtin `completion_rate=0%`、OpenClaw `completion_rate=100%`，当前 `PRD-WORLD_SIMULATOR-038` T4 结论为 `failed`，必须保持 `experimental`；另记录 `scripts/openclaw-parity-p0.sh` 默认 `--openclaw-connect-timeout-ms=3000` 会制造假性 `provider_unreachable`。
- 遗留事项：修复 builtin 基线退化与 OpenClaw parity 默认 timeout 风险后，重跑 `P0` parity。

## 01:40:00 CST / producer_system_designer
- 完成内容：审阅 `PRD-WORLD_SIMULATOR-038` T4 parity 证据并冻结当前放行口径：OpenClaw 保持 `experimental`，不得默认启用。
- 结论：`PRD-WORLD_SIMULATOR-040` 的双轨默认策略不变，但 builtin/OpenClaw 体验等价门禁尚未通过，后续必须先修复基线与 timeout 风险。
- 遗留事项：等待工程侧修复后重跑 `P0`，再决定是否进入下一层 parity。


## 10:59:16 CST / agent_engineer
- 完成内容：在 `crates/agent_world/src/bin/world_openclaw_parity_bench.rs` 为 builtin parity lane 增加 `P0-001` patrol guardrail，确保 builtin 在 parity 场景下不会执行偏离 phase-1 巡游目标的动作；同时将 bench / `scripts/openclaw-parity-p0.sh` 默认 connect-timeout 统一提升到 `15000ms`。
- 验证：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_openclaw_parity_bench -- --nocapture` 通过；真实批次 `openclaw_builtin_parity_20260317_fix2` 中 builtin 样本恢复为 `status=passed`、`move_agent=4`。
- 遗留事项：真实 OpenClaw 样本在 `fix2` 中仍出现 `timeout=4`，需继续排查 local_http / runtime agent 请求链路。

## 10:59:16 CST / qa_engineer
- 完成内容：复验 `openclaw_builtin_parity_20260317_fix2`，确认 builtin 基线退化已收口，但 OpenClaw lane 仍为 `wait=4` / `timeout=4`。
- 结论：`PRD-WORLD_SIMULATOR-038` 的正式 T4 结论暂不改写，当前阻断从“builtin 基线 + timeout 风险”收敛为“真实 OpenClaw 连续 timeout”；继续保持 `experimental`。
- 遗留事项：在 OpenClaw timeout 根因修复后，发起新的 builtin/OpenClaw 同批次 parity 结论。

## 10:59:16 CST / producer_system_designer
- 完成内容：确认 `fix2` 后产品口径调整为“builtin parity 基线已恢复，但 OpenClaw 仍未达到可宣称等价的稳定性”，并回写专题状态。
- 结论：默认/放行策略不变；在真实 OpenClaw lane 不再连续 timeout 前，不进入 `P1/P2` 扩面，也不允许对外宣称 parity 达标。
- 遗留事项：等待工程侧给出 OpenClaw timeout 收敛证据，再决定是否发起下一轮 QA/producer 双签。

## 11:06:54 CST / producer_system_designer
- 完成内容：
  - 基于仓库现状完成 WASM 构建链路盘点，确认 Agent World 已具备 deterministic guard、canonical packaging、keyed hash manifest、identity manifest、DistFS hydrate 与 multi-runner 对账等基础设施。
  - 新增 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.prd.md`，明确 Agent World 的 WASM 信任模型采用 binary-first：共识层与 runtime 只认 canonical wasm binary 的 hash 与 identity，源码验证属于社会层。
  - 回写 `doc/world-runtime/prd.md`，新增 `PRD-WORLD_RUNTIME-020/021/022` 与根级验收口径、验证映射。
- 遗留事项：
  - 需要 `wasm_platform_engineer` 继续执行 `WDBP-1 ~ WDBP-4`，把 build recipe / metadata / release evidence / source compile 审计字段进一步统一到代码与门禁。
  - 需要 `qa_engineer` 后续补 deterministic pipeline 专题回归矩阵与失败签名固化。

## 11:08:09 CST / wasm_platform_engineer
- 完成内容：
  - 新增 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.design.md` 与 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md`，把构建入口、canonicalizer、manifest/identity、DistFS、release manifest 与 runtime binary-only 消费串成统一详细设计。
  - 回写 `doc/world-runtime/project.md`、`doc/world-runtime/prd.index.md`、`doc/world-runtime/README.md`，登记 `TASK-WORLD_RUNTIME-041` 及后续 `TASK-WORLD_RUNTIME-042/043/044`。
  - 执行 `./scripts/doc-governance-check.sh`，结果通过：`doc-governance-check: OK`。
- 遗留事项：
  - 当前 canonicalizer 仍是 `strip-custom-sections-v1`，尚不是 semantic-equivalence hash；后续若扩展 canonicalizer，必须显式版本化迁移。
  - `compile_module_artifact_from_source` 虽已复用 deterministic builder，但 metadata/identity 字段仍未与 builtin 发布链路完全对齐，这是后续实现优先级最高的缺口之一。

## 11:25:51 CST / producer_system_designer
- 完成内容：
  - 根据新澄清的目标回收前一版口径：本专题真正要解决的是“不同宿主平台 hash 漂移”，因此不能继续把多平台 keyed 发布 hash 当作长期方案。
  - 将 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.prd.md` 改写为 Docker-first：明确 pinned builder image、single canonical publish hash、CI 只比较容器输出、runtime 只认容器产物。
  - 同步回写 `doc/world-runtime/prd.md` 与 `doc/world-runtime/project.md`，把 `TASK-WORLD_RUNTIME-042/043/044` 调整为 builder image、Docker canonical hash 与 source compile 外移路线。
- 遗留事项：
  - 需要 `wasm_platform_engineer` 下一步定义 builder image 路径、镜像 digest 管理方式与 wrapper script contract。
  - 需要 `runtime_engineer` 评估 `compile_module_artifact_from_source` 外移到 external builder 后的动作协议与治理边界。

## 11:25:51 CST / wasm_platform_engineer
- 完成内容：
  - 将 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.design.md` 修正为 Docker canonical builder 详细设计，明确当前仓库尚无 `Dockerfile`、manifest 迁移目标为单 canonical token `linux-x86_64=<sha256>`、production source compile 不应继续在 runtime 进程内 host 直编。
  - 更新 `doc/world-runtime/README.md`，把专题维护口径改为 `WASM Docker 确定性构建`。
- 遗留事项：
  - 现有 CI 与 manifest 代码仍是 keyed 平台模型，后续实现需要兼容读路径并切换写路径。
  - 当前仓库还没有 builder image 与 wrapper，实现阶段前需要先确定 `docker/wasm-builder/Dockerfile` 与 receipt schema。

## 11:08:00 CST / agent_engineer
- 完成内容：将 `world_openclaw_parity_bench` 与 `scripts/openclaw-parity-p0.sh` 的默认产物根目录从 `artifacts/` 收口到已忽略的 `output/openclaw_parity/`，并把 `artifacts/` 与 `tools/openclaw/agent_world_runtime_workspace/.openclaw/` 加入 `.gitignore`，避免本地运行产物持续污染 `git status`。
- 验证：检查 `.gitignore` 与脚本默认值已生效；后续 parity 批次默认落到 `output/openclaw_parity/<run_id>`。
- 遗留事项：历史已生成的 `artifacts/*` 证据路径暂不迁移，避免影响既有文档中的本地证据引用。


## 11:14:00 CST / agent_engineer
- 完成内容：将当前本地 OpenClaw parity 产物目录从仓库根下的 `artifacts/` 迁移到 `output/openclaw_parity/`，并同步回写近期 OpenClaw 相关项目/测试/devlog 文档中的本地证据路径。
- 验证：当前本地批次已位于 `output/openclaw_parity/openclaw_builtin_parity_20260317_fix2` 等目录；仓库内不再残留旧的 OpenClaw parity 本地证据路径引用。
- 遗留事项：更早或未在本机生成的历史证据仍按文档路径约定查找，但后续新批次统一使用 `output/openclaw_parity/`。


## 11:22:00 CST / agent_engineer
- 完成内容：移除 `.gitignore` 中仓库根 `artifacts/` 的兜底忽略，仅保留 `output/` 与 OpenClaw 本地工作目录忽略；当前 parity 默认输出已统一到 `output/openclaw_parity/`。
- 验证：仓库默认脚本/bench 仍写入 `output/openclaw_parity/`，`tools/openclaw/agent_world_runtime_workspace/.openclaw/` 继续保持忽略。
- 遗留事项：若后续仍有旧入口把运行产物写回仓库根 `artifacts/`，需要逐个迁移入口，而不是再扩大忽略范围。


## 11:49:12 CST / agent_engineer
- 完成内容：定位到 `OpenClaw(Local HTTP)` timeout 根因不在上游模型，而在 `openclaw gateway call agent` 链路；已为 `world_openclaw_local_bridge` 增加 gateway timeout → `openclaw agent --local` fallback，并用 `sha256(session_key)` 生成稳定 `session_id` 保持会话隔离。
- 验证：`env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_openclaw_local_bridge -- --nocapture` 通过；真实批次 `output/openclaw_parity/openclaw_timeout_probe_20260317_fixlocal4` 与 `output/openclaw_parity/openclaw_builtin_parity_20260317_fix3` 已确认 OpenClaw `timeout_count=0`、`move_agent=4`。
- 遗留事项：虽然 timeout 已修复，但 `fix3` 中 builtin/OpenClaw 的 `median_extra_wait_ms` 仍分别约 `9900/13957`，明显高于 `P0` 通过线，下一步需要继续压缩绝对等待时延。

## 11:49:12 CST / qa_engineer
- 完成内容：复验 `openclaw_builtin_parity_20260317_fix3`，确认 builtin/OpenClaw 在 `P0-001` 上都已恢复 `completion_rate=100%`、`timeout_rate=0%`。
- 结论：timeout 与 completion gap 阻断已收口，但 `median/p95 extra wait` 仍显著超线，因此 `PRD-WORLD_SIMULATOR-038` 继续保持 `failed / experimental`，暂不推进 `P1/P2`。
- 遗留事项：等待工程侧继续优化 prompt / 调用链延迟后，再发起新的 T4/T5 结论批次。

## 11:49:12 CST / producer_system_designer
- 完成内容：确认产品口径更新为“OpenClaw 已恢复可行动作与 completion parity，但绝对等待时延仍不达 P0 门槛”。
- 结论：依旧不允许默认启用，也不进入下一层 parity；后续优化目标从“修 timeout”转为“把等待体感拉回 P0 通过线”。
- 遗留事项：等待 latency 收敛证据后，再决定是否重签 `PRD-WORLD_SIMULATOR-038` T4/T5 结论。

## 12:03:18 CST / wasm_platform_engineer
- 完成内容：
  - 新增 `docker/wasm-builder/Dockerfile` 与 `docker/wasm-builder/README.md`，固化 canonical builder image：固定 nightly toolchain、`rust-src`、`wasm32-unknown-unknown` 与内置 `wasm_build_suite`，并约定 canonical container platform 为 `linux-x86_64`（Docker `linux/amd64`）。
  - 重写 `scripts/build-wasm-module.sh`：宿主入口改为 Docker-only wrapper，自动构建默认 builder image、把源码 workspace 挂载到容器内 `/workspace`，并在容器内执行同一条 deterministic build path；不再保留 host-native fallback。
  - 回写 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.prd.md`、`design.md`、`project.md` 与 `doc/world-runtime/project.md`，将 `WDBP-1` / `TASK-WORLD_RUNTIME-042` 标记为完成，并把下一步收口切换到 build receipt、canonical token 和 CI/release evidence 迁移。
- 遗留事项：
  - 当前 builder 仍未输出 `build receipt`；`builder_image_digest/source_hash/build_manifest_hash` 绑定要在下一阶段补进 identity / release evidence。
  - 现有 keyed manifest、CI summary 和 runtime source compile 仍未迁移，后续需要分别由 `wasm_platform_engineer` / `runtime_engineer` 完成 `WDBP-2~4`。

## 13:24:01 CST / producer_system_designer
- 完成内容：回写 `PRD-WORLD_SIMULATOR-038` 的分层 latency gate 文档，包括 parity PRD、场景矩阵、评分卡、聚合模板、专题 project、模块 project 与 T4 结论文档；正式把真实在线 LLM provider 的等待口径拆为“行为等价硬门禁 + rollout latency_class”。
- 结论：`openclaw_builtin_parity_20260317_fix3` 在新口径下记为 `behavior_parity_pass / latency_class B / keep experimental`；允许受限试点，但不得默认启用。
- 遗留事项：继续由 `agent_engineer` / `viewer_engineer` 收敛 OpenClaw absolute wait latency，达到 `latency_class A` 后再重签默认启用与扩面结论。

## 14:16:01 CST / wasm_platform_engineer
- 完成内容：
  - 完成 `WDBP-2` 代码落地：`tools/wasm_build_suite` 现已在 Docker canonical path 下输出 `build receipt`，并把 `source_hash`、`build_manifest_hash`、`builder_image_ref/digest`、`container_platform` 写入 metadata/receipt。
  - 完成 builtin 工件治理迁移：`crates/agent_world_distfs/src/bin/sync_builtin_wasm_identity.rs` 改为 receipt 驱动 identity 生成；`scripts/sync-m1-builtin-wasm-artifacts.sh` 写路径收敛为单 canonical token `linux-x86_64=<sha256>`，同时保留 legacy multi-token 读兼容；`m1/m4/m5` manifest 与 identity 已全部重写。
  - 完成 CI 摘要口径切换的基础改造：`scripts/ci-m1-wasm-summary.sh` 现在显式区分 `host_platform` 与 `canonical_platform`，不同宿主只比较 Docker canonical 输出，不再把 host-native hash 当发布依据。
  - 回写 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md` 与 `doc/world-runtime/project.md`，将专题状态推进到 `WDBP-3 / TASK-WORLD_RUNTIME-043` 的 release evidence 收口阶段。
  - 执行验证：
    - `env -u RUSTC_WRAPPER cargo test --manifest-path tools/wasm_build_suite/Cargo.toml`
    - `env -u RUSTC_WRAPPER cargo check -p agent_world_distfs --bin sync_builtin_wasm_identity`
    - `./scripts/sync-m1-builtin-wasm-artifacts.sh --check`
    - `./scripts/sync-m4-builtin-wasm-artifacts.sh --check`
    - `./scripts/sync-m5-builtin-wasm-artifacts.sh --check`
    - `./scripts/doc-governance-check.sh`
    - `git diff --check`
- 遗留事项：
  - `TASK-WORLD_RUNTIME-043` 还没有把 release evidence / release gate 全量切到 receipt 驱动，当前只完成了 manifest / identity / CI summary 子切片。
  - `TASK-WORLD_RUNTIME-044` 尚未开始，`compile_module_artifact_from_source` 的生产路径仍需外移到 external Docker builder 或在 production 默认禁用。

## 14:35:02 CST / runtime_engineer
- 完成内容：修复 `crates/agent_world/src/bin/world_openclaw_local_bridge.rs` 与 `crates/agent_world/src/bin/world_openclaw_parity_bench.rs` 的 Rust 格式问题，执行 `env -u RUSTC_WRAPPER cargo fmt --all` 后确认 CI `fmt` diff 已清零。
- 验证方式：`env -u RUSTC_WRAPPER cargo fmt --all -- --check` 已通过；`CI_VERBOSE=1 ./scripts/ci-tests.sh required` 进入完整 required gate 验证。
- 遗留事项：required gate 未再卡在 `fmt`，但后续被现存 runtime 用例阻断：`runtime::tests::gameplay_protocol::step_with_modules_applies_gameplay_directive_emits_to_domain_events` 与 `runtime::tests::gameplay_protocol::step_with_modules_uses_gameplay_tick_modules_without_fallback` 因 `m5_builtin_modules.identity.json` 缺少 `m5.gameplay.crisis.cycle` 的 hash token 失败，本次未扩修该问题。

## 14:35:02 CST / qa_engineer
- 完成内容：复验本轮 CI 目标问题，确认 `scripts/ci-tests.sh required` 中的 `cargo fmt --all -- --check` 已通过，不再构成阻断项。
- 结论：当前 required gate 的剩余失败签名已收敛为 runtime 测试数据/清单问题，而非格式问题；失败位置为 `crates/agent_world/src/runtime/tests/gameplay_protocol_split_part2.rs:460` 与 `crates/agent_world/src/runtime/tests/gameplay_protocol_split_part2.rs:483`。
- 遗留事项：若要恢复 required gate 全绿，需要由对应 owner 继续处理 builtin identity manifest `m5_builtin_modules.identity.json` 缺 hash token 的问题并回归相关 gameplay protocol 用例。

## 14:38:22 CST / wasm_platform_engineer
- 完成内容：
  - 推进 `WDBP-3 / TASK-WORLD_RUNTIME-043` 的 release evidence / release gate 子切片：runtime `ModuleReleaseSubmitAttestation`、`DomainEvent::ModuleReleaseAttested` 与持久化 state 现已显式携带 `builder_image_digest`、`container_platform`、`canonicalizer_version`，发布 attestation 不再只绑定 `source_hash/build_manifest_hash/wasm_hash`。
  - 收紧 release gate：`ModuleReleaseApply` 现要求阈值内 attestation 的 receipt evidence 完全一致，并要求 attestation 的 `source_hash/build_manifest_hash/wasm_hash` 与 manifest `artifact_identity` 对齐；新增拒绝回归覆盖“builder digest 漂移导致 apply 被阻断”。
  - 扩展 CI 对账证据：`scripts/ci-m1-wasm-summary.sh` 现会导出 `receipt_evidence + identity_build_recipe`，`scripts/ci-verify-m1-wasm-summaries.py` 会跨 runner 校验 receipt/build recipe 一致，而不只比较 wasm hash / identity hash。
  - 回写 `doc/world-runtime/wasm/wasm-deterministic-build-pipeline.project.md` 与 `doc/world-runtime/project.md`，把 `TASK-WORLD_RUNTIME-043` 当前阶段明确为“receipt-aware release gate 已落地，剩余 full-tier 跨宿主证据收口”。
  - 执行验证：
    - `env -u RUSTC_WRAPPER cargo test -p agent_world module_release_ --features test_tier_required -- --nocapture`
    - `./scripts/ci-m1-wasm-summary.sh --module-set m1 --runner-label linux-x86_64 --out <tmp>`
    - `python3 ./scripts/ci-verify-m1-wasm-summaries.py --module-set m1 --summary-dir <tmp> --expected-runners linux-x86_64`
    - `bash -n scripts/ci-m1-wasm-summary.sh`
    - `python3 -m py_compile scripts/ci-verify-m1-wasm-summaries.py`
- 遗留事项：
  - 还没有补真实多宿主（至少 macOS + Linux）full-tier summary 证据，当前只在本机 runner 上验证了新的 receipt-aware summary/verify 路径。
  - `TASK-WORLD_RUNTIME-044` 仍未开始，runtime 内源码编译路径还没有外移或 production 默认禁用。

## 14:43:58 CST / qa_engineer
- 完成内容：复验 GitHub issue `#39` 当前是否仍存在。按原始 real-play 路径执行 `.agents/skills/oasis7/scripts/oasis7-run.sh play --no-open-browser` 后，用默认 headless `agent-browser` 打开 `http://127.0.0.1:4173/?ws=ws://127.0.0.1:5011`，再次命中 `SwiftShader` 与 `copy_deferred_lighting_id_pipeline` fatal，页面保持空白；随后检查发现源码已有 `software_safe`/`render_mode=auto` 逻辑，但当时被服务的 `crates/agent_world_viewer/dist` 产物不含相关标记。使用 `cd crates/agent_world_viewer && env -u NO_COLOR trunk build --dist dist` 重建后，再次打开同一 URL，页面自动跳转到 `software_safe.html?...software_safe_reason=software_renderer_detected`，不再出现黑屏 fatal。
- 验证：1) 原始复现控制台再次出现 `ANGLE ... SwiftShader`、`wgpu error: Validation Error`、`copy_deferred_lighting_id_pipeline`、`CONTEXT_LOST_WEBGL`；2) 重建后 `agent-browser snapshot -i` 可见 `Filter targets`、`Play/Pause/Step x1` 等交互元素，`window.__AW_TEST__.getState()` 返回 `renderMode=software_safe`、`rendererClass=software`、`softwareSafeReason=software_renderer_detected`。
- 结论：issue `#39` 对“当前本地被服务的旧 `dist` 产物”仍可复现；若使用最新源码重建 Viewer Web 产物，则黑屏 crash 已收口为 `software_safe` 自动降级。当前更准确的问题形态是“发布/运行路径仍可能拿到过期 viewer dist，导致 `#39` 表象复发”。
- 遗留事项：建议由 `viewer_engineer` / `qa_engineer` 继续确认 `world_game_launcher` / release bundle / `oasis7` 默认入口是否总是消费最新 Viewer dist，并补一条防止 stale dist 重新放出 `#39` 的门禁验证。

## 15:21:58 CST / viewer_engineer
- 完成内容：为 source-tree Viewer Web 入口补齐 stale dist freshness gate。`scripts/agent-browser-lib.sh` 现会把 `crates/agent_world_viewer/index.html`、`software_safe.html`、`software_safe.js`、`assets/`、`favicon.ico` 与 `Cargo.lock`/proto 输入纳入 freshness 计算；`.agents/skills/oasis7/scripts/oasis7-run.sh play` 在 repo source 模式下会先解析 fresh Viewer static dir，再把 `--viewer-static-dir <fresh-dir>` 传给 `world_game_launcher`，避免继续直接消费仓库里的旧 `crates/agent_world_viewer/dist`。
- 验证：`bash -n scripts/agent-browser-lib.sh scripts/bundle-freshness-lib.sh .agents/skills/oasis7/scripts/oasis7-run.sh scripts/agent-browser-viewer-dist-freshness-test.sh`、`bash ./scripts/agent-browser-viewer-dist-freshness-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-path-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-bootstrap-test.sh` 均通过。
- 遗留事项：当前已补 source-tree / bundle freshness scope，但尚未把同类 root-level Viewer 入口漂移回归接到更高层 required gate；若后续再次出现 stale dist 事故，建议由 `qa_engineer` 再补一条更贴近 real-play 的端到端 smoke。

## 15:21:58 CST / qa_engineer
- 完成内容：为 Viewer Web freshness gate 新增针对性回归脚本 `scripts/agent-browser-viewer-dist-freshness-test.sh`，显式覆盖“`software_safe.js` 新于 `dist/index.html` 时必须触发 trunk rebuild”这一失败签名，避免 issue `#39` 因 stale dist 重新表象复发。
- 结论：本轮门禁已把“源码已有 software-safe fallback，但旧 dist 仍被继续服务”的问题从隐性风险收口为可回归检测项。
- 遗留事项：若要把该问题纳入发布前强门禁，可再决定是否把新脚本并入更高层测试入口。

## 15:43:35 CST / agent_engineer
- 完成内容：修复 `oasis7-run.sh download` 在 release bundle 检测失败时仍可能继续执行并把宿主 `/.` 误拷进缓存 `bundle/` 的问题。现在脚本会在解压前校验 `archive_path` 存在，并在 `find_extracted_bundle_dir` 失败、返回空路径、返回非绝对路径或缺少 `run-game.sh` 时立刻非零退出，拒绝创建/填充缓存 bundle 目录。
- 验证：`bash -n .agents/skills/oasis7/scripts/oasis7-run.sh .agents/skills/oasis7/scripts/oasis7-run-download-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-download-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-path-test.sh`、`bash ./.agents/skills/oasis7/scripts/oasis7-run-bootstrap-test.sh` 均通过；新增的 download 负向回归已覆盖“解压目录缺少 `run-game.sh` 时必须失败且不得生成伪 bundle”路径。
- 遗留事项：后续若要进一步强化 operator 体验，可继续考虑把 bundle 失败签名透出到更高层 launcher/UI，但本轮宿主根目录误拷风险已收口。

## 15:43:35 CST / qa_engineer
- 完成内容：为 `#41` 补充负向回归：`oasis7-run-download-test.sh` 现在会显式模拟“archive 下载成功、解压成功、但 extracted bundle 缺少 `run-game.sh`”的失败路径，并检查脚本必须报错退出、stdout 不得输出 bundle 路径、缓存目录中不得出现 `dev/usr/lib` 等宿主根路径污染。
- 结论：本轮已把 `#41` 从高风险宿主文件系统误操作，收敛为可自动回归检测的 bundle 安全门禁问题。
- 遗留事项：如需把该门禁提升到更高层发布收口，可再评估是否并入更广的脚本组合测试。
