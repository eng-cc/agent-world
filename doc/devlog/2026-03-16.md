# 2026-03-16

## 00:04:40 CST / runtime_engineer
- 完成内容：定位 `v0.0.26` `release-gate-runtime` 新暴露的 3 个 `economy_priority_logistics` 失败用例，确认根因是 `ScheduleRecipe` 被拒后现在会继续追加 `FactoryProductionBlocked`，而旧测试仍把 `journal().events.last()` 误当成 `ActionRejected`。在 `crates/agent_world/src/runtime/tests/economy_priority_logistics.rs` 中补了“最近一次 `ActionRejected` / `FactoryProductionBlocked`” helper，并把 `stage gate / unlock_stage / preferred_factory_tags` 三条用例改成同时断言拒绝原因与 `governance_gate` 阻塞跟进事件；定向回归三条用例均已通过。
- 遗留事项：继续 push 新提交并重打 release tag，确认 `release-gate-runtime` 不再被这 3 条陈旧断言阻断；若下一轮 runtime gate 还暴露新失败，再按新根因继续收口。

## 10:20:05 CST / qa_engineer
- 完成内容：承接 release 耗时治理，基于成功 run `23114063444` 的关键路径拆解，确认 `runtime gate -> build-web-dist -> package-native` 是主瓶颈；本轮先落地低风险 P1：在 `.github/workflows/release-packages.yml` 的 `build-web-dist` 一次性产出 viewer / launcher 两份 web dist artifact，并给 `scripts/release-prepare-bundle.sh` / `scripts/build-game-launcher-bundle.sh` 增加 `--web-launcher-dist` 复用入口，让 `package-native` 不再为每个平台重复 `trunk install` 与 `agent_world_client_launcher` 的 `trunk build`。
- 遗留事项：继续观察下一轮 release 的关键路径是否从 package-native 明显收敛；若 runtime gate 仍是最长板，下一步再拆 `ci-tests.sh full` 的并行 shard。

## 11:33:00 CST / qa_engineer
- 完成内容：重跑 `v0.0.28` release 验证时，定位到 `package-native` 在 Linux/macOS 的回归根因并非 artifact 缺失，而是 `scripts/release-prepare-bundle.sh` 调 `scripts/build-game-launcher-bundle.sh` 时漏传了 `--web-launcher-dist`，导致脚本在打包机上错误回退到 `trunk` fallback 并因未安装 `trunk` 失败；现已补齐参数透传，保持 package 阶段纯复用预构建 viewer/launcher dist。
- 遗留事项：重新推一轮 release，确认 `build-web-dist` / `package-native` 新时长与上一轮相比是否实际下降；若仍有平台侧失败，再按 job 日志继续收口。

## 13:42:00 CST / qa_engineer
- 完成内容：继续推进 release 耗时治理，基于 `23126917164` 的成功 run 拆解确认 `build-web-dist` 仍有同轮 cache 未复用、bundle 原生打包仍有双次 cargo 调用；现已把 `.github/workflows/release-packages.yml` 的 `release-gate-web` 与 `build-web-dist` 统一到同一组 `Swatinem/rust-cache` shared-key，并把 `scripts/build-game-launcher-bundle.sh` 的 5 个原生二进制构建收敛到单次 `cargo build`，减少重复 bootstrap / metadata 解析成本，同时把 release 资产复用要求回写到 `doc/testing/prd.md` / `doc/testing/project.md`。
- 遗留事项：下一轮 release 需要重点观察 `build-web-dist` 的 `Install trunk` 是否因 cache 命中明显下降，以及 Windows `Build launcher bundle` 是否继续收敛；`release-gate-runtime` 内部的 `ci-tests.sh full` 仍占大头，后续若继续压时长需考虑按测试分片并行。

## 14:18:00 CST / qa_engineer
- 完成内容：继续沿 release 关键路径收口 runtime gate，基于上一轮成功 run 观察确认 `release-gate-runtime` 仍主要被 `./scripts/ci-tests.sh full` 串行占满；现已把 `scripts/ci-tests.sh` 扩成 `full-core` / `full-support` 两个 shard，并在 `.github/workflows/release-packages.yml` 中将 runtime gate 拆成 `release-gate-runtime-core`、`release-gate-runtime-support`、`release-gate-runtime-sync` 三个并行 job，再由聚合 `release-gate` 统一判定放行，保持语义不变同时压缩关键路径。
- 遗留事项：观察下一轮 release 中 `release-gate-runtime-core` 是否成为新的最长 shard，并确认 runtime shard 拆分没有引入 cache 抖动或 artifact 缺失；若仍需进一步压缩，再考虑细分 `agent_world` full-tier 自身测试集。

## 14:46:00 CST / qa_engineer
- 完成内容：继续针对 `release-gate-runtime-core` 热点做低风险重平衡，复盘 `23131974671` 后确认 `runtime-core` 中 `agent_world --features wasmtime --lib --bins` 约占 158s，且完全可以移入仍有余量的 `full-support` shard；现已在 `scripts/ci-tests.sh` 中调整 shard 归属，让 `full-core` 只承载最重的 `agent_world --tests` 全量集，而 `full-support` 接管 `agent_world --lib --bins`，并同步回写 testing PRD / project 的 shard 重平衡口径。
- 遗留事项：下一轮 release 需要确认 `runtime-core` 是否进一步下降到约 1240s 左右，以及 `runtime-support` 增长后是否仍低于新的 `runtime-core`；若最长板仍主要来自 `agent_world --tests`，下一步再考虑细分该测试集本身。

## 16:49:00 CST / qa_engineer
- 完成内容：按 `.agents/skills/oasis7/SKILL.md` 跑通一次真实 OpenClaw 本地试玩链路，确认 `127.0.0.1:18789` 由 OpenClaw Gateway 提供、`127.0.0.1:5841` 由 repo-backed `world_openclaw_local_bridge` 提供；使用 `oasis7-run.sh play --no-open-browser` 拉起 `world_game_launcher`（`openclaw_local_http` + `agent_world_p0_low_freq_npc`），再用 `agent-browser` 进入 `http://127.0.0.1:4173/?ws=ws://127.0.0.1:5011` 完成玩家新手链路：选中 `agent-0`、推进 tick，并在 `step x32` 后观察到新世界反馈，任务进度从 `3/4` 到 `4/4`。
- 完成内容：补记一条环境级体验结论：默认 headless `agent-browser` 会落到 `SwiftShader` 软件渲染并触发 `wgpu` shader compilation fatal，表现为 Viewer 黑屏；改用 `agent-browser --headed` 配合 `--use-angle=gl,--ignore-gpu-blocklist` 后切到 Intel/Mesa 硬件 WebGL，Web 试玩恢复正常。
- 遗留事项：`oasis7-run.sh download` 在本机本轮执行时停在 bundle 下载/解包阶段，真实试玩先走了 repo-backed bridge/bootstrap 路径；若后续要给制作人/运营稳定复用 bundle-first 路径，需单独复查 release bundle 下载卡点与校验/解包耗时表现。

## 17:00:30 CST / qa_engineer
- 完成内容：将本轮真实 `oasis7` 试玩中确认的问题逐个转成 GitHub issue：`#39 Web oasis7 playtest crashes in headless agent-browser on SwiftShader (Viewer black screen)` 聚焦默认 headless Web 闭环黑屏 / `wgpu` fatal；`#40 oasis7-run.sh download has no progress or phase output, so bundle-first bootstrap looks hung` 聚焦 bundle-first 首次下载阶段缺少进度与阶段反馈、易被误判为卡死的操作体验问题。
- 遗留事项：等待 owner 判断 `#39` 是走 viewer 兼容性降级、`agent-browser` 默认参数兜底，还是 `oasis7`/制作人脚本强制硬件 WebGL；`#40` 则可按低风险脚本改进补进度条/phase log，不必与功能修复耦合。

## 17:18:00 CST / runtime_engineer
- 完成内容：切换到 `runtime_engineer` 视角优先处理 `#40`，在 `.agents/skills/oasis7/scripts/oasis7-run.sh` 为 bundle-first 下载路径补了 operator 可见性：缓存复用提示、asset/checksum/extract/bundle-ready 阶段日志、TTY 下 `curl --progress-bar`、非 TTY 下周期性 heartbeat（默认 `10s`，可用 `OASIS7_DOWNLOAD_HEARTBEAT_SECS` 覆盖），解决首次拉取 release bundle 长时间静默、易被误判为卡死的问题。
- 完成内容：新增回归脚本 `.agents/skills/oasis7/scripts/oasis7-run-download-test.sh`，并已通过 `bash -n .agents/skills/oasis7/scripts/oasis7-run.sh`、`.agents/skills/oasis7/scripts/oasis7-run-download-test.sh`、`.agents/skills/oasis7/scripts/oasis7-run-path-test.sh`、`.agents/skills/oasis7/scripts/oasis7-run-bootstrap-test.sh`。
- 完成内容：按 `PRD-WORLD_SIMULATOR-037` 回写 `doc/world-simulator/llm/llm-openclaw-local-http-provider-integration-2026-03-12.prd.md`、`doc/world-simulator/llm/llm-openclaw-local-http-provider-integration-2026-03-12.project.md` 与 `doc/world-simulator/project.md`，把本次修复追踪为 `TASK-WORLD_SIMULATOR-138`。
- 遗留事项：`#39` 的 SwiftShader / `wgpu` fatal 仍待单独处理；若后续要继续改善 `#40`，可以再评估是否给 bundle 下载输出 archive 大小 / 总耗时等摘要信息。
