## 09:31:47 CST / viewer_engineer
- 完成内容：
  - 修复 `crates/agent_world_viewer/src/web_test_api.rs` 在 wasm 构建下的借用冲突：`snapshot.last_control_feedback.as_mut()` 更新期间不再直接读取 `snapshot.control_profile`，恢复 `agent_world_viewer` 的 Web 产物编译能力。
  - 新增 `scripts/viewer-post-onboarding-qa.sh`，把 `#46` 的 Web required-tier 复验固化为 `connected -> step(8) -> select(first_agent) -> step(24)` bundle-first 流程，自动导出状态、截图、console 与 QA summary。
- 执行验证：
  - `env -u RUSTC_WRAPPER cargo check -p agent_world_viewer --target wasm32-unknown-unknown`
  - `./scripts/build-game-launcher-bundle.sh --out-dir output/release/game-launcher-local`
- 遗留事项：
  - `__AW_TEST__` 目前仍未直接暴露 `PostOnboarding` 语义字段，脚本结论仍需配合截图人工复核；若后续继续强化 QA 自动化，可补更细的 UI 语义快照。

## 09:41:03 CST / qa_engineer
- 完成内容：
  - 使用 `./scripts/viewer-post-onboarding-qa.sh --bundle-dir output/release/game-launcher-local --no-llm` 在 fresh bundle Web 会话完成 `#46` 闭环验证，产出 `output/playwright/playability/post-onboarding-20260319-094056/` 证据包。
  - 人工复核 `00-initial.png`、`01-post-onboarding-entry.png`、`02-post-onboarding-followup.png`，确认 `4/4` 后 Mission HUD 切到 `PostOnboarding`、顶部首局总结改为进入下一阶段语义，且 onboarding 提示不再持续驻留。
  - 回写 `doc/playability_test_result/card_2026_03_19_09_40_56.md`、`doc/game/gameplay/gameplay-post-onboarding-stage-2026-03-18.project.md`、`doc/game/project.md` 与 `testing-manual.md`，将 `TASK-GAMEPLAY-POD-004` 收口为 completed。
- 执行验证：
  - `./scripts/viewer-post-onboarding-qa.sh --bundle-dir output/release/game-launcher-local --no-llm`
  - `cat output/playwright/playability/post-onboarding-20260319-094056/post-onboarding-summary.md`
  - `cat output/playwright/playability/post-onboarding-20260319-094056/console.errors.log`
- 遗留事项：
  - 右侧聊天区仍可见与本任务无关的 `AgentNotFound` 历史噪音，当前记为观察项，不构成 `#46` 阻断。

## 10:15:20 CST / qa_engineer
- 完成内容：
  - 新增 `scripts/viewer-post-onboarding-headless-smoke.sh`，把 `#46` 的无 UI 验证固化为 live TCP 协议烟测：`hello -> subscribe -> request_snapshot -> step(8) -> request_snapshot -> step(24) -> request_snapshot`。
  - 新增 `doc/testing/evidence/post-onboarding-headless-smoke-2026-03-19.md`，明确该脚本只验证同会话控制推进、快照推进与 `RuntimeEvent` feed，不替代 headed Web/UI 卡片渲染验收。
  - 回写 `testing-manual.md`、`doc/game/gameplay/gameplay-post-onboarding-stage-2026-03-18.project.md` 与 `doc/game/project.md`，补齐 `#46` 无 UI required-tier 证据入口。
- 执行验证：
  - `./scripts/viewer-post-onboarding-headless-smoke.sh --bundle-dir output/release/game-launcher-local --no-llm --viewer-port 4273 --web-bind 127.0.0.1:5111 --live-bind 127.0.0.1:5123 --chain-status-bind 127.0.0.1:5231`
- 遗留事项：
  - 无 UI 烟测只证明协议与事件流存在，不证明屏幕上的 `PostOnboarding` 卡片文案；后者继续以 headed Web 证据为准。

## 11:20:57 CST / producer_system_designer
- 完成内容：
  - 新增 `doc/game/gameplay/gameplay-pure-api-client-parity-2026-03-19.{prd,design,project}.md`，把“纯 API 客户端在信息粒度、动作能力和持续游玩上与 UI 等价”正式定义为 `PRD-GAME-008`。
  - 回写 `doc/game/prd.md`、`doc/game/project.md`、`doc/game/gameplay/gameplay-top-level-design.{prd,project}.md`、`doc/game/prd.index.md` 与 `doc/game/README.md`，将 pure API parity 从口头目标提升为可追踪专题与任务。
  - 明确下一执行切片为 `TASK-GAMEPLAY-API-002`：先对齐协议级 canonical 玩家语义，再做正式玩家动作面与 parity 验收。
- 执行验证：
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 当前仅完成需求冻结与任务拆解，尚未下沉 UI 私有语义到协议层；在 `TASK-GAMEPLAY-API-002/003/004` 完成前，不得宣称 pure API 已达到正式可玩等价。

## 12:04:47 CST / viewer_engineer
- 完成内容：
  - 在 `crates/agent_world/src/simulator/persist.rs` 新增 `player_gameplay` canonical 玩家快照 schema，把 `stage / goal / progress / blocker / next_step / available_actions / recent_feedback` 下沉到 live `WorldSnapshot`。
  - 在 `crates/agent_world/src/viewer/runtime_live.rs` 与 `crates/agent_world/src/viewer/runtime_live/gameplay_snapshot.rs` 打通 live runtime -> protocol 聚合链路：基于 runtime 状态与最近一次 live control ack 生成 `FirstSessionLoop -> PostOnboarding` 的玩家阶段语义，纯 API 客户端不再需要猜测 UI 私有主目标。
  - 重写 `crates/agent_world/src/viewer/runtime_live/tests.rs` 的定向测试，确认默认快照暴露 `FirstSessionLoop` canonical 字段，且收到推进反馈后会切到 `PostOnboarding` 并保留 `recent_feedback`。
  - 回写 `doc/game/gameplay/gameplay-pure-api-client-parity-2026-03-19.project.md` 与 `doc/game/project.md`，将 `TASK-GAMEPLAY-API-002` 标记完成并把下一步推进到 `TASK-GAMEPLAY-API-003`。
- 执行验证：
  - `env -u RUSTC_WRAPPER cargo check -p agent_world`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world compat_snapshot_exposes_player_gameplay_snapshot -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world compat_snapshot_promotes_to_post_onboarding_after_control_feedback -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world runtime_live_snapshot_includes_runtime_snapshot_payload -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo check -p agent_world_viewer --target wasm32-unknown-unknown`
- 遗留事项：
  - 当前只完成 canonical 玩家语义下沉，尚未把纯 API 正式玩家动作面补齐到“长期持续游玩”等价；`TASK-GAMEPLAY-API-003` 仍需继续补查看/推进/聊天/恢复之外的正式玩法动作入口与客户端恢复契约。

## 12:18:44 CST / agent_engineer
- 完成内容：
  - 新增 `crates/agent_world/src/bin/world_pure_api_client.rs`，交付参考纯 API 客户端，直接复用 live TCP viewer 协议与 Ed25519 玩家签名链路，支持 `snapshot / step / play / pause / agent_chat / prompt_control / reconnect_sync / rotate_session / revoke_session`。
  - 为客户端补齐最小单测，覆盖公钥派生、`agent_chat` 签名请求和 `prompt_control apply` 签名请求，确保无 UI 模式下玩家动作不会依赖 Viewer 私有前端逻辑生成 payload。
  - 在 source no-LLM live 会话上用新客户端实际跑通 `snapshot --player-gameplay-only`、`step --count 2 --events` 和 `reconnect-sync --player-id player-api-demo`，确认 pure API 客户端可以直接查看 canonical 玩家语义、推进世界并请求阶段恢复 ack。
  - 回写 `doc/game/gameplay/gameplay-pure-api-client-parity-2026-03-19.project.md`、`doc/game/project.md` 与 `testing-manual.md`，将 `TASK-GAMEPLAY-API-003` 收口并把下一步推进到 `TASK-GAMEPLAY-API-004`。
- 执行验证：
  - `env -u RUSTC_WRAPPER cargo check -p agent_world --bin world_pure_api_client`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world --bin world_pure_api_client -- --nocapture`
  - `cargo run -q -p agent_world --bin world_pure_api_client -- keygen`
  - `./scripts/run-game-test.sh --no-llm --viewer-port 4275 --web-bind 127.0.0.1:5115 --live-bind 127.0.0.1:5125 --chain-status-bind 127.0.0.1:5235`
  - `cargo run -q -p agent_world --bin world_pure_api_client -- --addr 127.0.0.1:5125 snapshot --player-gameplay-only`
  - `cargo run -q -p agent_world --bin world_pure_api_client -- --addr 127.0.0.1:5125 step --count 2 --events`
  - `cargo run -q -p agent_world --bin world_pure_api_client -- --addr 127.0.0.1:5125 reconnect-sync --player-id player-api-demo --with-snapshot`
  - `env -u RUSTC_WRAPPER cargo check -p agent_world`
  - `env -u RUSTC_WRAPPER cargo check -p agent_world_viewer --target wasm32-unknown-unknown`
- 遗留事项：
  - 当前 pure API 客户端已具备正式动作面，但 UI/API 是否达到“等价可玩”仍需 `qa_engineer` 在 `TASK-GAMEPLAY-API-004` 中给出 parity matrix、required-tier 长玩与 full-tier 长稳结论。

## 13:25:04 CST / qa_engineer
- 完成内容：
  - 新增 `scripts/world-pure-api-parity-smoke.sh`，把 pure API required/full 回归固化为可复跑脚本：自动拉起 stack，调用 `world_pure_api_client` 完成 `snapshot -> step -> step -> reconnect-sync`，并输出 JSON/Markdown summary。
  - 在 source required、fresh bundle required 与 bundle full 三条链路上执行 pure API 回归，产出 `output/playwright/playability/pure-api-required-20260319-131630/`、`output/playwright/playability/pure-api-required-20260319-132259/` 与 `output/playwright/playability/pure-api-full-20260319-132316/`。
  - 新增 `doc/testing/evidence/pure-api-parity-validation-2026-03-19.md` 与 `doc/playability_test_result/card_2026_03_19_13_25_04.md`，给出 UI/API parity matrix 与 playability 结论：当前 pure API 已达到 `playable`，但还不能记为 `parity_verified`。
  - 回写 `doc/game/gameplay/gameplay-pure-api-client-parity-2026-03-19.project.md`、`doc/game/project.md` 与 `testing-manual.md`，明确当前阻断集中在恢复面不完整、未证明到达首个持续能力里程碑、以及 UI 尚未完全消费 canonical `player_gameplay`。
- 执行验证：
  - `./scripts/build-game-launcher-bundle.sh --out-dir output/release/game-launcher-local`
  - `./scripts/world-pure-api-parity-smoke.sh --tier required --no-llm --viewer-port 4277 --web-bind 127.0.0.1:5117 --live-bind 127.0.0.1:5127 --chain-status-bind 127.0.0.1:5237`
  - `./scripts/world-pure-api-parity-smoke.sh --tier required --bundle-dir output/release/game-launcher-local --no-llm --viewer-port 4278 --web-bind 127.0.0.1:5118 --live-bind 127.0.0.1:5128 --chain-status-bind 127.0.0.1:5238`
  - `./scripts/world-pure-api-parity-smoke.sh --tier full --bundle-dir output/release/game-launcher-local --no-llm --viewer-port 4279 --web-bind 127.0.0.1:5119 --live-bind 127.0.0.1:5129 --chain-status-bind 127.0.0.1:5239`
- 遗留事项：
  - `TASK-GAMEPLAY-API-004` 仍未收口。需要后续补齐：`reconnect-sync` 一步恢复 `player_gameplay`、pure API 到首个持续能力里程碑的 required/full 证据，以及 UI 对 canonical `player_gameplay` 的直接消费。

## 15:10:42 CST / runtime_engineer
- 完成内容：
  - 修复 `crates/agent_world/src/bin/world_pure_api_client.rs` 的 `reconnect-sync --with-snapshot` 行为，改为在 recovery ack 后显式请求一次 live `snapshot`，现在同一条命令可直接恢复 `latest_snapshot + player_gameplay`。
  - 调整 `crates/agent_world_viewer/src/egui_right_panel_player_guide.rs`：Mission HUD / PostOnboarding 主卡改为优先消费 canonical `snapshot.player_gameplay`，只有缺少协议级玩家语义时才回退旧事件聚合逻辑。
  - 为 Viewer 新增 canonical 消费回归用例，并顺手补齐 `agent_world_viewer` 内所有 `WorldSnapshot` 测试 fixture 的 `player_gameplay: None` 字段，恢复 Viewer test compile 基线。
  - 回写 `doc/testing/evidence/pure-api-parity-validation-2026-03-19.md`、`doc/playability_test_result/card_2026_03_19_13_25_04.md`、`doc/game/gameplay/gameplay-pure-api-client-parity-2026-03-19.project.md`、`doc/game/project.md` 与 `testing-manual.md`，把 pure API parity 的剩余阻断收敛为“尚未证明可达首个持续能力里程碑”这一项。
- 执行验证：
  - `env -u RUSTC_WRAPPER cargo test -q -p agent_world --bin world_pure_api_client`
  - `env -u RUSTC_WRAPPER cargo test -q -p agent_world_viewer build_player_post_onboarding_snapshot_prefers_canonical_player_gameplay_snapshot`
  - `env -u RUSTC_WRAPPER cargo test -q -p agent_world_viewer build_player_post_onboarding_snapshot_uses_canonical_blocker_fields`
  - `env -u RUSTC_WRAPPER cargo check -q -p agent_world -p agent_world_viewer`
  - `env -u RUSTC_WRAPPER cargo build -q -p agent_world --bin world_pure_api_client`
  - `target/debug/world_pure_api_client --addr 127.0.0.1:5132 reconnect-sync --player-id player-api-smoke --with-snapshot`
  - `./scripts/world-pure-api-parity-smoke.sh --tier required --no-llm --viewer-port 4283 --web-bind 127.0.0.1:5123 --live-bind 127.0.0.1:5133 --chain-status-bind 127.0.0.1:5243`
- 遗留事项：
  - pure API parity 仍未达 `parity_verified`。当前真正剩余的问题是：no-LLM 路径缺少把 `PostOnboarding` 推进到首个持续能力里程碑的正式动作面，required/full 证据还不能证明“持续玩”已经闭环。

## 15:17:59 CST / runtime_engineer
- 完成内容：
  - 定位并修复 pure API fresh no-LLM 路径中的真实阻断：`runtime_snapshot` 内部 `pending_factory_builds` 等 `u64` 键 map 在 JSON 中会变成字符串键，native 反序列化在首次工厂建造 `step` 后报 `invalid type: string "6", expected u64`。
  - 在 `crates/agent_world/src/runtime/{util,state,snapshot}.rs` 为数值键 map 增加统一字符串键兼容反序列化，并在 `crates/agent_world/src/simulator/tests/persist.rs` 补回归 `snapshot_runtime_snapshot_accepts_stringified_numeric_map_keys`。
- 执行验证：
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -q -p agent_world snapshot_runtime_snapshot_accepts_stringified_numeric_map_keys`
  - `env -u RUSTC_WRAPPER cargo check -q -p agent_world -p agent_world_viewer`
- 遗留事项：
  - 继续由 `qa_engineer` 复跑 pure API required/full，确认 `TASK-GAMEPLAY-API-004` 是否可以正式收口。

## 15:18:47 CST / qa_engineer
- 完成内容：
  - 使用 fresh source no-LLM live 会话复跑 pure API required/full 收口链路，确认 `gameplay_action(build smelter) -> gameplay_action(schedule iron ingot) -> reconnect-sync` 已能稳定推进到 `post_onboarding.choose_midloop_path`。
  - 回写 `doc/testing/evidence/pure-api-parity-validation-2026-03-19.md`、`doc/playability_test_result/card_2026_03_19_13_25_04.md`、`doc/game/gameplay/gameplay-pure-api-client-parity-2026-03-19.project.md`、`doc/game/project.md` 与 `testing-manual.md`，将 `TASK-GAMEPLAY-API-004` 从 blocked 收口为 completed。
- 执行验证：
  - `./scripts/world-pure-api-parity-smoke.sh --tier required --no-llm --viewer-port 4297 --web-bind 127.0.0.1:5161 --live-bind 127.0.0.1:5163 --chain-status-bind 127.0.0.1:5249`
  - `./scripts/world-pure-api-parity-smoke.sh --tier full --no-llm --viewer-port 4299 --web-bind 127.0.0.1:5171 --live-bind 127.0.0.1:5173 --chain-status-bind 127.0.0.1:5251`
- 遗留事项：
  - 继续建议在 release gate 中优先保留 bundle 口径抽样，但这已不再阻断当前 pure API parity 结论。

## 16:33:45 CST / producer_system_designer
- 完成内容：
  - 新增 `doc/core/player-access-mode-contract-2026-03-19.{prd,design,project}.md`，冻结 `standard_3d / software_safe / pure_api` 三模式总契约，统一默认用途、fallback、claim envelope 与禁宣称项。
  - 明确 `player_parity / headless_agent / debug_viewer` 属于 execution lane，而不是玩家访问模式；要求后续 release / QA / playability 结论先绑定 mode，再附加 lane。
  - 回写 `doc/core/prd.md`、`doc/core/project.md`、`doc/core/prd.index.md` 与 `doc/core/README.md`，把 `PRD-CORE-009 / TASK-CORE-028` 挂入 core 主入口与索引。
- 执行验证：
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 后续若 `testing-manual.md`、`world-simulator`、`game` 或 `OpenClaw` 专题新增同层模式别名，必须先按 `PRD-CORE-009` 回写 taxonomy，再继续扩展下游文档。

## 18:03:32 CST / producer_system_designer
- 完成内容：
  - 回写 `doc/core/project.md` 与 `doc/core/player-access-mode-contract-2026-03-19.project.md`，新增 `TASK-CORE-029 / T4`，把“三模式总契约”的下游术语统一纳入正式追踪。
  - 回写 `testing-manual.md`、`doc/world-simulator/prd.md`、`doc/world-simulator/project.md`、`doc/world-simulator/llm/llm-openclaw-agent-dual-mode-2026-03-16.prd.md`、`doc/testing/openclaw-dual-mode-t4-blocker-2026-03-16.md`、`doc/game/prd.md` 与 `doc/game/gameplay/gameplay-pure-api-client-parity-2026-03-19.prd.md`，明确 `standard_3d / software_safe / pure_api` 是玩家访问模式，`player_parity / headless_agent / debug_viewer` 是 execution lane。
  - 明确 pure API 验证属于 `pure_api` 正式入口，OpenClaw T4 / `oasis7` 默认策略属于 lane 默认值，避免把 lane 误写成“第四套游戏入口”。
- 执行验证：
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 后续若 `world-simulator` 或 `testing` 再新增 OpenClaw / Viewer 口径文档，必须沿用“先 mode、再 lane”的结论顺序；否则会重新引入 claim 混淆。

## 19:47:26 CST / liveops_community
- 完成内容：
  - 回写 `doc/readme/project.md` 与 `doc/site/project.md`，新增 `TASK-README-013` / `TASK-SITE-013`，将三模式公开口径同步纳入 `readme` 与 `site` 模块正式追踪。
  - 回写根 `README.md`，把旧“离线回放 / 在线脚本 / 在线 LLM”运行模式摘要改成技术预览访问面说明，明确当前公开访问面为 `standard_3d / software_safe / pure_api`，并说明 execution lane 不是额外对外模式。
  - 回写 `site/index.html` 与 `site/en/index.html`，将“Online Verification (LLM default)”与 proof chip 的旧表述收敛为三模式访问面口径，避免外部读者把执行方式误解成第四套公开入口。
- 执行验证：
  - `./scripts/readme-link-check.sh`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 若后续 `site/doc` 或 release note 首页要显式介绍三模式，需继续沿用“技术预览访问面”而非“已开放玩家模式”的保守表述。

## 20:07:00 CST / liveops_community
- 完成内容：
  - 定位最新 `Pages` workflow 失败根因：`scripts/site-manual-sync-check.sh` 仍要求旧版 `agent-browser --headed open "http://127.0.0.1:4173/?ws=ws://127.0.0.1:5011"` 基线，而源手册与 `site/doc/cn|en/viewer-manual.html` 已统一切到 `&test_api=1` 新入口，导致 `Site Quality Gates` 假失败。
  - 回写 `scripts/site-manual-sync-check.sh`，将 required pattern 升级为当前 `&test_api=1` 命令；并回写 `doc/site/project.md`，新增 `TASK-SITE-014` 追踪本次 CI 修复。
- 执行验证：
  - `./scripts/site-manual-sync-check.sh`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 若后续 viewer manual 再调整 query 参数，需同步更新 `site-manual-sync-check.sh`，否则 Pages gate 会再次出现同类假失败。

## 20:24:28 CST / liveops_community
- 完成内容：
  - 站在 `liveops_community` 视角调研 `https://www.moltbook.com/`、`/developers` 与 `/help` 当前公开口径，确认 Moltbook 的公开定位是 agent social network、human-verified identity、submolts 社区与 developer early access，而不是普通泛社媒。
  - 新增 `doc/readme/governance/readme-moltbook-promotion-plan-2026-03-19.{prd,design,project,md}`，沉淀面向 Moltbook 的推广方案，覆盖平台判断、账号定位、内容支柱、30 天节奏、禁宣称项、反馈回流与 KPI。
  - 回写 `doc/readme/prd.md`、`doc/readme/project.md`、`doc/readme/prd.index.md` 与 `doc/readme/README.md`，将 `PRD-README-010 / TASK-README-014` 纳入 readme 模块正式追踪。
  - 新增 `doc/readme/governance/liveops-to-producer-task-readme-014-moltbook-promotion-plan-2026-03-19.md`，把该方案挂入 `liveops_community -> producer_system_designer` 的审核链。
- 执行验证：
  - `rg -n "Moltbook|内容支柱|禁宣称项|30 天执行节奏|反馈回流" doc/readme/governance/readme-moltbook-promotion-plan-2026-03-19.md doc/readme/governance/readme-moltbook-promotion-plan-2026-03-19.prd.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 若后续要进入真实 Moltbook 发帖阶段，仍需先基于该方案再产出具体贴文草案，并由 `producer_system_designer` 做发布前口径审核。

## 20:39:18 CST / liveops_community
- 完成内容：
  - 在 `doc/readme/prd.md` 与 `doc/readme/project.md` 新增 `PRD-README-011 / TASK-README-015`，将“Molbook 首批发帖草案包”纳入正式追踪。
  - 新增 `doc/readme/governance/readme-moltbook-post-drafts-2026-03-19.{prd,design,project,md}`，沉淀 6 条英文主贴、6 条首评补充、6 条高频回复模板，以及禁宣称提醒与发前复核清单。
  - 新增 `doc/readme/governance/liveops-to-producer-task-readme-015-moltbook-post-drafts-2026-03-19.md`，把该文案包挂入 `liveops_community -> producer_system_designer` 审核链。
  - 同步回写 `doc/readme/README.md` 与 `doc/readme/prd.index.md`，确保新专题在模块入口可达。
- 执行验证：
  - `rg -n "Post 1|Post 6|Reply Template|Do Not Say|Review Status" doc/readme/governance/readme-moltbook-post-drafts-2026-03-19.md doc/readme/governance/readme-moltbook-post-drafts-2026-03-19.prd.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 真实发帖前仍建议按当周 feed 语气做轻微开头优化，但不得删掉 `technical preview / not playable yet` 边界。

## 20:46:32 CST / liveops_community
- 完成内容：
  - 回写 `doc/readme/prd.md` 与 `doc/readme/project.md`，新增 `PRD-README-012 / TASK-README-016`，把“体验/观察技术预览后可去 GitHub 提 issue 或 PR”的回流 CTA 纳入 Moltbook 渠道正式追踪。
  - 更新 `doc/readme/governance/readme-moltbook-promotion-plan-2026-03-19.md`，将 GitHub `issue` / `PR` 明确写入 builder-facing CTA 与外部回流出口。
  - 更新 `doc/readme/governance/readme-moltbook-post-drafts-2026-03-19.md`，给 6 条首批主贴补齐 GitHub 反馈/贡献 CTA，并新增 2 条 GitHub 回复模板。
- 执行验证：
  - `rg -n "GitHub|issue|PR|pull request" doc/readme/governance/readme-moltbook-promotion-plan-2026-03-19.md doc/readme/governance/readme-moltbook-post-drafts-2026-03-19.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 真正对外发帖时，仍要把 CTA 保持在“after you inspect or try the preview”这一表述，不能改写成“after you play the game”。

## 20:54:18 CST / liveops_community
- 完成内容：
  - 回写 `doc/readme/prd.md` 与 `doc/readme/project.md`，新增 `PRD-README-013 / TASK-README-017`，把“首批 Moltbook 帖文压缩为更短的 feed-native 版本”纳入正式追踪。
  - 更新 `doc/readme/governance/readme-moltbook-post-drafts-2026-03-19.md`，将 6 条主贴主文压缩为更短的最终发布版，并把更长解释继续留在首评承接。
  - 将文案包状态调整为 `short_form_draft_for_internal_review`，明确当前内容是面向最终发布的短版，而不是长草稿。
- 执行验证：
  - `rg -n "Short-form publish note|technical preview|not playable yet|GitHub issue or PR" doc/readme/governance/readme-moltbook-post-drafts-2026-03-19.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项：
  - 真实发帖前仍可针对当周 feed 语气微调首句，但不要再把主文重新拉长。
