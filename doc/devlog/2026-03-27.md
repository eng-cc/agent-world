# 2026-03-27

## 09:26:45 CST / viewer_engineer
- 完成内容:
  - 把 `crates/oasis7/src/viewer/web_bridge.rs` 的 `ViewerWebBridge::run()` 从串行 `accept -> serve_stream` 改成每个 websocket 一条独立桥接线程，补掉 browser/web-bridge 层最后一个单连接瓶颈。
  - 新增定向回归 `bridge_run_accepts_second_websocket_while_first_stays_open`，要求首个 websocket 保持打开时第二个 websocket 仍能透过 bridge 连到 upstream。
  - 用真实本地 hosted `oasis7_game_launcher --deployment-mode hosted_public_join --chain-disable` 栈复验双浏览器接入：两个独立 `agent-browser` session 最终都能收敛到 `debug_viewer:subscribed` 并看到同一份 live snapshot，不再永久停在第二页 detached。
- 验证:
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib bridge_run_accepts_second_websocket_while_first_stays_open -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_live_run_accepts_probe_while_viewer_session_is_open -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
  - `agent-browser --session hosted-concurrent-a open 'http://127.0.0.1:6101/?ws=ws://127.0.0.1:6102'`
  - `agent-browser --session hosted-concurrent-b open 'http://127.0.0.1:6101/?ws=ws://127.0.0.1:6102'`
  - `agent-browser --session hosted-concurrent-a get text body`
  - `agent-browser --session hosted-concurrent-b get text body`
- 遗留事项:
  - 第二个页面首轮 attach 仍可能短暂经历一次自动重连窗口后才进入 `subscribed`；当前已确认不是永久卡死，也不再阻断双页并发观察。
  - hosted player lane 的下一真实 blocker 仍是带 backend grant 的 `prompt_control_*` success evidence，而不是 browser/web-bridge 并发接入。

## 09:46:07 CST / producer_system_designer
- 完成内容:
  - 对齐 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md`、`doc/p2p/project.md` 与当前真实 hosted 联调状态，明确 `TASK-P2P-041-D` 的 transport / probe / 多页并发问题已不再是主 blocker。
  - 把真实联调分层结论正式落文：伪造 env signer 会被拒绝为 keypair mismatch；换成真实匹配 signer 后，不带 `--with-llm` 会停在 `llm_mode_required`；带 `--with-llm` 的 attached `prompt_control` 链路当前真实 blocker 为 `release_token does not map to an active player slot`。
  - 冻结当前判断：浏览器/runtime 仍显示 session 已注册且 agent 已绑定，但 admission 漂移到 `active_player_sessions=0` 且 `runtime_only_player_sessions=1`，说明 hosted session issuer 在 runtime 仍持有绑定时提前释放了 active slot；后续工程 owner 应优先修复该生命周期竞争，再补真实 success evidence。
- 验证:
  - `rg -n "release_token does not map to an active player slot|llm_mode_required|keypair mismatch|生命周期竞争|真实 signer" doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md doc/p2p/project.md doc/devlog/2026-03-27.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 本次仅完成状态对齐，尚未修复 `release_token` 生命周期竞争本体。
  - `TASK-P2P-041-D` 仍未取得“真实 signer + --with-llm + attached player session + backend grant + prompt_control success”的最终通过证据。

## 10:00:12 CST / runtime_engineer
- 完成内容:
  - 修复 hosted session issuer 的 `release_token` 生命周期竞争：`HostedPlayerSessionIssuer::observe_runtime_active_players()` 现改为先使用当前 runtime probe snapshot 给仍在 runtime 中绑定的 active slot 续租，再执行过期清理，避免本轮 probe 明明已看到该 player 仍活跃时，还先按旧 `last_seen` 把 release token 清掉。
  - 新增定向回归 `hosted_player_session_runtime_probe_refreshes_runtime_bound_slot_before_expiry_prune`，冻结“runtime 仍绑定时，不得因 probe 前的 lease 过线而把 active slot 误剪成 runtime-only occupancy”这一修复语义。
  - 执行 `oasis7_game_launcher` bin 全量单测，确认 hosted player-session / runtime presence / strong-auth 现有分支未被这次修复打坏。
- 验证:
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_runtime_probe_refreshes_runtime_bound_slot_before_expiry_prune -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_runtime_seen_slots_keep_full_lease_ttl -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_runtime_reconcile_releases_seen_players_missing_from_runtime -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
- 遗留事项:
  - 尚未重跑真实 hosted `prompt_control` 强鉴权成功链路，`release_token does not map to an active player slot` 是否已在真实栈消失还需实链验证。
  - 若真实链路通过，下一层 blocker 需要重新盘点；若仍失败，应继续检查 viewer heartbeat 与 issuer/reconnect 之间的时序竞争。

## 10:18:43 CST / runtime_engineer
- 完成内容:
  - 定位出 `release_token` 的第二层竞争不在 120s lease，而在 `pending_registration_ttl_ms`：当 player session issue 已超过 30s、但刚完成 `register_session` 且后台 runtime presence monitor 还没来得及看到新绑定时，首个 strong-auth grant 会把 slot 仍当作 pending 直接剪掉。
  - 在 public player plane 路由前补了同步 runtime reconcile：`admission/issue/refresh/release/strong-auth grant` 处理前会短连 `live_bind` 拉最新 snapshot，把刚完成 register 的 runtime binding 先回写到 issuer，再继续 hosted session / grant 逻辑。
  - 用真实 signer + `--with-llm` + browser-local ephemeral key + approval code 重新跑 hosted 栈，最终在未绑定的 `agent-1` 上确认 `prompt_control preview` 与 `prompt_control apply` 都已通过，`strongAuthLastGrantError = null`，且 runtime snapshot 显示 `agent-1` 已绑定当前 player。
- 验证:
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `agent-browser --session hosted-prompt-rerun2 open 'http://127.0.0.1:6101/?ws=...&hosted_access=...'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-prompt-rerun2 'window.__AW_TEST__.setStrongAuthApprovalCode("preview-code")'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-prompt-rerun2 'window.__AW_TEST__.sendPromptControl("preview", { agentId: "agent-1", ... })'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-prompt-rerun2 'window.__AW_TEST__.sendPromptControl("apply", { agentId: "agent-1", ... })'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-prompt-rerun2 'window.__AW_TEST__.getState()'`
  - `python3 - <<'PY' ... request_snapshot ... print(snapshot["model"]["agent_player_bindings"]) ... PY`
- 遗留事项:
  - 当前通过仍只覆盖 `prompt_control_*` 的 preview-grade backend reauth；`main_token_transfer` 继续保持 `blocked_until_strong_auth`。
  - 默认 `llm_bootstrap` 场景里 `agent-0` 已被历史 player 占用，真实 hosted 验证时需要选用未绑定 agent，或后续补更明确的占用提示 / handoff 流程。

## 10:47:11 CST / runtime_engineer
- 完成内容:
  - 给 runtime-live 的 `authoritative_recovery_ack/error` 补了结构化 `revoke_reason/revoked_by` contract，并在 runtime 侧按 `player_id + session_pubkey` 记住真实 revoke 元数据，让后续 `reconnect_sync/register_session` 再命中 `session_revoked` 时不再只回裸错误码。
  - `software_safe.js` 现会把上述 revoke 元数据保留到 guest fallback，并把 `Hosted Recovery` 面板文案、`__AW_TEST__.getState().authRevokeReason/authRevokedBy` 与 `hostedRecoveryHint` 一起更新；remote revoke / operator kick 后页面不会再静默自动补发新会话掩盖撤销原因。
  - 扩展 `runtime_authoritative_recovery_rotate_and_revoke_session_enforced_for_agent_chat`：新增断言覆盖 revoke ack、`ReconnectSync` revoked error 与 `RegisterSession` revoked error 的结构化 `revoke_reason/revoked_by`。
- 验证:
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_authoritative_recovery_rotate_and_revoke_session_enforced_for_agent_chat -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 当前只补了 recovery / public player 面的 revoke 结构化回流；更完整的 hosted handoff UI、operator 批量 kick 流程和对外更正模板仍待后续专题收口。
  - 若后续要让 runbook 直接引用浏览器证据，还需要补一份新的 agent-browser revoke drill 证据文档。

## 11:36:58 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/hosted-world-browser-revoke-recovery-2026-03-27.md`，用真实 hosted 页面确认 external `revoke-session` 之后，下一轮 heartbeat 会把浏览器从 `player_session` 收敛到 `guest_session`。
  - 实测页面不仅返回 `authError=session_revoked...`，还会同步暴露 `authRevokeReason=operator_kick_for_drill`、`authRevokedBy=hosted-revoke-operator` 与带操作者/原因的 `Hosted Recovery` 文案，证明这轮结构化 revoke metadata 已真正到达玩家面。
  - 取证过程中确认一条执行前提：要让本地 hosted 栈使用最新 recovery contract，需先显式重编 `oasis7_viewer_live` sibling bin，再拉起 `oasis7_game_launcher`。
- 验证:
  - `env -u RUSTC_WRAPPER cargo build -q -p oasis7 --bin oasis7_viewer_live`
  - `env OASIS7_HOSTED_STRONG_AUTH_PUBLIC_KEY=... OASIS7_HOSTED_STRONG_AUTH_PRIVATE_KEY=... OASIS7_HOSTED_STRONG_AUTH_APPROVAL_CODE=preview-code env -u RUSTC_WRAPPER cargo run -q -p oasis7 --bin oasis7_game_launcher -- --deployment-mode hosted_public_join --viewer-static-dir crates/oasis7_viewer --viewer-host 127.0.0.1 --viewer-port 6201 --web-bind 127.0.0.1:6202 --live-bind 127.0.0.1:6203 --chain-disable --no-open-browser`
  - `source scripts/agent-browser-lib.sh && ab_open hosted-revoke-evidence 0 'http://127.0.0.1:6201/software_safe.html?ws=ws%3A%2F%2F127.0.0.1%3A6202&hosted_access=%7B%22deployment_mode%22%3A%22hosted_public_join%22%7D&test_api=1'`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-revoke-evidence 'window.__AW_TEST__.getState()'`
  - `env -u RUSTC_WRAPPER cargo run -q -p oasis7 --bin oasis7_pure_api_client -- --addr 127.0.0.1:6203 --client hosted-revoke-operator revoke-session --player-id hosted-player-0000019d2d3c4a63-00000003 --session-pubkey 5c9228b48496775b2a3410f742e4b20ff31ef9e6e9307bbdf5ab756d77cba4da --revoke-reason operator_kick_for_drill`
  - `sleep 35`
  - `source scripts/agent-browser-lib.sh && ab_eval hosted-revoke-evidence 'window.__AW_TEST__.getState()'`
  - `source scripts/agent-browser-lib.sh && ab_run hosted-revoke-evidence get text body`
  - `source scripts/agent-browser-lib.sh && ab_run hosted-revoke-evidence screenshot output/playwright/hosted-p2p-041/hosted-revoke-recovery.png`
- 遗留事项:
  - 当前 revoke 感知仍依赖固定 heartbeat 周期；若要进一步缩短玩家体感延迟，仍需补更主动的 push/presence 方案。
  - 本次证据只覆盖单玩家 remote revoke drill，尚未覆盖 operator 批量 kick / hosted handoff / incident correction 模板。

## 11:58:22 CST / liveops_community
- 完成内容:
  - 扩写 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.runbook.md`，正式补上 hosted operator 的 `session revoke` 实操步骤，明确何时需要 revoke、应收集哪些字段、推荐命令、预期回执，以及执行后如何在浏览器侧复核 `Hosted Recovery` / `authRevokeReason` / `authRevokedBy`。
  - 新增 `doc/testing/templates/hosted-world-share-correction-template.md`，统一错误 URL 更正、revoke 后重新获取 hosted player session 和 preview claims boundary 三类对外文案。
  - 回写专题 project 状态，收口 `TASK-P2P-041-F` 中“session revoke 实操步骤 / 对外更正模板仍待补齐”的过时表述。
- 验证:
  - `rg -n "revoke-session|Hosted Recovery|hosted-world-share-correction-template|preview" doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.runbook.md doc/testing/templates/hosted-world-share-correction-template.md doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md doc/p2p/project.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 远程 operator tunnel / reverse proxy 配置示例仍未落文。
  - hosted share announcement 模板和更完整的 hosted handoff / batch kick 沟通脚本仍待后续补齐。

## 12:03:37 CST / liveops_community
- 完成内容:
  - 继续扩写 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.runbook.md`，补齐远程 operator tunnel / reverse proxy 的最低暴露策略，明确公网只能暴露 `public join URL`、player WebSocket 与 `public player plane`，operator/control 面必须继续停留在 loopback、VPN 或人工 tunnel 内。
  - 新增 `doc/testing/templates/hosted-world-share-announcement-template.md`，统一 hosted world 首轮分享、重复提醒与 preview claims boundary 文案，并把“错误 URL 更正”继续留给现有 correction 模板处理。
  - 回写 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md` 与 `doc/p2p/project.md`，将 `TASK-P2P-041-F` 正式标记完成，清除“announcement/tunnel policy 仍待补齐”的过时 blocker。
- 验证:
  - `rg -n "hosted-world-share-announcement-template|reverse proxy|Tunnel / Reverse Proxy|TASK-P2P-041-F|分享 / 误分享 / 撤销 / 事故通报" doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.runbook.md doc/testing/templates/hosted-world-share-announcement-template.md doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md doc/p2p/project.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - `TASK-P2P-041-F` 已收口，但 `TASK-P2P-041-C/D/E` 仍需继续核对并补齐 product flow、strong-auth matrix 与 abuse evidence matrix。

## 12:18:52 CST / qa_engineer
- 完成内容:
  - 新增 `doc/testing/evidence/hosted-world-abuse-suite-matrix-2026-03-27.md`，把 replay、expiry、revocation、operator/public URL confusion、admission limit、capability bypass 六类 abuse 覆盖统一收敛成一份 topic 级证据矩阵。
  - 重跑本专题关键定向测试：`runtime_prompt_control_hosted_public_join*`、`runtime_agent_chat_requires_explicit_session_registration`、`runtime_session_register_*`、`runtime_authoritative_recovery_rotate_and_revoke_session_enforced_for_agent_chat`、`hosted_player_session_*`、`hosted_prompt_control_strong_auth_grant_*`、`hosted_strong_auth_*`、`hosted_mode_rejects_remote_private_control_plane_matrix`，确认当前 failure signature 与 browser/live evidence 一致。
  - 回写 `doc/p2p/blockchain/p2p-hosted-world-player-access-and-session-auth-2026-03-25.project.md` 与 `doc/p2p/project.md`，将 `TASK-P2P-041-B/C/D/E` 一并按当前 PRD 完成定义标记为完成，并把剩余内容降级为后续增强议题而非 blocker。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_prompt_control_hosted_public_join -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_agent_chat_requires_explicit_session_registration -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_session_register_ -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_authoritative_recovery_rotate_and_revoke_session_enforced_for_agent_chat -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_ -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_prompt_control_strong_auth_grant_ -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_strong_auth_ -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher hosted_mode_rejects_remote_private_control_plane_matrix -- --nocapture`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 本专题已完成；后续若要继续推进 production custody / wallet plugin / hosted handoff，应新开子专题，不再复用 `TASK-P2P-041`。

## 12:06:23 CST / producer_system_designer
- 完成内容:
  - 对 `doc/p2p/project.md` 做最后一轮 hosted-world 专题收口审计，删除 `TASK-P2P-041` 在模块总表中的过期 `first slice / 最新进展` 长列表，避免同一页同时保留阶段性结论和最终完成结论。
  - 将模块总表中的 hosted-world 摘要压缩为单一真值，只保留 `TASK-P2P-041-F`、`TASK-P2P-041-B/C/D/E` 与 `TASK-P2P-041` 三条最终完成记录，明确本专题已经闭环，剩余项全部转入新专题。
- 验证:
  - `rg -n "TASK-P2P-041-B|TASK-P2P-041-C|TASK-P2P-041-D|TASK-P2P-041-E|TASK-P2P-041-F|first slice|最新进展" doc/p2p/project.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - `TASK-P2P-041` 相关文档链路现已一致；后续若继续做 hosted-world 强化，需新开子专题。
## 10:23:49 CST / liveops_community
- 完成内容:
  - 按用户要求继续重试昨天未完成的 Moltbook 外部游戏讨论评论，优先回查 `AI Council - Diplomacy Game for AI Agents`、`Why Games Are Better Testbeds Than Production` 与昨天已成功评论过的 `CrustLand: why recovery arcs matter`。
  - 先走本地 `7897` proxy 回查帖子与评论状态，出现 `curl: (35) OpenSSL SSL_connect: SSL_ERROR_SYSCALL in connection to www.moltbook.com:443`；改用直连 API 再探测，同样在 `15s` 窗口内返回 `curl: (28) Connection timed out after 15001 milliseconds`。
  - 结合昨天已记录的 `AI Council` 连续两次 `500 Internal server error` 和 `MoltMob` 评论超时，这次确认当前阻塞点是 Moltbook 连接面/平台稳定性，而不是选帖或文案问题，因此本轮停止继续补发，避免重复评论和无意义重试。
- 验证:
  - `GET /api/v1/posts/d566995d-78cf-4af3-9ef4-21ddc5c9a366` via proxy
  - `GET /api/v1/posts/d566995d-78cf-4af3-9ef4-21ddc5c9a366/comments?sort=new&limit=10` via proxy
  - `GET /api/v1/posts/1c0c9a7a-de63-4870-a3be-596e17170dd8` via proxy
  - `GET /api/v1/posts/1c0c9a7a-de63-4870-a3be-596e17170dd8/comments?sort=new&limit=10` via proxy
  - `GET /api/v1/posts/d79f7706-1482-4649-a67f-e91c50b78897` via proxy
  - `GET /api/v1/posts/d79f7706-1482-4649-a67f-e91c50b78897/comments?sort=new&limit=10` via proxy
  - direct `GET /api/v1/posts/1c0c9a7a-de63-4870-a3be-596e17170dd8`
  - direct `GET /api/v1/posts/d566995d-78cf-4af3-9ef4-21ddc5c9a366`
- 遗留事项:
  - 等 Moltbook 连接恢复后，优先重试 `AI Council` 那条关于 `permanent reputation / asymmetric victory conditions / costly trust` 的纯玩法评论。
  - `MoltMob` 那条后续重试前先回查楼层，确认昨天那次 timeout 没有慢写入成幽灵评论，再决定是否补发。

## 13:11:35 CST / qa_engineer
- 完成内容:
  - 落地 `TASK-SCRIPTS-014` 的 worktree-isolated harness 实装闭环：新增 `scripts/worktree-harness.sh` / `scripts/worktree-harness-lib.sh`，并把 `run-game-test.sh`、`run-producer-playtest.sh` 接到 worktree-local `output/harness/<worktree-id>/` 目录、端口与元数据契约上。
  - 修复 source-mode 真正阻断项：`run-game-test.sh` 现会先显式构建 `oasis7_game_launcher`、`oasis7_viewer_live`、`oasis7_chain_runtime`，再以绝对路径启动 launcher，并注入 `OASIS7_VIEWER_LIVE_BIN` / `OASIS7_CHAIN_RUNTIME_BIN`，不再依赖 PATH 或偶然已有的 sibling binary。
  - 补强 harness 失败语义：`run-game-test.sh` 启动阶段若 launcher 提前退出会立即失败，不再空等 HTTP/web-bridge timeout；`worktree-harness.sh` 则增加真实存活刷新，避免陈旧 `state.json` 继续冒充 `ready`。
  - 实测 `./scripts/worktree-harness.sh up --no-llm --smoke-timeout 30` 在单次闭环内通过，`agent-browser` 产物落在 `output/harness/wt-781b155c/artifacts/smoke-20260327-131008/`，其中 `state.json` 显示 `connectionStatus=connected`、`controlProfile=live`、`rendererClass=hardware`、`renderer=ANGLE (Intel, Mesa Intel(R) Iris(R) Xe Graphics (RPL-P), OpenGL 4.6)`。
  - 同步修掉本轮验证前暴露出的 viewer wasm 编译阻塞，补齐 `crates/oasis7_viewer/src/egui_right_panel_chat.rs`、`crates/oasis7_viewer/src/egui_right_panel_chat_auth.rs`、`crates/oasis7_viewer/src/viewer_automation.rs`、`crates/oasis7_viewer/src/selection_linking.rs` 与当前协议字段/事件枚举的缺项，使 `env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown` 重新通过。
- 验证:
  - `bash -n scripts/worktree-harness.sh scripts/worktree-harness-lib.sh scripts/run-game-test.sh scripts/run-producer-playtest.sh`
  - `./scripts/worktree-harness.sh --help`
  - `./scripts/run-game-test.sh --help`
  - `./scripts/run-producer-playtest.sh --help`
  - `./scripts/worktree-harness.sh up --no-llm`
  - `./scripts/worktree-harness.sh url`
  - `./scripts/worktree-harness.sh logs`
  - `./scripts/worktree-harness.sh up --no-llm --smoke-timeout 30`
  - `./scripts/worktree-harness.sh status --json`
  - `./scripts/worktree-harness.sh down`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
  - `env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown`
- 遗留事项:
  - 当前 Codex 执行环境会在命令返回后回收后台子进程，因此跨命令 `up -> smoke/status` 会被工具环境打断；仓库脚本已补 `nohup` 与状态刷新，但“命令返回后后台栈继续存活”的最终行为仍应在真实开发者 shell 中再做一次人工确认。
  - `status --json` 在本环境里会如实把被回收后的栈刷新成 `stopped`；这不是 harness 协议回归，而是执行器生命周期限制。

## 13:23:34 CST / producer_system_designer
- 完成内容:
  - 按当前团队协作策略更新根 `AGENTS.md`，把“每个新需求默认新开独立 `git worktree`”正式写成开发工作流规则。
  - 明确一需求一 `worktree`、代码/文档/测试/`devlog`/验证产物在对应 `worktree` 内闭环、当前 `worktree` 已绑定未完成任务时必须新开，以及只有用户明确要求时才允许复用现有 `worktree`。
  - 补充本地 Web/launcher/`agent-browser`/smoke 闭环默认绑定需求专属 `worktree` 与隔离 harness，避免后续多任务并行时再次退回共享工作目录。
- 验证:
  - `sed -n '1,120p' AGENTS.md`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
- 遗留事项:
  - 当前只是把规则写进 `AGENTS.md`；若后续要进一步降低执行成本，可再补一个标准化的新建 `worktree` 脚本或命名模板。
