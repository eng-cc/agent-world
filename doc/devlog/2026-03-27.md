# 2026-03-27

## 09:26:45 CST / viewer_engineer
- 完成内容:
  - 把 `crates/oasis7/src/viewer/web_bridge.rs` 的 `ViewerWebBridge::run()` 从串行 `accept -> serve_stream` 改成每个 websocket 一条独立桥接线程，补掉 browser/web-bridge 层最后一个单连接瓶颈。
  - 新增定向回归 `bridge_run_accepts_second_websocket_while_first_stays_open`，要求首个 websocket 保持打开时第二个 websocket 仍能透过 bridge 连到 upstream。
  - 用真实本地 hosted `oasis7_game_launcher --deployment-mode hosted_public_join --chain-disable` 栈复验双浏览器接入：两个独立 `agent-browser` session 最终都能收敛到 `debug_viewer:subscribed` 并看到同一份 live snapshot，不再永久停在第二页 detached。
- 验证:
  - `cargo fmt --all`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib bridge_run_accepts_second_websocket_while_first_stays_open -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_live_run_accepts_probe_while_viewer_session_is_open -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `./scripts/doc-governance-check.sh`
  - `git diff --check`
  - `agent-browser --session hosted-concurrent-a open 'http://127.0.0.1:6101/?ws=ws://127.0.0.1:6102'`
  - `agent-browser --session hosted-concurrent-b open 'http://127.0.0.1:6101/?ws=ws://127.0.0.1:6102'`
  - `agent-browser --session hosted-concurrent-a get text body`
  - `agent-browser --session hosted-concurrent-b get text body`
- 遗留事项:
  - 第二个页面首轮 attach 仍可能短暂经历一次自动重连窗口后才进入 `subscribed`；当前已确认不是永久卡死，也不再阻断双页并发观察。
  - hosted player lane 的下一真实 blocker 仍是带 backend grant 的 `prompt_control_*` success evidence，而不是 browser/web-bridge 并发接入。
