# 2026-03-26

## 22:10:00 CST / runtime_engineer
- 完成内容:
  - 为 `oasis7_web_launcher` 增加 `deployment_mode`，在 `hosted_public_join` 下把 `/api/state`、`/api/start|stop`、`/api/chain/start|stop`、`/api/gui-agent/*` 与 console static 收口为 loopback-only private control plane。
  - 新增 `/api/public/state` 与 hosted player access contract snapshot，冻结 `deployment_mode`、hosted verdict、`gui-agent` legacy surface 与 admission contract 默认值。
  - 为 `oasis7_game_launcher` 增加 `deployment_mode`，在 `hosted_public_join` 下停止向公开 viewer HTML 注入长期 signer bootstrap。
  - 顺手按工程约束拆分 `oasis7_web_launcher/server.rs`、`oasis7_game_launcher/static_http.rs`、`oasis7_game_launcher/runtime_paths.rs`，把超长 Rust 文件压回门禁线内。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher --bin oasis7_game_launcher`
- 遗留事项:
  - `TASK-P2P-041-B`: viewer 仍未落 `guest session -> player session` UX 与按钮 capability gate。
  - `TASK-P2P-041-C`: runtime 仍未实现 session validation、entity bind/resume/revoke。
  - `TASK-P2P-041-F`: hosted operator 目前只有 loopback-only private control plane，远程 operator tunnel/runbook 仍待补齐。

## 23:58:00 CST / viewer_engineer
- 完成内容:
  - 为 `crates/oasis7_viewer/software_safe.js` 增加 hosted-world session ladder 状态模型，显式显示 `guest_session / player_session / strong_auth`、`authSource`、`deploymentHint` 与 reconnect 提示，不再只停留在 `auth=ready|missing` 的二元口径。
  - prompt/chat 现在按 capability 给出结构化禁用原因；至少能区分 `guest_session`、`observer_only` 与 `strong_auth_required` 占位，并让本地 send 前 guard 与 UI 文案保持一致。
  - `__AW_TEST__.getState()` 已补 `authTier`、`authSource`、`authDeploymentHint` 与 `authSurface`，供后续 agent-browser/QA 采样 hosted public join 的 session/capability 状态。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
- 遗留事项:
  - `TASK-P2P-041-B` 仍未完成真实 join/login/reconnect；当前只是把 viewer 侧 session/capability 梯度显式化。
  - `TASK-P2P-041-C` 仍需把 `guest session -> player session` 接到真正的 session issue、entity bind/resume/revoke 与 ownership 冲突规则。

## 02:46:00 CST / runtime_engineer
- 完成内容:
  - 为 runtime-live 增加显式 `session_register` 路径，并把 prompt/chat/gameplay 从“首个签名动作隐式注册 session”改成“先 register，再 validate_known_session_key 放行”的严格模式。
  - `RuntimeSessionPolicy::validate_known_session_key` 现会在未注册 session 时返回 `session_not_found`；同时补了 `player_id -> agent_id` 反向绑定与 one-player-one-agent 规则，避免同一 player 静默切占第二个 agent。
  - `ReconnectSync` / `SessionRegistered` / `SessionRotated` ack 已补 `agent_id`；`RevokeSession` 会清掉该 player 的 binding 与 nonce/chat replay 痕迹。Viewer web/native sender 也已在 prompt/chat 前插入 `session_register`。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer sync_viewer_auth_nonce_from_state_tracks_persisted_nonce -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_agent_chat_requires_explicit_session_registration -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_authoritative_recovery_rotate_and_revoke_session_enforced_for_agent_chat -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_prompt_control_apply_updates_snapshot_and_bindings -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_gameplay_action_can_reach_first_capability_milestone_without_ui -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_session_register_rejects_same_player_binding_to_second_agent -- --nocapture`
- 遗留事项:
  - `TASK-P2P-041-C` 仍未完成完整 hosted guest/player issuer；当前 `session_register` 仍属于 runtime-live 内显式注册，而不是正式 public session issuance API。
  - `TASK-P2P-041-D` 仍未把 `main token transfer`、敏感 prompt/control 等动作接入 `strong_auth` 分级。

## 13:28:34 CST / runtime_engineer
- 完成内容:
  - 为 `oasis7_web_launcher` 增加 hosted-world `main token transfer` barrier：在 `deployment_mode=hosted_public_join` 下，`POST /api/chain/transfer` 现在会直接返回结构化 `strong_auth_required`，不再让 public join 路径继续借用 trusted-local signer bootstrap。
  - 把 `deployment_mode` 从 `oasis7_game_launcher` 继续透传到 `oasis7_viewer_live` / runtime-live，并让 `prompt_control preview/apply/rollback` 在 hosted public join 下统一返回 `strong_auth_required`，避免敏感 prompt/control 继续走 `player_session` 直达。
  - `oasis7_client_launcher` 现已把 `deployment_mode` 写入启动参数，并在转账窗口对 `hosted_public_join` 直接显示 strong-auth barrier，不再继续尝试 trusted-local signer bootstrap 提交。
  - 收口 `software_safe.js` 的敏感动作口径：`prompt_control` 在 hosted public join 推断路径与 remote-origin legacy bootstrap 下都会明确显示为 `strong_auth_required`，避免把 preview bootstrap 误当成 hosted-ready 能力。
  - 同步回写 `TASK-P2P-041-D` 项目追踪，保持“只完成 first barrier slice、未完成正式 strong-auth lane”的真实状态。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher submit_chain_transfer_requires_strong_auth_for_hosted_public_join -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_prompt_control_hosted_public_join_requires_strong_auth -- --nocapture`
- 遗留事项:
  - hosted `strong_auth` 仍未落 challenge/proof/verification lane；`main token transfer` 目前是显式拒绝，不是 hosted-ready 放行。
  - 当前还没有真正的 hosted `strong_auth` proof issuer；`main token transfer` 与 `prompt_control` 只是 barrier/disable，不是强鉴权成功后的可用链路。

## 13:38:52 CST / runtime_engineer
- 完成内容:
  - 为 `/api/public/state.hosted_access` 增加结构化 `action_matrix`，把 `gameplay_action`、`agent_chat`、`prompt_control_preview/apply/rollback`、`main_token_transfer` 的 `required_auth + availability + reason` 冻结成可读 contract。
  - `hosted_public_join` 口径下，public snapshot 现在会明确导出 `main_token_transfer` / `prompt_control_*` 为 `strong_auth + blocked_until_strong_auth`，并保留 `agent_chat` / `gameplay_action` 为 `player_session + public_player_plane`。
  - 同步回写 hosted-world 项目追踪与模块 `doc/p2p/project.md`，让 QA/viewer/liveops 可以直接消费 snapshot 真值，不再继续靠文档与 hostname 推断 action matrix。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher public_snapshot_exposes_hosted_access_contract -- --nocapture`
- 遗留事项:
  - `action_matrix` 目前仍是静态 contract；后续真正的 hosted strong-auth issuer 落地后，还需要把它和运行时 challenge/proof 状态联动，而不是只停留在 barrier 文案。

## 14:20:19 CST / viewer_engineer
- 完成内容:
  - `oasis7_web_launcher` 生成的 game URL 现会附带精简 `hosted_access` query hint，包含 `deployment_mode / verdict / browser_signer_bootstrap / session_ladder / action_matrix`，不再要求 software_safe 通过跨域 fetch 才能拿到 hosted 真值。
  - `software_safe.js` 现会优先消费这份 `hosted_access` hint 来判断 `deploymentHint`、`prompt_control` / `agent_chat` / `strong_auth_actions` capability，不再只靠 hostname 推断 hosted/public 路径。
  - `__AW_TEST__.getState()` 已补 `hostedAccess`，方便 QA/agent-browser 直接采样当前 viewer 看到的 hosted contract 真值。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher build_game_url_uses_request_host_for_wildcard_bindings -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher public_snapshot_exposes_hosted_access_contract -- --nocapture`
- 遗留事项:
  - 当前 query hint 仍是静态 snapshot，不会随着后续 hosted session/strong-auth issuer 的真实状态实时刷新；等 issuer 落地后，viewer 还需要接入更动态的状态源。

## 14:27:31 CST / viewer_engineer
- 完成内容:
  - 把 `hosted_access` query hint 从 `oasis7_web_launcher` 扩展到全部 game URL 生产者：`oasis7_game_launcher` 和 `oasis7_client_launcher` 本地生成的 viewer URL 现在也会附带同口径 hosted contract。
  - 这样无论是直接起 `game_launcher`、经 `web_launcher` 分享 URL，还是从 `client_launcher` 打开网页，`software_safe.js` 都会收到同一套 hosted access 真值，不再出现“入口不同、前端能力判定不同”的漂移。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher build_game_url_rewrites_zero_bind_host_to_loopback -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher build_game_url_brackets_ipv6_hosts -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher build_game_url_rewrites_zero_host -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher build_game_url_brackets_ipv6_hosts -- --nocapture`
- 遗留事项:
  - 当前各入口携带的是静态 hosted hint；真正的 session issue / resume / strong-auth issuer 落地后，还需要决定哪些字段继续放 URL、哪些改走更动态的 state source。

## 15:01:58 CST / runtime_engineer
- 完成内容:
  - 为 `oasis7_game_launcher` 的 viewer 同源 public player plane 新增 `GET /api/public/player-session/issue`，返回 server-issued `player_id`、`browser_local_ephemeral_ed25519` 鉴权模式与结构化 admission/rate-limit 记账。
  - 新增 `hosted_player_session` issuer 状态，先把 hosted v1 admission enforcement 收敛到公开 issue 面，避免继续只剩“文档里说有 player session”而没有真正的签发入口。
  - 同步把 hosted access contract 的 `public_endpoints` 补上 `/api/public/player-session/issue`，让 public snapshot / viewer hint / runbook 口径一致。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher public_snapshot_exposes_hosted_access_contract -- --nocapture`
- 遗留事项:
  - 当前 admission enforcement 只落了 `issue_rate_limit` 记账；`max_player_sessions/world_full/kick_policy` 还没接到 runtime occupancy 与 operator kick 流程。

## 15:01:58 CST / viewer_engineer
- 完成内容:
  - `software_safe.js` 现在会在 `hosted_public_join` 下优先尝试同源 `/api/public/player-session/issue`，拿到 server-issued `player_id` 后在浏览器本地生成/持久化临时 Ed25519 key，不再依赖 legacy viewer signer bootstrap。
  - viewer 已接入 `authoritative_recovery register_session/reconnect_sync` ack/error：首次打开会自动从 guest 升到 player session，刷新/断线后优先走 `reconnect_sync`，若 runtime 返回 `session_not_found` 则回退到显式重新注册。
  - `__AW_TEST__.getState()` 现补 `authRegistrationStatus`、`authSessionEpoch`、`authRecoveryErrorCode` 与 `authRecoveryErrorMessage`，方便 QA/agent-browser 直接采样 hosted player session 的实际生命周期。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
- 遗留事项:
  - 当前 viewer 还没有显式“更换身份/撤销后重登/重绑 agent”UI，revoke、world full 与 operator kick 仍需配后续专题继续补齐。

## 15:18:42 CST / runtime_engineer
- 完成内容:
  - 把 hosted player-session issuer 从“只记 rate-limit”扩到真实 active-slot 管理：`max_player_sessions` 现在会在 `issue` 路径直接返回 `world_full`，不再只是静态 admission 字段。
  - 为 public player plane 新增 `POST /api/public/player-session/release`，通过 `release_token` 归还当前 active slot；issuer 现会同时统计 `active_player_sessions / released_players_total`。
  - 同步把 `/api/public/player-session/release` 回写到 hosted access contract 的 public endpoints，保证 snapshot/runbook/QA 口径一致。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
- 遗留事项:
  - 目前的 `world_full` 仍只基于 issuer active slot，不是 runtime 世界真实 occupancy；operator kick / remote revoke 也还没把 slot 回流接到同一真值上。

## 15:18:42 CST / viewer_engineer
- 完成内容:
  - `software_safe.js` 新增显式 `Release Hosted Player Session` 动作：会向 runtime 发 `revoke_session`，再向同源 `/api/public/player-session/release` 归还 slot，并清掉浏览器本地持久化的 hosted player session。
  - viewer 现在会在收到 `session_revoked` 或关键 recovery 错误后尝试自动释放本地 slot，再按 hosted v1 规则决定是否重新 issue / register。
  - `__AW_TEST__` 已补 `logoutHostedPlayerSession()`，方便后续 agent-browser/QA 直接验证 slot release 与 guest 回落闭环。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
- 遗留事项:
  - 当前 release 只覆盖“本浏览器主动离开/本页检测到 session 失效”的路径；如果是 operator 在其他控制面踢人，还需要把远端回流也接到同一 slot 真值。

## 16:11:47 CST / runtime_engineer
- 完成内容:
  - 为 hosted public player plane 新增 `GET /api/public/player-session/admission`，允许 viewer/QA 在不实际 issue 的情况下直接读取 `active_player_sessions/max_player_sessions/remaining_issue_budget`。
  - issuer 现支持独立输出 admission snapshot；配套单测覆盖“已 issue 后 admission 正确反映 active slot”。
  - 同步把 `/api/public/player-session/admission` 纳入 hosted access public endpoints，保证 public contract、实际路由和测试口径一致。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`

## 21:19:17 CST / runtime_engineer
- 完成内容:
  - 为 hosted public player plane 新增 `/api/public/strong-auth/grant/prompt-control`，要求有效 `player_id + release_token` 会话、正确 `approval_code`，再由服务端环境变量中的 signer 签发短期 `HostedStrongAuthGrant`。
  - runtime-live 现已在 `hosted_public_join` 下同时校验 `player_session` proof 与 backend-signed grant：`prompt_control preview/apply/rollback` 缺 grant 时返回 `strong_auth_required`，grant 过期/错 signer/错绑定时返回 `strong_auth_grant_invalid`。
  - 为 `viewer::auth`、runtime hosted prompt-control、`oasis7_game_launcher::hosted_strong_auth` 与 hosted access action matrix 补了正反向单测，确保 preview-grade backend reauth 不只是手工打通。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib runtime_prompt_control_hosted_public_join -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --lib hosted_prompt_control_strong_auth_grant -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_prompt_control_strong_auth_grant -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_public_join_prompt_control -- --nocapture`
- 遗留事项:
  - 这条 hosted `prompt_control` 二次授权仍是 preview-grade：backend signer 目前只支持 env 托管 + `approval_code`，不是 production signer custody。
  - `main_token_transfer` 仍保持 `blocked_until_strong_auth`，尚未接入 hosted-ready 的资产强鉴权链路。

## 21:19:17 CST / viewer_engineer
- 完成内容:
  - `software_safe.js` 现已在 hosted public join 的 `prompt_control` lane 显示 `Backend Approval Code`，并在发起 prompt 请求前先向同源 strong-auth route 申请短期 backend grant。
  - `__AW_TEST__.getState()` 已补 `strongAuthApprovalCodeConfigured`、`strongAuthLastGrantActionId`、`strongAuthLastGrantExpiresAtUnixMs` 与 `strongAuthLastGrantError`，便于 QA/agent-browser 直接采样 grant 生命周期与失败签名。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher public_snapshot_exposes_hosted_access_contract -- --nocapture`
- 遗留事项:
  - 当前 viewer 只支持输入后端审批码并获取短期 grant；若后续切到钱包、OAuth 或生产级 signer custody，还需要再抽象成更正式的 hosted strong-auth UX。
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher public_snapshot_exposes_hosted_access_contract -- --nocapture`
- 遗留事项:
  - 当前 admission 真值仍是 issuer 侧 slot 视角，不是 runtime 真实 occupancy；如果 operator 在别的控制面 kick/revoke，仍需要把这份真值和 runtime 绑定状态打通。

## 16:11:47 CST / viewer_engineer
- 完成内容:
  - `software_safe.js` 现会主动拉取 `/api/public/player-session/admission`，并把 `hostedAdmission` 暴露给 `__AW_TEST__.getState()`，便于 QA 直接看到 `activeSlots/issueBudget`。
  - guest lane 新增显式 `Acquire Hosted Player Session` 动作；如果此前因为 `world_full/rate_limited` 没拿到 player session，现在不需要再靠发送 chat/prompt 来侧向触发重试。
  - 继续保留 page reload 的 reconnect 语义，没有把浏览器关闭/刷新强行等价成自动 release，以免把有效 resume 路径误杀。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
- 遗留事项:
  - 当前 admission 面只有“读”和“重试”；operator kick / remote revoke 仍缺少主动回流通知，guest lane 也还没有“等待队列/世界满员说明”这种更完整 UX。

## 16:28:14 CST / runtime_engineer
- 完成内容:
  - 为 public player plane 新增 `POST /api/public/player-session/refresh`，active slot 现带固定 `slot_lease_ttl_ms`；issuer 会在 `admission/issue/refresh/release` 前清理过期 lease，避免 stale slot 无限占位。
  - hosted admission snapshot 现会显式回出 `slot_lease_ttl_ms`，便于 viewer/QA 判断 lease 模型是否在工作。
  - 补充单测覆盖 refresh 成功续租，确认 hosted v1 的 active slot 已不是“只发不续、不续不回收”的一次性状态。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher public_snapshot_exposes_hosted_access_contract -- --nocapture`
- 遗留事项:
  - 当前 lease refresh 仍是 viewer 主动续租，不是 runtime/host authoritative presence；operator 在其他控制面 kick/revoke 时，仍需要把远端真值主动推回 issuer。

## 16:28:14 CST / viewer_engineer
- 完成内容:
  - `software_safe.js` 现在会在 hosted player session 成功注册后自动调用 `/api/public/player-session/refresh` 并启动 heartbeat，保持 slot lease 存活；掉线或回到 guest 时则停止 heartbeat。
  - summary 现会展示 `leaseTTL`，让 QA 能直接看到 hosted slot lease 是否被前端感知。
  - 续租失败时不会粗暴自动 logout；仍优先保留 reconnect 语义，把问题暴露给状态面和后续 recovery，而不是把刷新/短断线误杀成离场。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
- 遗留事项:
  - 当前 heartbeat 还是浏览器定时器，不是更强的服务端 presence；如果后续要更接近主流 hosted session，需要把 lease 与 runtime occupancy / operator kick 合并成单一 authoritative source。

## 16:49:47 CST / viewer_engineer
- 完成内容:
  - 把 hosted heartbeat 从“只续租 issuer slot”扩成“双探针”：定时续租 `/api/public/player-session/refresh` 的同时，也会周期性发送 `reconnect_sync`，让空闲玩家会话也能发现 runtime 侧的 `session_revoked/session_not_found`。
  - 这样即使玩家长时间不发 chat/prompt，也不需要等下一次主动交互才知道自己已经被远端 revoke/kick 到不可用状态。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
- 遗留事项:
  - 当前仍是“浏览器轮询发现问题”，不是服务端主动推送；如果要做到更强的 operator kick 体验，最终还是要把 issuer/runtime/operator 面并成一份 authoritative presence/source of truth。

## 18:04:57 CST / runtime_engineer
- 完成内容:
  - 把 `HostedPlayerSessionIssuer::release/refresh` 这轮硬化真正收口到 `player_id + release_token` 双绑定，并补上 `player_id_required/player_id_mismatch` 单测，避免 token-only 误归还或误续租其他玩家 slot。
  - 修复 `software_safe.js` 的 hosted reconnect 状态机：若 `register_session/reconnect_sync` 飞行中遇到 WebSocket 断开，现在会主动清掉挂起的 `syncInFlight` 并标成 `runtime=disconnected`，避免短断线后卡死不再自动恢复。
  - viewer summary 与 `__AW_TEST__.getState()` 现已显式暴露 `authRuntimeStatus/authBoundAgentId` 和 recovery error，方便 QA 直接看到 hosted player session 当前是否仍绑在 runtime agent 上。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher public_snapshot_exposes_hosted_access_contract -- --nocapture`
- 遗留事项:
  - `world_full/kick_policy` 仍是 issuer slot 视角，不是 runtime occupancy / operator truth。
  - 当前远端 revoke 仍主要靠浏览器周期性探针发现，不是服务端主动推送或统一 authoritative presence。

## 19:50:31 CST / runtime_engineer
- 完成内容:
  - `oasis7_game_launcher` 的 public player plane 现在会在 `admission/issue/refresh/release` 前主动通过 `live_bind` 拉取 runtime live snapshot，读取 `agent_player_bindings` 真值。
  - `HostedPlayerSessionIssuer` 新增 runtime presence reconcile：凡是“曾经已在 runtime binding 中出现过、现在又从 runtime snapshot 消失”的 player slot，都会被 issuer 立即回收，而不是继续等 lease TTL 自然过期。
  - 旧 browser session 在这种 reconcile 后再次 refresh/release 会收到结构化 `session_revoked`；同时补了 runtime probe mock 测试和 runtime reconcile 单测，确保这条回流链路可回归。
  - admission snapshot 现会额外回出 `runtime_bound_player_sessions`，viewer summary 会并排显示 `activeSlots` 与 `runtimeBound`，方便直接观察 issuer slot 和 runtime 真实绑定是否漂移。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher query_runtime_bound_players_reads_snapshot_bindings -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_runtime_reconcile_releases_seen_players_missing_from_runtime -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher public_snapshot_exposes_hosted_access_contract -- --nocapture`
- 遗留事项:
  - 当前 reconcile 还是“HTTP public route 触发时 probe 一次 runtime snapshot”，不是服务端 push，也还不是完整 authoritative presence bus。
  - `world_full` 仍未完全等价于 runtime 真正占用，因为“刚 issue 尚未 register”的 pending slot 以及更复杂的 operator kick policy 仍在 issuer 侧建模。

## 20:09:34 CST / runtime_engineer
- 完成内容:
  - 把 runtime reconcile 从“HTTP public route 命中时临时 probe”推进到 `oasis7_game_launcher` 独立 runtime presence monitor 线程里的后台周期性 probe，public player plane 就算一段时间没有 admission/refresh 请求，也会持续对账 `live_bind` runtime snapshot。
  - `HostedPlayerSessionIssuer` 现会记录 `runtime_probe_status/runtime_probe_error/last_runtime_probe_unix_ms`，admission snapshot 与 viewer summary 也会直接暴露这组状态，方便区分“runtime 对账失败”还是“确实没有绑定玩家”。
  - 补充单测覆盖 runtime probe failure 在 admission 中的可观测性，避免 probe 失败时静默退化成看似正常的 0 occupancy。
- 验证:
  - `node --check crates/oasis7_viewer/software_safe.js`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_runtime_probe_failure_surfaces_in_admission -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher hosted_player_session_admission_reports_current_snapshot -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
- 遗留事项:
  - 当前后台 reconcile 仍是轮询，不是 runtime 主动推送事件或统一 presence bus。
  - `world_full` 仍未完全收敛为 authoritative runtime occupancy，因为 pending issue slot / register 前状态和 operator policy 还没统一到一份 source of truth。

## 23:24:00 CST / runtime_engineer
- 完成内容:
  - 把 `oasis7_game_launcher` 的 runtime presence monitor 从“后台周期性短连 probe”升级成独立模块内的常驻 TCP client，运行时维持到 `live_bind` 的单连接，而不是每次都重新握手。
  - 新 monitor 现在会先订阅 `Events + Snapshot` 做首轮同步，随后切回 `Events` 监听 `AgentPlayerBound` 增量更新，再周期性切回 `RequestSnapshot` 做全量纠偏；这样 public route 不再自己临时拉 runtime snapshot。
  - 顺手把相关逻辑拆到 `oasis7_game_launcher/runtime_presence.rs`，继续把 `oasis7_game_launcher.rs` 的超长职责往子模块剥离，并补上 event+snapshot correction 单测。
- 验证:
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher query_runtime_bound_players_reads_snapshot_bindings -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher runtime_presence_monitor_client_merges_event_and_snapshot_correction -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_web_launcher public_snapshot_exposes_hosted_access_contract -- --nocapture`
- 遗留事项:
  - runtime 目前只有 `AgentPlayerBound` 通用事件，没有面向所有客户端的 unbind/revoke 广播，所以仍需要周期性 snapshot 纠偏，不能只靠事件流。
  - `world_full` / kick policy 仍未完全统一成单一 authoritative occupancy；pending issue slot、register 前状态与更细的 operator policy 还要继续收口。
