- 时刻：2026-02-28 11:44:48 CST
- 完成内容：完成“M4 资源产业链可玩性优先强化”T0（建档）。
  - 新增设计文档：`doc/world-simulator/m4/m4-resource-product-system-playability-priority-hardening-2026-02-28.md`。
  - 新增项目管理文档：`doc/world-simulator/m4/m4-resource-product-system-playability-priority-hardening-2026-02-28.project.md`。
  - 项目状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/world-simulator/m4/m4-resource-product-system-playability-priority-hardening-2026-02-28.md doc/world-simulator/m4/m4-resource-product-system-playability-priority-hardening-2026-02-28.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/world-simulator/m4/m4-resource-product-system-playability-priority-hardening-2026-02-28.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/world-simulator/m4/m4-resource-product-system-playability-priority-hardening-2026-02-28.project.md`。
- 遗留事项：
  - 执行 T1：补齐 `polymer_resin` 内置配方链路与 bootstrap/artifact 同步。

- 时刻：2026-02-28 11:55:56 CST
- 完成内容：完成“M4 资源产业链可玩性优先强化”T1（`polymer_resin` 配方链路 + bootstrap/artifact 同步）。
  - 新增内置模块 crate：`crates/oasis7_builtin_wasm_modules/m4_recipe_smelter_polymer_resin`（`Cargo.toml`/`Cargo.lock`/`src/lib.rs`）。
  - 更新 `crates/oasis7_wasm_store/src/lib.rs`、`crates/oasis7/src/runtime/mod.rs`：新增并导出 `M4_RECIPE_SMELT_POLYMER_RESIN_MODULE_ID`。
  - 更新 `crates/oasis7/src/runtime/world/bootstrap_economy.rs`：
    - bootstrap 模块清单新增 `m4.recipe.smelter.polymer_resin`；
    - 默认 `RecipeProfile` 新增 `recipe.smelter.polymer_resin`（`bootstrap` 阶段）。
  - 更新清单：
    - `crates/oasis7/src/runtime/world/artifacts/m4_builtin_module_ids.txt`
    - `crates/oasis7/src/runtime/world/artifacts/builtin_module_manifest_map.txt`
    - `crates/oasis7/src/runtime/world/artifacts/m4_builtin_modules.sha256`
    - `crates/oasis7/src/runtime/world/artifacts/m4_builtin_modules.identity.json`
  - 更新 `crates/oasis7/src/runtime/tests/economy_bootstrap.rs`：
    - 模块常量期望列表纳入新 recipe 模块；
    - 端到端链路测试改为先产出 `polymer_resin`，移除预置 `polymer_resin` 依赖。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo fmt --all -- --check`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 runtime::tests::builtin_wasm_identity -- --nocapture`。
  - test_tier_full：`env -u RUSTC_WRAPPER cargo test -p oasis7 --features "wasmtime test_tier_full" runtime::tests::economy_bootstrap -- --nocapture`。
  - test_tier_full：`./scripts/sync-m4-builtin-wasm-artifacts.sh --check`。
- 遗留事项：
  - 执行 T2：落地 `unlock_stage` 校验与 `TransferMaterial` 显式优先级。

- 时刻：2026-02-28 12:03:36 CST
- 完成内容：完成“M4 资源产业链可玩性优先强化”T2（`unlock_stage` 校验 + `TransferMaterial` 显式优先级）。
  - 更新 `crates/oasis7/src/runtime/events.rs`：`Action::TransferMaterial` 新增可选字段 `priority: Option<MaterialTransitPriority>`（`serde(default)`，兼容旧输入）。
  - 更新 `crates/oasis7/src/runtime/world/event_processing/action_to_event_core.rs`：物流优先级解析链路改为“显式输入 > material profile 默认优先级 > 关键词推断”。
  - 更新 `crates/oasis7/src/runtime/world/event_processing/action_to_event_economy.rs`：`ScheduleRecipe` 新增 `ProductProfile.unlock_stage` 校验，阶段不足时拒绝排产。
  - 更新 `crates/oasis7/src/runtime/tests/economy_priority_logistics.rs`：
    - 新增 `due_transits_allow_explicit_priority_override_for_non_urgent_material`；
    - 新增 `schedule_recipe_rejects_when_product_unlock_stage_exceeds_current_stage`。
  - 更新 `crates/oasis7/src/runtime/tests/economy.rs` 与既有物流测试调用点，补齐 `priority: None`。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo fmt --all -- --check`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 runtime::tests::economy_priority_logistics -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 runtime::tests::economy -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p oasis7`。
- 遗留事项：
  - 执行 T3：落地 `maintenance_sink` 持续消耗并更新默认 profile 参数。

- 时刻：2026-02-28 12:12:44 CST
- 完成内容：完成“M4 资源产业链可玩性优先强化”T3（`maintenance_sink` 持续消耗接线 + 默认参数更新）。
  - 更新 `crates/oasis7/src/runtime/world/event_processing/action_to_event_economy.rs`：
    - 新增 `merge_recipe_consume_with_maintenance_sink`，将 `ProductProfile.maintenance_sink` 按产出数量折算后并入配方实际消耗；
    - `ScheduleRecipe` 的库存校验、账本回退、报价观测、bottleneck 推断、事件 `consume` 字段统一改为基于“合并后消耗”。
  - 更新 `crates/oasis7/src/runtime/world/bootstrap_economy.rs`：
    - M4 默认 `ProductProfile` 为中后期产品补齐 `maintenance_sink` 首版参数；
    - `factory_core` 额外接入 `alloy_plate` 维护消耗。
  - 更新 `crates/oasis7/src/runtime/tests/economy_priority_logistics.rs`：新增 `schedule_recipe_applies_product_maintenance_sink_to_consume`，验证维护消耗并入排产消耗。
  - 更新 `crates/oasis7/src/runtime/tests/economy_bootstrap.rs`：补充 `hardware_part` 初始库存并同步断言（`alloy_plate/hardware_part` 余额）。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo fmt --all -- --check`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 runtime::tests::economy_priority_logistics -- --nocapture`。
  - test_tier_full：`env -u RUSTC_WRAPPER cargo test -p oasis7 --features "wasmtime test_tier_full" runtime::tests::economy_bootstrap -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p oasis7`。
- 遗留事项：
  - 执行 T4：完成项目文档状态收口并结项提交。

- 时刻：2026-02-28 12:15:42 CST
- 完成内容：完成“M4 资源产业链可玩性优先强化”T4（回归收口与结项）。
  - 更新项目管理文档：`doc/world-simulator/m4/m4-resource-product-system-playability-priority-hardening-2026-02-28.project.md`。
  - 状态收敛为“已完成（T0~T4）”，本轮任务结项。
- 测试：
  - 文档收口任务，无新增代码测试（T1~T3 已完成 required/full 回归）。
- 遗留事项：
  - 无（当前项目文档内任务已全部完成）。

- 时刻：2026-02-28 12:37:00 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T0（建档）。
  - 新建设计文档：`doc/testing/s10-five-node-real-game-soak.md`。
  - 新建项目管理文档：`doc/testing/s10-five-node-real-game-soak.project.md`。
  - 项目状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/testing/s10-five-node-real-game-soak.md doc/testing/s10-five-node-real-game-soak.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/testing/s10-five-node-real-game-soak.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/testing/s10-five-node-real-game-soak.project.md`。
- 遗留事项：
  - 执行 T1：实现 `scripts/s10-five-node-game-soak.sh` 五节点编排脚本。

- 时刻：2026-02-28 12:46:07 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T1（五节点编排脚本落地）。
  - 新增脚本：`scripts/s10-five-node-game-soak.sh`（`single` 拓扑五进程编排）。
  - 实现能力：
    - 五节点启动/停止、输出目录管理与节点级日志/命令留存；
    - full-mesh gossip + full-mesh libp2p replication 配置；
    - 固定五验证人集合注入（`35/20/20/15/10`）；
    - 指标聚合并输出 `run_config.json`、`timeline.csv`、`summary.json`、`summary.md`、`failures.md`（失败时）；
    - 门禁覆盖 `stall/lag_p95/distfs_failure_ratio/invariant` 与真实数据交换信号（`settlement_report.total_distributed_points`、`minted_records`）；
    - 支持 `--help`、`--dry-run`、`--llm/--no-llm`、阈值参数。
  - 更新项目管理文档：`doc/testing/s10-five-node-real-game-soak.project.md`，T1 勾选完成，状态推进至 T2。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/s10-five-node-game-soak.sh`。
  - test_tier_required（帮助输出）：`./scripts/s10-five-node-game-soak.sh --help`。
  - test_tier_required（闭环干跑）：`./scripts/s10-five-node-game-soak.sh --dry-run --no-prewarm --duration-secs 60 --out-dir .tmp/s10_game_longrun`。
- 遗留事项：
  - 执行 T2：将 S10 接入 `testing-manual.md` 并明确与 S9 的边界与触发口径。

- 时刻：2026-02-28 12:48:45 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T2（`testing-manual.md` 接线）。
  - 更新 `testing-manual.md`：
    - 套件目录由 `S0~S9` 扩展为 `S0~S10`；
    - 新增 `S10` 章节（命令、产物、门禁、与 S9 的职责边界）；
    - 更新改动路径触发矩阵，增加 `scripts/s10-five-node-game-soak.sh` 路径规则；
    - 更新高风险建议、证据目录、执行剧本和模板中的 S10 引用。
  - 更新项目管理文档：`doc/testing/s10-five-node-real-game-soak.project.md`，T2 勾选完成，状态推进至 T3。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l testing-manual.md`。
  - test_tier_required（S10 接线检查）：`rg -n "S0~S10|S10：五节点真实游戏数据在线长跑套件|scripts/s10-five-node-game-soak.sh|s10_game_longrun" testing-manual.md`。
- 遗留事项：
  - 执行 T3：完成脚本级验证收口、项目文档结项和日志收口。

- 时刻：2026-02-28 12:50:35 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T3（脚本验证与结项收口）。
  - 脚本级验证完成：`syntax/help/dry-run` 三项均通过。
  - 更新项目管理文档：`doc/testing/s10-five-node-real-game-soak.project.md`，T3 勾选完成，状态收敛为“已完成（T0~T3）”。
  - 本轮 S10 建设任务结项。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/s10-five-node-game-soak.sh`。
  - test_tier_required（帮助输出）：`./scripts/s10-five-node-game-soak.sh --help`。
  - test_tier_required（闭环干跑）：`./scripts/s10-five-node-game-soak.sh --dry-run --no-prewarm --duration-secs 60 --out-dir .tmp/s10_game_longrun`。
- 遗留事项：
  - 无（当前项目文档内任务已全部完成）。
- 时刻：2026-02-28 12:04:06 CST
- 完成内容：完成“step completion ack”文档收口任务（T6）。
  - 更新 `doc/world-simulator/viewer-step-completion-ack-2026-02-28.project.md`：T1~T6 全部标记完成。
  - 汇总当前结论：`step` 完成语义已由协议 ack 稳定化（`advanced` / `timeout_no_progress`）；A/B 仍暴露 live 侧真实无推进窗口。
- 遗留事项：无（本设计范围内任务已闭环）；后续若继续优化 B 段成功率，建议立项独立任务。

- 时刻：2026-02-28 12:30:39 CST
- 完成内容：完成“Viewer 产业链图谱化/分层符号化/语义缩放”任务（5 项落地 + 回归）。
  - 文档：新增设计文档 `doc/world-simulator/viewer-industry-graph-layered-symbolic-zoom-2026-02-28.md` 与项目文档 `doc/world-simulator/viewer-industry-graph-layered-symbolic-zoom-2026-02-28.project.md`。
  - 统一中间层：新增 `crates/oasis7_viewer/src/industry_graph_view_model.rs`，集中实现节点/边/层级/状态/根因链/语义缩放切片，替代原有散落文本聚合。
  - 文本层改造：
    - `ui_text_industrial.rs`、`ui_text_economy.rs`、`ui_text_ops_navigation.rs` 改为消费图谱模型。
    - 运营导航输出根因链：`Reject -> Shortage -> Congestion -> Stall`，并附 `target=` 跳转对象标识。
  - 主图层与符号：`world_overlay.rs` 接入图谱边渲染（material/electricity/data 三类流、吞吐线宽、流型颜色），新增节点符号（R1~R5 形态差异、阶段外环、瓶颈/拥塞/告警角标）。
  - 语义缩放：`egui_right_panel.rs` 新增世界/区域/节点三层缩放控件，并联动文本与 overlay 细节级别。
  - 接线：`app_bootstrap.rs` 注入 `IndustrySemanticZoomState`；`main.rs` 挂载新模块；`ui_text.rs` 统一通过图谱模型入口向三块摘要分发。
  - 本地化：`ui_locale_text.rs` 补充新文案映射。
- 测试：
  - test_tier_required：
    - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer industry_graph_view_model::tests -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer ui_text_industrial::tests -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer ui_text_economy::tests -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer ui_text_ops_navigation::tests -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer world_overlay::tests -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer overlay_section_renders_chunk_legend_and_width_hint -- --nocapture`
  - test_tier_full：
    - `env -u RUSTC_WRAPPER cargo test -p oasis7_viewer`
    - `env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown`
- 遗留事项：
  - 当前根因链 `target=` 已可用于定位标识，但尚未在 card 文本区做直接点击跳转；若需要“一键定位”，建议后续单独接入按钮式交互层。

- 时刻：2026-02-28 13:23:22 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T4（奖励 epoch 时长可控化）。
  - `oasis7_viewer_live` 新增参数 `--reward-runtime-epoch-duration-secs <n>`，支持以正整数覆盖默认 `epoch_duration_seconds`。
  - 奖励运行时接线完成：无状态初始化与状态恢复两条路径均支持 epoch 秒数覆盖。
  - `scripts/s10-five-node-game-soak.sh` 接线参数 `--reward-runtime-epoch-duration-secs`（默认 `60`），并将配置写入 `run_config.json` 与 `summary.md`。
  - 执行五节点真实短跑：`.tmp/s10_game_longrun/20260228-131900`，验证每节点均产出 `3` 份 `epoch-*.json`，且 `reward-runtime-state.json` 中 `epoch_duration_seconds=60`。
  - 更新项目管理文档：`doc/testing/s10-five-node-real-game-soak.project.md`，T4 勾选完成，状态收敛为“已完成（T0~T4）”。
- 测试：
  - test_tier_required（CLI 解析默认值）：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live parse_options_defaults`。
  - test_tier_required（CLI 自定义值）：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live parse_options_reads_custom_values`。
  - test_tier_required（CLI 参数校验）：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live parse_options_rejects_zero_reward_runtime_epoch_duration_secs`。
  - test_tier_required（脚本语法/帮助/干跑）：`bash -n scripts/s10-five-node-game-soak.sh`、`./scripts/s10-five-node-game-soak.sh --help`、`./scripts/s10-five-node-game-soak.sh --dry-run --no-prewarm --duration-secs 60 --out-dir .tmp/s10_game_longrun`。
  - test_tier_required（五节点真实短跑）：`./scripts/s10-five-node-game-soak.sh --duration-secs 180 --out-dir .tmp/s10_game_longrun --no-prewarm`（验证 epoch 产物通过；当前门禁仍因 `minted_records_empty` 失败）。
- 遗留事项：
  - `minted_records_empty` 门禁失败仍存在（不影响本次 epoch 可控化目标达成），建议后续单独立项排查 mint 前置条件与身份绑定链路。

- 时刻：2026-02-28 14:45:50 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T5（`minted_records_empty` 门禁修复）。
  - 奖励运行时增加 settlement 发布“共识就绪”判定：除 `last_status=Committed` 外，允许 `Pending` 且存在 committed checkpoint（`committed_height>0` 且 `network_committed_height>=committed_height`）的 leader 发布结算。
  - 奖励运行时增加节点身份绑定补齐：启动时预绑定 validator/leader/local/signer；每次结算前对 report 内缺失节点按根密钥派生并补齐绑定，避免 mint 前置校验失败。
  - 验证结果：
    - 首次复跑（`.tmp/s10_game_longrun/20260228-143841`）由于脚本直接使用旧 `target/debug/oasis7_viewer_live` 可执行，仍复现旧门禁失败；
    - 重建二进制后复跑（`.tmp/s10_game_longrun/20260228-144256`）门禁通过，`minted_non_empty_samples=2`，sequencer 节点每 epoch `minted_record_count=3`。
  - 更新项目管理文档：`doc/testing/s10-five-node-real-game-soak.project.md`，新增并完成 T5，状态收敛为“已完成（T0~T5）”。
- 测试：
  - test_tier_required（CLI 回归）：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live parse_options_defaults`。
  - test_tier_required（CLI 回归）：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live parse_options_reads_custom_values`。
  - test_tier_required（mint 预览回归）：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live build_reward_settlement_mint_records_uses_preview_world_without_mutation`。
  - test_tier_required（二进制构建）：`env -u RUSTC_WRAPPER cargo build -p oasis7 --bin oasis7_viewer_live`。
  - test_tier_required（五节点真实短跑，最终验证）：`./scripts/s10-five-node-game-soak.sh --duration-secs 130 --out-dir .tmp/s10_game_longrun --no-prewarm`。
- 遗留事项：
  - 无（S10 当前门禁项已闭环）。

- 时刻：2026-02-28 16:47:02 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T6（`committed_height` 长跑 stall 根因修复）。
  - 根因定位结论：
    - S10 固定 `node_id` 复用 `output/node-distfs/s10-*`，跨 run 恢复旧 PoS pending/广播游标后可进入长期无推进；
    - `oasis7_viewer_live` 单节点拓扑此前对非 sequencer 也挂执行 hook + 执行强约束，复制追平后触发 `execution driver requires contiguous committed heights`，导致共识推进链路被打断；
    - no-LLM 五节点在全量关闭 auto-attest 时易长期停在 pending，需提供可控的基线推进策略。
  - 代码改动：
    - 更新 `crates/oasis7/src/bin/oasis7_viewer_live/world_viewer_live_split_part1.rs`：
      - 单节点/triad/triad_distributed 路径中，仅 sequencer 角色启用执行 hook；
      - 执行强约束（`require_execution_on_commit` / `require_peer_execution_hashes`）改为仅 sequencer 角色启用。
    - 更新 `scripts/s10-five-node-game-soak.sh`：
      - 新增默认节点状态隔离（启动前搬迁历史 `output/node-distfs/s10-*` 到 run 目录 `node_state_backup/`）；
      - 新增 auto-attest 策略开关：`--node-auto-attest-all` / `--node-no-auto-attest-all` / `--node-auto-attest-sequencer-only`（默认 `sequencer_only`）；
      - `run_config.json` / `summary.md` 纳入 `node_auto_attest_mode` 与 `isolate_node_state` 配置落盘。
  - 文档改动：
    - 更新设计文档 `doc/testing/s10-five-node-real-game-soak.md`（新增状态隔离与 auto-attest 策略接口说明）。
    - 更新项目文档 `doc/testing/s10-five-node-real-game-soak.project.md`（新增并完成 T6，状态收敛为 T0~T6 已完成）。
- 测试：
  - test_tier_required（oasis7_viewer_live 角色化执行约束回归）：
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live start_live_node_applies_pos_options -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live start_live_node_starts_triad_distributed_storage_role -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live start_live_node_starts_triad_distributed_sequencer_without_static_peers -- --nocapture`
  - test_tier_required（脚本语法）：`bash -n scripts/s10-five-node-game-soak.sh`。
  - test_tier_required（五节点真实短跑回归）：
    - 失败复现样本（定位阶段）：`./scripts/s10-five-node-game-soak.sh --duration-secs 260 --max-stall-secs 120 --out-dir .tmp/s10_game_longrun --no-prewarm`（run: `20260228-162536`，stall 超阈）；
    - 修复后通过样本：`./scripts/s10-five-node-game-soak.sh --duration-secs 260 --max-stall-secs 120 --out-dir .tmp/s10_game_longrun --no-prewarm`（run: `20260228-164149`，`status=ok`，`max_stall_secs_observed=0`，各节点高度随 epoch 持续增长）。
- 遗留事项：
  - 无（T6 目标闭环，S10 当前基线门禁可稳定通过）。

- 时刻：2026-02-28 17:37:29 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T7（sequencer 执行桥非连续高度容错修复 + 长跑回归）。
  - 代码改动：`crates/oasis7/src/bin/oasis7_viewer_live/execution_bridge.rs`
    - `NodeRuntimeExecutionDriver::on_commit` 在遇到非连续 committed 高度时不再报错退出，改为记录告警并继续处理当前 commit，避免 `requires contiguous committed heights` 导致共识停摆。
    - 新增测试 `node_runtime_execution_driver_tolerates_non_contiguous_commit_heights`，覆盖 `height: 1 -> 3` 提交容错路径。
  - 文档改动：
    - 更新 `doc/testing/s10-five-node-real-game-soak.md`，补充 sequencer 执行桥非连续高度容错策略说明。
    - 更新 `doc/testing/s10-five-node-real-game-soak.project.md`，新增并完成 T7，状态收敛为“已完成（T0~T7）”。
  - 跑测结论：
    - 失败样本（修复前）：`.tmp/s10_game_longrun/20260228-165423`，`metric_gate_failed`，`stall=842s>300s`。
    - 修复验证样本（新二进制）：`.tmp/s10_game_longrun/20260228-172637`，`600s` 长跑 `status=ok`，`metric_gate=pass`，`max_stall_secs_observed=0`，`minted_non_empty_samples=10`。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live node_runtime_execution_driver_tolerates_non_contiguous_commit_heights -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live node_runtime_execution_driver_persists_chain_records -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live start_live_node_applies_pos_options -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live start_live_node_starts_triad_distributed_storage_role -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live start_live_node_starts_triad_distributed_sequencer_without_static_peers -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo build -p oasis7 --bin oasis7_viewer_live`。
  - test_tier_required（真实长跑）：`./scripts/s10-five-node-game-soak.sh --duration-secs 600 --max-stall-secs 300 --out-dir .tmp/s10_game_longrun --no-prewarm`。
- 遗留事项：
  - 无（T7 目标闭环，S10 长跑门禁恢复稳定通过）。

- 时刻：2026-02-28 18:05:21 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T8（结算签名错误清零）。
  - 代码改动：
    - `crates/oasis7/src/bin/oasis7_viewer_live/world_viewer_live_split_part2.rs`
      - reward runtime signer key 改为按 `signer_node_id` 从根密钥派生；
      - `reward_runtime_node_identity_bindings` 统一派生，不再对 signer 使用 root 公钥特例；
      - `ensure_reward_runtime_settlement_node_identity_bindings` 改为严格依赖配置绑定，缺失即报错，避免错误派生。
    - `crates/oasis7/src/bin/oasis7_viewer_live/reward_runtime_network.rs`
      - 结算 envelope 编码改为 CBOR；
      - 解码兼容 CBOR 优先，JSON 回退。
    - `crates/oasis7/src/bin/oasis7_viewer_live/world_viewer_live_tests_split_part2.rs`
      - 新增 signer 派生绑定单测；
      - 新增缺失绑定拒绝单测。
  - 验证结果：
    - 回归运行：`.tmp/s10_game_longrun/20260228-180015`（`260s`，门禁通过）；
    - 五节点 `stderr` 中 `reward runtime apply settlement envelope failed`、`verify settlement signature failed`、`settlement signer public key mismatch` 计数均为 `0`。
- 测试：
  - test_tier_required：
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live reward_runtime_node_identity_bindings_derive_signer_binding_from_root_key -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live ensure_reward_runtime_settlement_node_identity_bindings_rejects_missing_binding -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live build_reward_settlement_mint_records_uses_preview_world_without_mutation -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live settlement_envelope_roundtrip -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live settlement_envelope_signature_verifies -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo build -p oasis7 --bin oasis7_viewer_live`
    - `bash -n scripts/s10-five-node-game-soak.sh`
  - test_tier_required（真实短长跑）：
    - `./scripts/s10-five-node-game-soak.sh --duration-secs 260 --max-stall-secs 120 --out-dir .tmp/s10_game_longrun --no-prewarm`（run: `20260228-180015`）。
- 遗留事项：
  - 执行 T9：将结算 envelope 应用失败率纳入硬门禁并输出到 summary。

- 时刻：2026-02-28 18:19:55 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T9（结算 envelope 应用失败率硬门禁）。
  - 代码改动：
    - `crates/oasis7/src/bin/oasis7_viewer_live/world_viewer_live_split_part2.rs`
      - 增加结算 envelope 应用累计计数：`settlement_apply_attempts_total` / `settlement_apply_failures_total`；
      - 在 epoch 报表 `reward_settlement_transport` 输出 `settlement_apply_attempts_this_tick`、`settlement_apply_failures_this_tick`、`settlement_apply_attempts_total`、`settlement_apply_failures_total`、`settlement_apply_failure_ratio`。
    - `scripts/s10-five-node-game-soak.sh`
      - 新增参数 `--max-settlement-apply-failure-ratio <0~1>`（默认 `0`）；
      - 聚合各节点结算应用累计计数并计算 `settlement_apply_failure_ratio`；
      - 将结算应用失败率纳入硬门禁（并要求 `settlement_apply_attempts > 0`）；
      - `run_config.json` / `summary.json` / `summary.md` / `timeline.csv` 同步输出相关阈值与指标。
  - 文档改动：
    - 更新设计文档：`doc/testing/s10-five-node-real-game-soak.md`（新增参数、报表字段与门禁口径）。
    - 更新项目文档：`doc/testing/s10-five-node-real-game-soak.project.md`（T9 勾选完成，状态推进至 T10）。
  - 验证结果：
    - 旧二进制失败样本：`.tmp/s10_game_longrun/20260228-181108`（`metric_gate=fail`，`settlement_apply_attempts=0`，用于确认硬门禁生效）；
    - 重建二进制并复跑通过：`.tmp/s10_game_longrun/20260228-181651`（`130s`，`status=ok`，`metric_gate=pass`，`settlement_apply_attempts=7`，`settlement_apply_failures=0`，`settlement_apply_failure_ratio=0`）。
    - 五节点 `stderr` 结算相关计数均为 `0`：`reward runtime apply settlement envelope failed` / `verify settlement signature failed` / `settlement signer public key mismatch`。
- 测试：
  - test_tier_required：
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live settlement_envelope_roundtrip -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live settlement_envelope_signature_verifies -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live ensure_reward_runtime_settlement_node_identity_bindings_rejects_missing_binding -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo build -p oasis7 --bin oasis7_viewer_live`
    - `bash -n scripts/s10-five-node-game-soak.sh`
    - `./scripts/s10-five-node-game-soak.sh --dry-run --no-prewarm --duration-secs 60 --out-dir .tmp/s10_game_longrun --max-settlement-apply-failure-ratio 0`
    - `./scripts/s10-five-node-game-soak.sh --duration-secs 130 --out-dir .tmp/s10_game_longrun --no-prewarm --max-settlement-apply-failure-ratio 0`（run: `20260228-181651`）
- 遗留事项：
  - 执行 T10：连续多轮长跑（`3x600s + 1x更长`）并给出发布结论。

- 时刻：2026-02-28 19:17:08 CST
- 完成内容：完成“S10 五节点真实游戏数据在线长跑套件”T10（`3x600s + 1x1200s` 连续长跑稳定性验证与发布收口）。
  - 连续四轮跑测（均 `--no-prewarm`，`--max-settlement-apply-failure-ratio 0`）：
    - run `20260228-182339`（`600s`）：`overall_status=ok`，`metric_gate=pass`，`settlement_apply_attempts=48`，`settlement_apply_failures=0`，`minted_non_empty_samples=10`；
    - run `20260228-183422`（`600s`）：`overall_status=ok`，`metric_gate=pass`，`settlement_apply_attempts=49`，`settlement_apply_failures=0`，`minted_non_empty_samples=10`；
    - run `20260228-184508`（`600s`）：`overall_status=ok`，`metric_gate=pass`，`settlement_apply_attempts=46`，`settlement_apply_failures=0`，`minted_non_empty_samples=10`；
    - run `20260228-185554`（`1200s`）：`overall_status=ok`，`metric_gate=pass`，`settlement_apply_attempts=96`，`settlement_apply_failures=0`，`minted_non_empty_samples=20`。
  - 四轮关键告警核验：
    - 五节点 `stderr` 中 `reward runtime apply settlement envelope failed` / `verify settlement signature failed` / `settlement signer public key mismatch` 均为 `0`。
  - 项目文档收口：
    - 更新 `doc/testing/s10-five-node-real-game-soak.project.md`，T10 勾选完成，项目状态更新为“已完成（T0~T10）”。
- 测试：
  - test_tier_required（连续长跑）：
    - `./scripts/s10-five-node-game-soak.sh --duration-secs 600 --out-dir .tmp/s10_game_longrun --no-prewarm --max-settlement-apply-failure-ratio 0`（run: `20260228-182339`）
    - `./scripts/s10-five-node-game-soak.sh --duration-secs 600 --out-dir .tmp/s10_game_longrun --no-prewarm --max-settlement-apply-failure-ratio 0`（run: `20260228-183422`）
    - `./scripts/s10-five-node-game-soak.sh --duration-secs 600 --out-dir .tmp/s10_game_longrun --no-prewarm --max-settlement-apply-failure-ratio 0`（run: `20260228-184508`）
    - `./scripts/s10-five-node-game-soak.sh --duration-secs 1200 --out-dir .tmp/s10_game_longrun --no-prewarm --max-settlement-apply-failure-ratio 0`（run: `20260228-185554`）
  - test_tier_required（结果核验）：
    - `jq` 读取四轮 `summary.json` 门禁与指标；
    - `grep -c` 统计四轮五节点 `stderr` 关键告警计数。
- 遗留事项：
  - 无（S10 T0~T10 全部闭环）。

- 时刻：2026-02-28 19:26:56 CST
- 完成内容：按用户要求基于 `doc/game-test.md` 执行一轮“真实玩家”Playwright Web 闭环游玩并填写测试卡片。
  - 执行 `./scripts/run-game-test-ab.sh`，完成启动、试玩、A/B 探针、录屏与证据导出。
  - 新增卡片：`doc/playability_test_result/card_2026_02_28_19_22_20.md`。
  - 同步项目管理文档：`doc/game-test.project.md`（新增并完成 T25、补充本轮测试记录与风险观测）。
- 测试：
  - test_tier_required（Web 闭环试玩 + 录屏 + 量化）：`./scripts/run-game-test-ab.sh`
  - test_tier_required（产物核验）：`sed -n '1,260p' output/playwright/playability/20260228-192220/ab_metrics.md`
  - test_tier_required（控制台核验）：`sed -n '1,260p' .playwright-cli/console-2026-02-28T11-25-01-946Z.log`
- 遗留事项：
  - `step` 二次调用仍存在 accepted 但无推进（B 段 FAIL）；后续需继续治理 `step` 推进一致性与“无推进时的下一步建议”提示。

- 时刻：2026-02-28 19:38:45 CST
- 完成内容：按用户要求输出“游戏设计者视角”的可执行任务清单（目标指标 + 验收标准 + A/B 实验项）。
  - 新增设计文档：`doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.md`。
  - 新增项目管理文档：`doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.project.md`。
  - 清单聚焦控制可预期性：`step` 二次调用不推进的可解释反馈、无进展恢复 CTA、首局动作发现、A/B 实验止损规则。
- 测试：
  - test_tier_required（文档结构检查）：`rg -n "## 目标|## 范围|## 接口/数据|## 里程碑|## 风险" doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.md`
  - test_tier_required（项目文档结构检查）：`rg -n "## 任务拆解|## 依赖|## 状态" doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.project.md`
  - test_tier_required（文档行数约束）：`wc -l doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.md doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.project.md`
- 遗留事项：
  - 进入实现阶段时，优先执行 P0 三项并用 `scripts/run-game-test-ab.sh` 做 10 轮回归，先验证 B 段通过率是否达到门槛（`>=80%`）。

- 时刻：2026-02-28 19:50:50 CST
- 完成内容：执行“Viewer 控制可预期性改版”P0.1（控制反馈语义面板改版）。
  - `web_test_api.rs`：控制阶段语义从 `applied/blocked` 混用调整为 `completed_advanced/completed_no_progress`，并保留 `received/executing/blocked`。
  - `egui_right_panel_player_guide.rs`：Mission HUD 控制反馈卡片改为四态展示（Received/Executing/Completed/Blocked），其中 `completed_no_progress` 以 Completed 标签 + 预警色展示。
  - `egui_right_panel_player_mission_tests.rs`：更新阶段标签映射测试，覆盖 `completed_advanced`、`completed_no_progress` 与 legacy `applied` 兼容。
  - 更新项目文档：`doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.project.md`（T6~T10 完成）。
- 测试：
  - test_tier_required（viewer 定向单测）：`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer player_control_stage_label_maps_core_states -- --nocapture`
  - test_tier_required（wasm 编译检查）：`env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown`
- 遗留事项：
  - 下一任务执行 P0.2：把无进展恢复 CTA（`>2.5s`）从数据层提示推进到玩家可操作交互闭环，并做 A/B 回归验证。
- 时刻：2026-02-28 19:17:54 CST
- 完成内容：完成“启动器链路重构：链运行时与游戏进程解耦”T0（建档）。
  - 新建设计文档：`doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.md`。
  - 新建项目管理文档：`doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.project.md`。
  - 项目状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.md doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.project.md`。
- 遗留事项：
  - 执行 T1：新增 `oasis7_chain_runtime`（节点启动/停止、状态 API、余额 API）。

- 时刻：2026-02-28 19:31:30 CST
- 完成内容：完成“启动器链路重构：链运行时与游戏进程解耦”T1（新增 `oasis7_chain_runtime`）。
  - 新增二进制：`crates/oasis7/src/bin/oasis7_chain_runtime.rs`。
  - 落地能力：
    - 独立启动 NodeRuntime（支持 `node_id/world_id/role/tick/validator/gossip` 参数）；
    - 默认写入独立执行目录 `output/chain-runtime/<node_id>/...`；
    - 提供 HTTP 接口：`/healthz`、`/v1/chain/status`、`/v1/chain/balances`；
    - `balances` 接口读取 execution world，输出节点 credits、main token 账户余额与 recent mint records。
  - 更新项目管理文档：`doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.project.md`，T1 勾选完成，状态推进至 T2。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_chain_runtime`。
- 遗留事项：
  - 执行 T2：改造 `oasis7_game_launcher` 默认托管 `oasis7_chain_runtime`，并将 `oasis7_viewer_live` 切换为 `--no-node`。

- 时刻：2026-02-28 19:36:48 CST
- 完成内容：完成“启动器链路重构：链运行时与游戏进程解耦”T2（`oasis7_game_launcher` 默认托管链运行时）。
  - 更新 `crates/oasis7/src/bin/oasis7_game_launcher.rs`：
    - 启动器新增链运行时编排：默认启动 `oasis7_chain_runtime` 并监控其生命周期；
    - `oasis7_viewer_live` 启动参数新增 `--no-node`，将节点逻辑从游戏进程链路拆出；
    - CLI 新增链配置项：`--chain-enable/--chain-disable`、`--chain-status-bind`、`--chain-node-*`；
    - 新增环境变量解析：`OASIS7_WORLD_CHAIN_RUNTIME_BIN`；
    - `wait_until_ready` 新增链状态 HTTP 就绪检查。
  - 更新项目管理文档：`doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.project.md`，T2 勾选完成，状态推进至 T3。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_game_launcher --bin oasis7_chain_runtime`。
- 遗留事项：
  - 执行 T3：更新发行打包脚本与桌面启动器参数透传，确保发行入口完整承接链运行时配置。

- 时刻：2026-02-28 19:42:54 CST
- 完成内容：完成“启动器链路重构：链运行时与游戏进程解耦”T3（发行链路与桌面启动器参数透传）。
  - 更新 `scripts/build-game-launcher-bundle.sh`：
    - 打包产物纳入 `bin/oasis7_chain_runtime`；
    - `run-client.sh` 注入 `OASIS7_WORLD_CHAIN_RUNTIME_BIN`；
    - README 与产物摘要同步新增链运行时说明。
  - 更新 `crates/oasis7_client_launcher/src/main.rs`：
    - GUI 增加链运行时配置项（enable/status bind/node id/world id/role/tick/validators）；
    - 参数构建新增 `--chain-*` 透传；
    - 补充链配置解析与参数构建单元测试。
  - 更新项目管理文档：`doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.project.md`，T3 勾选完成，状态推进至 T4。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7_client_launcher -- --nocapture`。
  - test_tier_required：`bash -n scripts/build-game-launcher-bundle.sh`。
  - test_tier_required：`./scripts/build-game-launcher-bundle.sh --help`。
  - test_tier_required：`./scripts/build-game-launcher-bundle.sh --dry-run --profile dev --out-dir output/release/game-launcher-dryrun`。
- 遗留事项：
  - 执行 T4：完成收口测试与项目结项。

- 时刻：2026-02-28 19:45:11 CST
- 完成内容：完成“启动器链路重构：链运行时与游戏进程解耦”T4（回归与结项收口）。
  - 更新项目管理文档：`doc/world-simulator/launcher-chain-runtime-decouple-2026-02-28.project.md`。
  - 状态收敛为“已完成（T0~T4）”，当前项目结项。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_game_launcher --bin oasis7_chain_runtime`。
  - test_tier_required：`target/debug/oasis7_game_launcher --help`。
  - test_tier_required：`target/debug/oasis7_chain_runtime --help`。
- 遗留事项：
  - 无（本项目任务已全部完成）。

- 时刻：2026-02-28 20:00:52 CST
- 完成内容：执行“Viewer 控制可预期性改版”P0.2（无进展恢复 CTA 交互闭环）。
  - `web_test_api.rs`：移除 `step` 卡住后的自动 `play` 代跑；在 `>=2.5s`（150 帧）无增量时统一落 `completed_no_progress`，并输出恢复指引。
  - `egui_right_panel_player_guide.rs`：Mission HUD 的 Control Feedback 卡片在 `completed_no_progress` 时显示可操作恢复按钮（`Recover: play` / `Retry: step x8`）。
  - `egui_right_panel_player_guide.rs`：避免与 stuck hint 卡片重复渲染恢复按钮，减少界面干扰。
  - `egui_right_panel_player_mission_tests.rs`：新增恢复 CTA 判定测试。
  - 更新项目文档：`doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.project.md`（T11~T15 完成）。
- 测试：
  - test_tier_required（viewer 定向单测）：`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer player_control_stage_label_maps_core_states -- --nocapture`
  - test_tier_required（viewer 定向单测）：`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer player_control_stage_shows_recovery_actions_only_for_no_progress_completion -- --nocapture`
  - test_tier_required（wasm 编译检查）：`env -u RUSTC_WRAPPER cargo check -p oasis7_viewer --target wasm32-unknown-unknown`
- 遗留事项：
  - 下一任务执行 P0.3：按 `scripts/run-game-test-ab.sh` 做 10 轮回归，验证 B 段通过率门槛（`>=80%`）与无进展窗口目标（`<=2500ms`）。
- 时刻：2026-02-28 20:20:56 CST
- 完成内容：完成“Viewer 控制可预期性改版”P0.3（10 轮 A/B 回归与门槛判定）。
  - 完成 10/10 轮有效样本回归，汇总目录：`output/playwright/playability/p0_3_20260228-201225`。
  - 产出汇总：`p0_3_summary.json`、`p0_3_summary.md`。
  - 门槛判定结果：`B pass rate=70% (7/10, 未达 >=80%)`，`stall>2500ms=100% (10/10)`，`max stall=5892ms`，当前版本未满足发布门槛。
  - 更新项目文档：`doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.project.md`（新增并完成 T16/T17，推进 T18）。
- 测试：
  - test_tier_required（P0.3 10 轮 A/B 回归，headed）：循环执行 `scripts/run-game-test-ab.sh`，汇总写入 `output/playwright/playability/p0_3_20260228-201225/`。
  - test_tier_required（结果核验）：`cat output/playwright/playability/p0_3_20260228-201225/p0_3_summary.json`。
  - test_tier_required（结果核验）：`cat output/playwright/playability/p0_3_20260228-201225/p0_3_summary.md`。
- 遗留事项：
  - 执行 T18（P0.4）：收敛 `completed_no_progress` reason 分类与恢复策略，目标是将 `maxNoProgressWindowMs` 压到 `<=2500ms` 并把 B 段通过率提升到 `>=80%`。
- 时刻：2026-02-28 20:38:12 CST
- 完成内容：推进“Viewer 控制可预期性改版”T18.1（P0.4 诊断复测）。
  - 连续执行 3 轮 headed A/B 复测并留档：
    - `output/playwright/playability/20260228-202833/ab_metrics.json`
    - `output/playwright/playability/20260228-203202/ab_metrics.json`
    - `output/playwright/playability/20260228-203526/ab_metrics.json`
  - 诊断结论：当前共识链路下 `play` 与 `step followup` 仍频繁出现 accepted 但无推进，`maxNoProgressWindowMs` 维持约 `6s`，B 段稳定性未达门槛。
  - 进行过一轮 runtime 试验性改动验证（非共识/共识 drive 保活与补决策邮箱），复测未体现稳定收益，已在本地回滚，未并入主线。
  - 更新项目文档：`doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.project.md`（T18.1 勾选完成，T18.2 待执行）。
- 测试：
  - test_tier_required（真实 A/B 复测，headed）：`./scripts/run-game-test-ab.sh --headed`（共 3 轮）。
  - test_tier_required（定向回归）：
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 live_world_script_event_drive_keeps_requeue_on_idle_step -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 consensus_committed_advances_world_even_when_session_paused -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 step_request_emits_completion_ack_timeout_when_consensus_has_no_commit -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 live_world_llm_event_driven_gate_avoids_repeated_empty_ticks -- --nocapture`
    - `env -u RUSTC_WRAPPER cargo test -p oasis7 live_world_event_drive_gate_tracks_llm_mailbox -- --nocapture`
  - test_tier_required（编译检查）：`env -u RUSTC_WRAPPER cargo check -p oasis7`。
- 遗留事项：
  - 执行 T18.2：针对共识链路 `play` 持续驱动与 `step followup` 推进语义做修复，并重新执行 10 轮门槛回归（目标：`B>=80%`、`maxNoProgressWindowMs<=2500`）。
- 时刻：2026-02-28 20:48:39 CST
- 完成内容：推进“Viewer 控制可预期性改版”T18.2a（A/B 失败原因分类补齐）。
  - 更新 `scripts/run-game-test-ab.sh`：
    - `sendControlProbe` 增加 `failCategory`、`feedbackStage`、`feedbackReason`、`feedbackHint` 采集；
    - `ab_metrics.md` 与 `card_quant_metrics.md` 增加 B 段 primary/followup 失败分类输出。
  - 验证样本：`output/playwright/playability/20260228-204632/ab_metrics.json` 已出现明确分类（如 `timeout_no_delta`、`completed_no_progress`、`rejected`）。
  - 更新项目文档：`doc/world-simulator/viewer-control-predictability-tasklist-2026-02-28.project.md`（T18.2a 完成，T18.2b 待执行）。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/run-game-test-ab.sh`。
  - test_tier_required（真实 A/B 复测，headed）：`./scripts/run-game-test-ab.sh --headed`（run: `20260228-204632`）。
  - test_tier_required（结果核验）：
    - `cat output/playwright/playability/20260228-204632/ab_metrics.json`
    - `cat output/playwright/playability/20260228-204632/ab_metrics.md`
    - `cat output/playwright/playability/20260228-204632/card_quant_metrics.md`
- 遗留事项：
  - 执行 T18.2b：修复共识链路 `play` 持续推进与 `step followup` 一致性（当前该样本 `effectiveControlHitRate=0%`，`maxNoProgressWindowMs=5980`）。
- 时刻：2026-02-28 15:56:47 CST
- 完成内容：完成“Viewer Texture Inspector Art Capture 优化”T0（建档）。
  - 新建设计文档：`doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.md`。
  - 新建项目管理文档：`doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.project.md`。
  - 项目状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.md doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.project.md`。
- 遗留事项：
  - 执行 T1：实现 `--art-capture`、分实体镜头与 `viewer_art.png` 产物。

- 时刻：2026-02-28 16:24:52 CST
- 完成内容：完成“Viewer Texture Inspector Art Capture 优化”T1（脚本实现）。
  - 更新 `scripts/viewer-texture-inspector.sh`：新增 `--art-capture`、`--automation-steps`、`--crop-window` 参数。
  - 新增分实体镜头预设（`agent/location/asset/power_plant/power_storage`）并在 art-capture 模式自动启用。
  - art-capture 模式下默认启用 `use_source_mesh` 与 `director` 体验模式，降低 UI 干扰。
  - 新增产物 `viewer_art.png`（裁切图）；并将 `art_capture/automation_steps/crop_window/viewer_art_capture_status` 写入 `meta.txt`。
  - 更新项目管理文档：T1 完成，状态推进到 T2。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（帮助输出）：`./scripts/viewer-texture-inspector.sh --help`。
  - test_tier_required（最小实跑）：`./scripts/viewer-texture-inspector.sh --inspect agent,location --variants default --scenario llm_bootstrap --art-capture --no-prewarm --out-dir output/texture_inspector/art_capture_smoke_20260228`。
  - test_tier_required（产物检查）：`find output/texture_inspector/art_capture_smoke_20260228 -type f | sort`、`sed -n '1,260p' output/texture_inspector/art_capture_smoke_20260228/*/*/meta.txt`。
- 遗留事项：
  - 执行 T2：完成项目文档结项与日志收口提交。

- 时刻：2026-02-28 16:29:21 CST
- 完成内容：完成“Viewer Texture Inspector Art Capture 优化”T2（验证与收口）。
  - 项目管理文档更新为“已完成（T0~T2）”。
  - 本轮优化任务结项。
- 测试：
  - test_tier_required（沿用 T1 执行结果）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（沿用 T1 执行结果）：`./scripts/viewer-texture-inspector.sh --help`。
  - test_tier_required（沿用 T1 执行结果）：`./scripts/viewer-texture-inspector.sh --inspect agent,location --variants default --scenario llm_bootstrap --art-capture --no-prewarm --out-dir output/texture_inspector/art_capture_smoke_20260228`。
- 遗留事项：
  - 无（当前项目文档内任务已全部完成）。

- 时刻：2026-02-28 18:15:26 CST
- 完成内容：完成“Viewer Texture Inspector Art Capture 优化”T3/T4（closeup 校验增强 + 镜头可读性调参 + 全量复跑）。
  - 更新 `scripts/viewer-texture-inspector.sh`：
    - 新增 closeup 产物链路（`viewer_closeup.png`、`viewer_art_closeup.png`）与 closeup 状态元数据。
    - 新增 `--closeup-automation-steps` 参数与分实体 closeup/fallback 镜头模板。
    - 新增 `power_plant/power_storage` 三变体一致性校验与重试，输出 `variant_validation.txt` 并回写 `meta.txt`。
    - 修复 art-capture 默认镜头“贴脸”问题：将各实体默认 `zoom` 从强近景（<1）重标定为可读近中景（>1）。
  - 更新文档：
    - `doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.md`
    - `doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.project.md`
  - 产物复跑：`output/texture_inspector/closeup_full_20260228_v2`（15/15 组合完整）。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（帮助输出）：`./scripts/viewer-texture-inspector.sh --help`。
  - test_tier_required（烟测）：`./scripts/viewer-texture-inspector.sh --inspect power_plant,power_storage --variants all --scenario llm_bootstrap --art-capture --no-prewarm --out-dir output/texture_inspector/closeup_validation_smoke_20260228_v2`。
  - test_tier_required（全量复跑）：`./scripts/viewer-texture-inspector.sh --inspect all --variants all --scenario llm_bootstrap --art-capture --no-prewarm --out-dir output/texture_inspector/closeup_full_20260228_v2`。
  - test_tier_required（产物完整性）：`find output/texture_inspector/closeup_full_20260228_v2 -mindepth 2 -maxdepth 2 -type d | wc -l`、`find output/texture_inspector/closeup_full_20260228_v2 -name variant_validation.txt -print -exec cat {} \;`。
- 遗留事项：
  - `power_plant` 三变体在肉眼层面的材质差异仍偏弱（虽已通过“非同图”校验）；若需“强可辨识”美术评审图，建议后续补充差异增强渲染口径（例如专用对比灯位/差异图导出）。

- 时刻：2026-02-28 19:21:59 CST
- 完成内容：完成“Viewer Texture Inspector Art Capture 优化”T5（焦点目标 + 灯光口径 + SSIM 门禁）。
  - 更新 `scripts/viewer-texture-inspector.sh`：
    - 新增参数：`--art-lighting`、`--no-art-lighting`、`--variant-ssim-threshold`。
    - power 实体镜头焦点改为实体类型：`focus=first_power_plant` / `focus=first_power_storage`（含 fallback closeup）。
    - art-capture 默认可启用评审灯光口径（`ACES + bloom off + 曝光/环境光/补光/轮廓光`）。
    - 变体校验从“仅哈希去重”升级为“哈希 + SSIM 阈值门禁”，并输出 `retry_reason/min_pair_ssim/ssim_threshold`。
  - 更新文档：
    - `doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.md`
    - `doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.project.md`
  - 产物：
    - smoke：`output/texture_inspector/ssim_lighting_focus_smoke_20260228`
    - full：`output/texture_inspector/ssim_lighting_focus_full_20260228`
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（帮助输出）：`./scripts/viewer-texture-inspector.sh --help`。
  - test_tier_required（smoke）：`./scripts/viewer-texture-inspector.sh --inspect power_plant,power_storage --variants all --scenario llm_bootstrap --art-capture --no-prewarm --out-dir output/texture_inspector/ssim_lighting_focus_smoke_20260228`。
  - test_tier_required（full）：`./scripts/viewer-texture-inspector.sh --inspect all --variants all --scenario llm_bootstrap --art-capture --no-prewarm --out-dir output/texture_inspector/ssim_lighting_focus_full_20260228`。
  - test_tier_required（产物完整性）：`find output/texture_inspector/ssim_lighting_focus_full_20260228 -mindepth 2 -maxdepth 2 -type d | wc -l`。
  - test_tier_required（门禁结果）：`find output/texture_inspector/ssim_lighting_focus_full_20260228 -name variant_validation.txt -print -exec cat {} \;`。
- 遗留事项：
  - `power_plant/power_storage` 在当前材质与光照下仍高相似（`min_pair_ssim` 高于 `0.9995`），已被门禁稳定识别为 `failed_after_retry`；后续需引入“材质对比专用灯位/差异热图”以支持强可辨识评审。

- 时刻：2026-02-28 20:18:34 CST
- 完成内容：完成“Viewer Texture Inspector Art Capture 优化”T6（非 power 场景构图稳定修复）。
  - 更新 `scripts/viewer-texture-inspector.sh`：
    - 新增场景自适应焦点回退：power 实体在 `*power*` 场景使用 `first_power_*`，其余场景回退 `first_location`，避免 `focus` 目标缺失导致自动化步骤中断。
    - 新增 power 镜头姿态模板函数：`resolve_power_hero_pose` / `resolve_power_closeup_pose` / `resolve_power_fallback_closeup_pose`。
    - 为非 power 场景下 power 实体重标定构图参数：
      - hero：`pan=0.4,0,0;zoom=1.6;orbit=20,-30`
      - closeup：`pan=0.6,0,0;zoom=2.2;orbit=20,-30`
      - fallback closeup：`pan=0.6,0,0;zoom=2.6;orbit=20,-30`
  - 更新文档：
    - `doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.md`
    - `doc/scripts/viewer-texture-inspector-art-capture-2026-02-28.project.md`
  - 产物：
    - smoke：`output/texture_inspector/focus_fallback_fix_smoke_v2_20260228`
    - 抽样验证：
      - `output/texture_inspector/focus_fallback_fix_pp_quick_v3_20260228`
      - `output/texture_inspector/focus_fallback_fix_ps_quick_v2_20260228`
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（quick）：
    - `./scripts/viewer-texture-inspector.sh --inspect power_plant --variants default --scenario llm_bootstrap --art-capture --no-prewarm --out-dir output/texture_inspector/focus_fallback_fix_pp_quick_v3_20260228`
    - `./scripts/viewer-texture-inspector.sh --inspect power_storage --variants default --scenario llm_bootstrap --art-capture --no-prewarm --out-dir output/texture_inspector/focus_fallback_fix_ps_quick_v2_20260228`
  - test_tier_required（smoke）：`./scripts/viewer-texture-inspector.sh --inspect power_plant,power_storage --variants all --scenario llm_bootstrap --art-capture --no-prewarm --out-dir output/texture_inspector/focus_fallback_fix_smoke_v2_20260228`。
  - test_tier_required（产物检查）：
    - `find output/texture_inspector/focus_fallback_fix_smoke_v2_20260228 -mindepth 2 -maxdepth 2 -type d | wc -l`
    - `find output/texture_inspector/focus_fallback_fix_smoke_v2_20260228 -name variant_validation.txt -print -exec cat {} \;`
- 遗留事项：
  - 当前镜头构图可读性已修复，但 `power_plant/power_storage` 三变体仍被 SSIM 门禁判定为高相似（`failed_after_retry`）；后续需从材质资产或差异可视化（热图/对比图）层面继续优化可辨识度。

- 时刻：2026-02-28 20:51:37 CST
- 完成内容：完成“Viewer Texture Inspector 材质可辨识增强”T0（建档）。
  - 新建设计文档：`doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.md`。
  - 新建项目管理文档：`doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.project.md`。
  - 项目状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.md doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.project.md`。
- 遗留事项：
  - 执行 T1：实现 `--preview-mode lookdev`，补齐 meta 记录并跑 smoke。

- 时刻：2026-02-28 20:54:38 CST
- 完成内容：完成“Viewer Texture Inspector 材质可辨识增强”T1（`--preview-mode lookdev` 落地）。
  - 更新 `scripts/viewer-texture-inspector.sh`：新增 `--preview-mode <scene_proxy|lookdev>` 参数。
  - 在 `lookdev` 模式下显式关闭 location 视觉干扰：
    - `OASIS7_VIEWER_LOCATION_SHELL_ENABLED=0`
    - `OASIS7_VIEWER_LOCATION_RADIATION_GLOW=0`
    - `OASIS7_VIEWER_LOCATION_DAMAGE_VISUAL=0`
  - 更新 `meta.txt` 输出字段：`preview_mode` 与 lookdev 三个开关状态。
  - 更新项目管理文档：`doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.project.md`，状态推进至“执行 T2”。
- 测试：
  - test_tier_required（语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（帮助输出）：`./scripts/viewer-texture-inspector.sh --help | rg -n "preview-mode|scene_proxy|lookdev"`。
  - test_tier_required（lookdev smoke）：`./scripts/viewer-texture-inspector.sh --inspect power_plant --variants default --art-capture --preview-mode lookdev --viewer-wait 8 --out-dir output/texture_inspector/material_recognizability_t1_smoke_20260228`。
  - test_tier_required（元数据检查）：`nl -ba output/texture_inspector/material_recognizability_t1_smoke_20260228/power_plant/default/meta.txt | sed -n '1,220p'`。
- 遗留事项：
  - 执行 T2：实现 `direct_entity` 预览链路与失败回退元数据。

- 时刻：2026-02-28 20:57:59 CST
- 完成内容：完成“Viewer Texture Inspector 材质可辨识增强”T2（`direct_entity` 预览链路 + 回退元数据）。
  - 更新 `scripts/viewer-texture-inspector.sh`：`--preview-mode` 扩展为 `scene_proxy|lookdev|direct_entity`。
  - 新增 `direct_entity` 路径：非 `location` 实体时直接写入被检实体槽位（`OASIS7_VIEWER_${src_prefix}_*_TEXTURE_ASSET`），不再强制覆写 `LOCATION_*`。
  - 新增模式回退逻辑：`entity=location` 且请求 `direct_entity` 时回退到 `scene_proxy`。
  - 元数据新增字段：`preview_mode_effective`、`preview_mode_fallback_reason`。
  - art-capture 默认 `use_source_mesh` 自动开启改为仅对非 `direct_entity` 模式生效。
  - 更新项目管理文档：`doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.project.md`，状态推进至“执行 T3”。
- 测试：
  - test_tier_required（语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（帮助输出）：`./scripts/viewer-texture-inspector.sh --help | rg -n "preview-mode|direct_entity"`。
  - test_tier_required（direct_entity smoke）：`./scripts/viewer-texture-inspector.sh --inspect asset,location --variants default --art-capture --preview-mode direct_entity --viewer-wait 8 --out-dir output/texture_inspector/material_recognizability_t2_smoke_20260228`。
  - test_tier_required（模式元数据检查）：
    - `nl -ba output/texture_inspector/material_recognizability_t2_smoke_20260228/asset/default/meta.txt | sed -n '1,220p'`
    - `nl -ba output/texture_inspector/material_recognizability_t2_smoke_20260228/location/default/meta.txt | sed -n '1,220p'`
- 遗留事项：
  - 执行 T3：引入 per-entity/per-variant 参数层并接入脚本。

- 时刻：2026-02-28 21:02:02 CST
- 完成内容：完成“Viewer Texture Inspector 材质可辨识增强”T3（per-entity/per-variant 参数层）。
  - 更新 `scripts/viewer-texture-inspector.sh`：新增 `--material-profile <theme_default|art_review_v1>`。
  - 新增参数层函数：
    - `clear_material_overrides`：清理 agent/asset/facility 材质覆盖环境变量。
    - `apply_variant_material_profile`：按 `entity x variant` 写入 roughness/metallic 覆盖值。
  - `art_review_v1` 模式下将 `material_variant_preset_for_run` 固定为 `default`，避免与全局 preset 标量缩放叠加。
  - `meta.txt` 新增字段：`material_profile`、`material_variant_preset_for_run`、各类材质覆盖值。
  - 更新项目管理文档：`doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.project.md`，状态推进至“执行 T4”。
- 测试：
  - test_tier_required（语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（帮助输出）：`./scripts/viewer-texture-inspector.sh --help | rg -n "material-profile|art_review_v1|theme_default"`。
  - test_tier_required（参数层 smoke）：`./scripts/viewer-texture-inspector.sh --inspect agent --variants default,matte,glossy --art-capture --preview-mode direct_entity --material-profile art_review_v1 --viewer-wait 8 --out-dir output/texture_inspector/material_recognizability_t3_smoke_20260228`。
  - test_tier_required（覆盖值检查）：
    - `for v in default matte glossy; do echo "== $v =="; rg -n "variant=|material_profile|material_variant_preset_for_run|material_agent_roughness_override|material_agent_metallic_override" output/texture_inspector/material_recognizability_t3_smoke_20260228/agent/$v/meta.txt; done`
    - `nl -ba output/texture_inspector/material_recognizability_t3_smoke_20260228/agent/default/meta.txt | sed -n '1,120p'`
- 遗留事项：
  - 执行 T4：接入变体级贴图槽位并完成回归。

- 时刻：2026-02-28 21:05:20 CST
- 完成内容：完成“Viewer Texture Inspector 材质可辨识增强”T4（变体级贴图槽位能力 + 小规模回归）。
  - 更新 `scripts/viewer-texture-inspector.sh`：新增变体模板参数：
    - `--base-texture-template`
    - `--normal-texture-template`
    - `--mr-texture-template`
    - `--emissive-texture-template`
  - 新增 `resolve_variant_texture_override`：支持 `{variant}` 占位符展开。
  - 贴图覆写路径由“统一 override”升级为“变体级 override”，同时在 `scene_proxy/direct_entity` 两条链路生效。
  - `meta.txt` 新增模板与实际生效路径字段，支持复盘每变体贴图来源。
  - 更新项目管理文档：`doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.project.md`，状态推进至“执行 T5”。
- 测试：
  - test_tier_required（语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（帮助输出）：`./scripts/viewer-texture-inspector.sh --help | rg -n "texture-template|\\{variant\\}"`。
  - test_tier_required（模板贴图 smoke，三变体）：
    - 预置模板纹理：`mkdir -p .tmp/texture_variant_templates && cp ...`
    - 运行：`./scripts/viewer-texture-inspector.sh --inspect agent --variants default,matte,glossy --art-capture --preview-mode direct_entity --base-texture-template .tmp/texture_variant_templates/base_{variant}.png --normal-texture-template .tmp/texture_variant_templates/normal_{variant}.png --mr-texture-template .tmp/texture_variant_templates/mr_{variant}.png --emissive-texture-template .tmp/texture_variant_templates/emissive_{variant}.png --viewer-wait 8 --out-dir output/texture_inspector/material_recognizability_t4_smoke_20260228`。
  - test_tier_required（模板展开检查）：`for v in default matte glossy; do echo "== $v =="; rg -n "variant=|base_texture_template_override|base_texture_effective_override|normal_texture_effective_override|mr_texture_effective_override|emissive_texture_effective_override" output/texture_inspector/material_recognizability_t4_smoke_20260228/agent/$v/meta.txt; done`。
- 遗留事项：
  - 执行 T5：完善评审矩阵与门禁收口，并结项。
- 时刻：2026-02-28 19:59:51 CST
- 完成内容：完成“Viewer 与节点彻底拆分”T0（建档）。
  - 新建设计文档：`doc/world-simulator/viewer-node-hard-decouple-2026-02-28.md`。
  - 新建项目管理文档：`doc/world-simulator/viewer-node-hard-decouple-2026-02-28.project.md`。
  - 项目状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/world-simulator/viewer-node-hard-decouple-2026-02-28.md doc/world-simulator/viewer-node-hard-decouple-2026-02-28.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/world-simulator/viewer-node-hard-decouple-2026-02-28.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/world-simulator/viewer-node-hard-decouple-2026-02-28.project.md`。
- 遗留事项：
  - 执行 T1：重构 `oasis7_viewer_live` 为纯 viewer 入口并清理 CLI 节点参数接线。

- 时刻：2026-02-28 20:05:15 CST
- 完成内容：完成“Viewer 与节点彻底拆分”T1（代码重构）。
  - 重写 `crates/oasis7/src/bin/oasis7_viewer_live.rs` 为纯 viewer 启动入口，不再 include 旧的内嵌节点分块实现。
  - 新 `oasis7_viewer_live` 仅保留 `scenario/--bind/--web-bind/--no-web-bind/--llm/--no-llm` 参数。
  - 对 `--node-*`、`--topology`、`--triad-*`、`--reward-runtime-*`、`--no-node`、`--viewer-no-consensus-gate` 增加显式报错，并提示迁移到 `oasis7_chain_runtime`。
  - 更新 `crates/oasis7/src/bin/oasis7_game_launcher.rs`：启动 viewer 时不再传递 `--topology/--viewer-no-consensus-gate/--no-node`。
  - 更新项目管理文档：`doc/world-simulator/viewer-node-hard-decouple-2026-02-28.project.md`，T1 勾选完成，状态推进到 T2。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live --bin oasis7_game_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_viewer_live --bin oasis7_game_launcher --bin oasis7_chain_runtime`。
- 遗留事项：
  - 执行 T2：完成 required 回归收口、项目文档结项、任务日志补齐并提交。

- 时刻：2026-02-28 20:07:55 CST
- 完成内容：完成“Viewer 与节点彻底拆分”T2（回归收口与结项）。
  - 验证 `oasis7_viewer_live` 已切换为纯 viewer CLI（help 输出仅保留 viewer 参数，旧节点参数显式拒绝并给出迁移提示）。
  - 验证 `oasis7_game_launcher` 不再向 viewer 传递 `--topology/--viewer-no-consensus-gate/--no-node`。
  - 更新项目管理文档：`doc/world-simulator/viewer-node-hard-decouple-2026-02-28.project.md`，状态收敛为“已完成（T0~T2）”。
- 测试：
  - test_tier_required：`target/debug/oasis7_viewer_live --help`。
  - test_tier_required：`target/debug/oasis7_viewer_live --no-node`（预期报错，提示迁移到 `oasis7_chain_runtime`）。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_viewer_live --bin oasis7_game_launcher`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_viewer_live --bin oasis7_game_launcher --bin oasis7_chain_runtime`。
- 遗留事项：
  - 若要物理删除历史未编译节点源码（`oasis7_viewer_live/*` 旧实现），建议单独立项做归档/清理，避免一次性删除影响追溯与复盘。

- 时刻：2026-02-28 20:14:40 CST
- 完成内容：完成“启动链路脚本迁移”T0（建档）。
  - 新建设计文档：`doc/testing/launcher-chain-script-migration-2026-02-28.md`。
  - 新建项目管理文档：`doc/testing/launcher-chain-script-migration-2026-02-28.project.md`。
  - 项目状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/testing/launcher-chain-script-migration-2026-02-28.md doc/testing/launcher-chain-script-migration-2026-02-28.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/testing/launcher-chain-script-migration-2026-02-28.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/testing/launcher-chain-script-migration-2026-02-28.project.md`。
- 遗留事项：
  - 执行 T1：迁移 `run-game-test.sh` 与 `viewer-release-qa-loop.sh` 到 `oasis7_game_launcher`。

- 时刻：2026-02-28 20:19:41 CST
- 完成内容：完成“启动链路脚本迁移”T1（日常脚本迁移到 `oasis7_game_launcher`）。
  - 更新 `scripts/run-game-test.sh`：
    - 启动命令从 `oasis7_viewer_live + run-viewer-web.sh` 切换为单进程 `oasis7_game_launcher`；
    - 启动参数改为 `--scenario/--live-bind/--web-bind/--viewer-host/--viewer-port/--no-open-browser`，`--with-llm` 按开关透传；
    - 保持产物路径兼容：继续输出 `oasis7_viewer_live.log` 与 `web_viewer.log`（后者写入迁移说明占位内容），`session.meta` 保留原字段并新增 `LAUNCHER_PID`。
  - 更新 `scripts/viewer-release-qa-loop.sh`：
    - 启动命令迁移为 `oasis7_game_launcher`；
    - `--with-consensus-gate` 改为显式 deprecated no-op 提示，避免误导；
    - 保留 `release-qa-live-*.log` / `release-qa-web-*.log` 产物约定。
  - 更新项目文档：`doc/testing/launcher-chain-script-migration-2026-02-28.project.md`，T1 勾选完成并推进到 T2。
- 测试：
  - test_tier_required：`bash -n scripts/run-game-test.sh scripts/viewer-release-qa-loop.sh`
  - test_tier_required：`./scripts/run-game-test.sh --help`
  - test_tier_required：`./scripts/viewer-release-qa-loop.sh --help`
- 遗留事项：
  - 执行 T2：为 `s10-five-node-game-soak.sh`、`p2p-longrun-soak.sh` 增加启动前显式阻断与迁移指引。

- 时刻：2026-02-28 20:22:29 CST
- 完成内容：完成“启动链路脚本迁移”T2（长跑脚本显式阻断）。
  - 更新 `scripts/s10-five-node-game-soak.sh`：
    - `usage` 增加 `Status: blocked (2026-02-28)` 提示；
    - 新增 `legacy_chain_entrypoint_block`，在参数解析后立即失败并输出迁移方向（`oasis7_chain_runtime` / `oasis7_game_launcher`）。
  - 更新 `scripts/p2p-longrun-soak.sh`：
    - `usage` 增加 `Status: blocked (2026-02-28)` 提示；
    - 新增 `legacy_chain_entrypoint_block`，在参数解析后立即失败并输出迁移方向（`oasis7_chain_runtime` / `oasis7_game_launcher`）。
  - 更新项目文档：`doc/testing/launcher-chain-script-migration-2026-02-28.project.md`，T2 勾选完成并推进到 T3。
- 测试：
  - test_tier_required：`bash -n scripts/s10-five-node-game-soak.sh scripts/p2p-longrun-soak.sh`
  - test_tier_required：`./scripts/s10-five-node-game-soak.sh`（预期 `rc=1` 且输出迁移阻断提示）
  - test_tier_required：`./scripts/p2p-longrun-soak.sh`（预期 `rc=1` 且输出迁移阻断提示）
  - test_tier_required：`./scripts/s10-five-node-game-soak.sh --help`
  - test_tier_required：`./scripts/p2p-longrun-soak.sh --help`
- 遗留事项：
  - 执行 T3：更新手册与文档中旧启动命令口径，完成项目收口。

- 时刻：2026-02-28 20:26:47 CST
- 完成内容：完成“启动链路脚本迁移”T3（文档收口与项目结项）。
  - 更新 `doc/testing/web-ui-playwright-closure-manual.md`：
    - S6 启动命令从 `oasis7_viewer_live + run-viewer-web.sh` 迁移为单入口 `oasis7_game_launcher`；
    - 启动失败分诊与清理约定改为 `oasis7_game_launcher` 口径；
    - 步骤编号重排，避免迁移后层级混乱。
  - 更新 `testing-manual.md`：
    - L4 默认链路改为 `oasis7_game_launcher + Playwright`；
    - S9/S10 改为“迁移守卫”状态，不再给出可执行但已失效的长跑命令；
    - 改动路径矩阵与高风险充分度标准同步到迁移后口径。
  - 更新项目文档：`doc/testing/launcher-chain-script-migration-2026-02-28.project.md`，T3 勾选完成，状态收敛为“已完成（T0~T3）”。
- 测试：
  - test_tier_required：`rg -n "oasis7_viewer_live --|--topology single --viewer-no-consensus-gate|run-viewer-web.sh --address|trunk serve" testing-manual.md doc/testing/web-ui-playwright-closure-manual.md`
  - test_tier_required：`rg -n "oasis7_game_launcher|迁移守卫|blocked \(2026-02-28\)" testing-manual.md doc/testing/web-ui-playwright-closure-manual.md doc/testing/launcher-chain-script-migration-2026-02-28.project.md`
  - test_tier_required：`bash -n scripts/run-game-test.sh scripts/viewer-release-qa-loop.sh scripts/s10-five-node-game-soak.sh scripts/p2p-longrun-soak.sh`
- 遗留事项：
  - 当前项目无遗留（T0~T3 已全部完成）；后续如需恢复 S9/S10 在线长跑，需单独立项实现基于 `oasis7_chain_runtime` 的新编排脚本。

- 时刻：2026-02-28 20:30:15 CST
- 完成内容：完成“基于 oasis7_chain_runtime 的长跑脚本可用化”T0（建档）。
  - 新建设计文档：`doc/testing/chain-runtime-soak-script-reactivation-2026-02-28.md`。
  - 新建项目管理文档：`doc/testing/chain-runtime-soak-script-reactivation-2026-02-28.project.md`。
  - 状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required：`wc -l doc/testing/chain-runtime-soak-script-reactivation-2026-02-28.md doc/testing/chain-runtime-soak-script-reactivation-2026-02-28.project.md`
  - test_tier_required：`rg -n "## 目标|## 范围|## 接口 / 数据|## 里程碑|## 风险" doc/testing/chain-runtime-soak-script-reactivation-2026-02-28.md`
  - test_tier_required：`rg -n "## 任务拆解|## 依赖|## 状态" doc/testing/chain-runtime-soak-script-reactivation-2026-02-28.project.md`
- 遗留事项：
  - 执行 T1：改造 `scripts/s10-five-node-game-soak.sh` 为可运行的 `oasis7_chain_runtime` 五节点脚本。

- 时刻：2026-02-28 20:39:17 CST
- 完成内容：完成“基于 oasis7_chain_runtime 的长跑脚本可用化”T1（S10 脚本可执行化）。
  - 重写 `scripts/s10-five-node-game-soak.sh`：从旧 `oasis7_viewer_live --node-*` 链路切换为 5 进程 `oasis7_chain_runtime` 编排。
  - 恢复可执行闭环：多节点启动/停止、`/healthz` 就绪检查、`/v1/chain/status` + `/v1/chain/balances` 轮询采样、`timeline.csv`/`summary.json`/`summary.md`/`failures.md` 产物输出。
  - 保留兼容参数与产物契约：继续接受 `--scenario/--llm/--reward-runtime-*`（记录为兼容字段），并保留既有 summary/timeline 文件名与门禁结构。
  - 更新项目文档：`doc/testing/chain-runtime-soak-script-reactivation-2026-02-28.project.md`（T1 勾选完成，状态推进至 T2）。
- 测试：
  - test_tier_required：`bash -n scripts/s10-five-node-game-soak.sh`
  - test_tier_required：`./scripts/s10-five-node-game-soak.sh --help`
  - test_tier_required：`./scripts/s10-five-node-game-soak.sh --dry-run --no-prewarm --duration-secs 20 --poll-interval-secs 1 --startup-timeout-secs 3 --out-dir .tmp/s10_runtime_dryrun_test`
  - test_tier_required（短窗实跑）：`./scripts/s10-five-node-game-soak.sh --no-prewarm --duration-secs 12 --startup-timeout-secs 10 --poll-interval-secs 2 --max-stall-secs 120 --max-lag-p95 100 --out-dir .tmp/s10_runtime_smoke_test`
- 遗留事项：
  - 执行 T2：改造 `scripts/p2p-longrun-soak.sh` 为 `oasis7_chain_runtime` 版本并保留 chaos 注入。

- 时刻：2026-02-28 20:51:54 CST
- 完成内容：完成“基于 oasis7_chain_runtime 的长跑脚本可用化”T2（P2P 脚本可执行化，含 chaos）。
  - 重写 `scripts/p2p-longrun-soak.sh`：从旧 `oasis7_viewer_live --topology/--triad-*` 链路切换为多实例 `oasis7_chain_runtime` 编排。
  - 保留核心入口与产物契约：`profile/topologies/chaos-plan/continuous-chaos` 参数族延续，输出 `run_config.json`、`timeline.csv`、`summary.json`、`summary.md`、`failures.md`、`chaos_events.log`。
  - 指标采样改为轮询 `status/balances` API；在有 chaos 事件时，将短暂 `running_false/http_failure/height_non_monotonic` 降级为告警，避免重启窗口误判。
  - 更新项目文档：`doc/testing/chain-runtime-soak-script-reactivation-2026-02-28.project.md`（T2 勾选完成，状态推进至 T3）。
- 测试：
  - test_tier_required：`bash -n scripts/p2p-longrun-soak.sh`
  - test_tier_required：`./scripts/p2p-longrun-soak.sh --help`
  - test_tier_required：`./scripts/p2p-longrun-soak.sh --dry-run --no-prewarm --topologies triad,triad_distributed --duration-secs 30 --out-dir .tmp/p2p_runtime_dryrun_test`
  - test_tier_required（短窗实跑，无 chaos）：`./scripts/p2p-longrun-soak.sh --no-prewarm --profile soak_smoke --topologies triad --duration-secs 8 --startup-timeout-secs 8 --poll-interval-secs 2 --max-stall-secs 120 --max-lag-p95 100 --out-dir .tmp/p2p_runtime_smoke_test`
  - test_tier_required（短窗实跑，含 chaos restart）：`./scripts/p2p-longrun-soak.sh --no-prewarm --profile soak_smoke --topologies triad --duration-secs 8 --startup-timeout-secs 8 --poll-interval-secs 2 --chaos-plan .tmp/p2p_runtime_chaos_plan_smoke.json --max-stall-secs 120 --max-lag-p95 100 --out-dir .tmp/p2p_runtime_chaos_smoke_test3`
- 遗留事项：
  - 执行 T3：更新 `testing-manual.md` 的 S9/S10 口径并完成结项提交。

- 时刻：2026-02-28 20:55:46 CST
- 完成内容：完成“基于 oasis7_chain_runtime 的长跑脚本可用化”T3（回归收口与结项）。
  - 更新 `testing-manual.md`：
    - S9/S10 从“迁移守卫”改为“可执行长跑套件”，补齐 smoke/endurance 命令与通过标准；
    - 改动路径矩阵同步改为直接要求 S9/S10（按改动面至少一条）；
    - 高风险充分度与风险缓解口径同步为“至少执行一条在线长跑”。
  - 收口修正：更新 `scripts/s10-five-node-game-soak.sh` 门禁判定，`metric_gate=insufficient_data` 不再强制失败，仅作为告警记录。
  - 更新项目文档：`doc/testing/chain-runtime-soak-script-reactivation-2026-02-28.project.md`，T3 勾选完成并结项（T0~T3）。
- 测试：
  - test_tier_required：`bash -n scripts/s10-five-node-game-soak.sh scripts/p2p-longrun-soak.sh`
  - test_tier_required：`./scripts/s10-five-node-game-soak.sh --dry-run --no-prewarm --duration-secs 10 --out-dir .tmp/s10_runtime_dryrun_test_t3`
  - test_tier_required：`./scripts/p2p-longrun-soak.sh --dry-run --no-prewarm --topologies triad --duration-secs 20 --out-dir .tmp/p2p_runtime_dryrun_test_t3`
  - test_tier_required：`rg -n "迁移守卫|阻断|blocked \(2026-02-28\)" testing-manual.md`（期望无命中）
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。

- 时刻：2026-02-28 21:34:13 CST
- 完成内容：完成“game-test 回退短测 + 手动长玩复测”任务（T26）。
  - 已回退上一轮短测提交内容（`card_2026_02_28_21_05_39.md` 与对应项目/日志增量），改为手动长玩流程。
  - 采用 `./scripts/run-game-test.sh` 启动链路，使用 Playwright 手动操作完成约 9 分钟长玩，生成录屏与阶段截图。
  - 新增测试卡片：`doc/playability_test_result/card_2026_02_28_21_22_51.md`。
  - 更新项目管理文档：`doc/game-test.project.md`（新增 T26、补充手动长玩产物与风险观测）。
- 测试：
  - test_tier_full（手动长玩闭环 + 录屏）：`./scripts/run-game-test.sh` + `PLAYWRIGHT_CLI_SESSION=playability-manual-20260228-212251 ./.codex/skills/playwright/scripts/playwright_cli.sh open/video-start/eval/screenshot/video-stop/close`
  - test_tier_required（控制采样核对）：`sed -n '1,260p' output/playwright/playability/20260228-212251-long/manual-control.log`
  - test_tier_required（控制台核对）：`sed -n '1,200p' output/playwright/playability/20260228-212251-long/console-2026-02-28T13-32-20-301Z.log`
- 遗留事项：
  - `move` 动作仍被拒绝（`unsupported action: move`），玩家可发现动作集合仍偏窄。
  - 发布模式控制台仍有 trunk 占位符 WS 错误（`ERR_NAME_NOT_RESOLVED`）与 `favicon 404` 噪音，建议继续治理，降低“误判不可玩”概率。

- 时刻：2026-02-28 21:44:49 CST
- 完成内容：完成“game-test A/B 控制动作选项清理”任务（T27）。
  - 更新 `scripts/run-game-test-ab.sh`：移除 `phase_b_move_probe`（`sendControl("move", ...)`）探针。
  - A/B 流程保持 `play/pause/step` 三类有效动作探针，不再主动触发 `unsupported action: move`。
  - 更新项目管理文档：`doc/game-test.project.md`（新增 T27，补充本轮修正结论与状态）。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/run-game-test-ab.sh`
  - test_tier_required（帮助输出）：`./scripts/run-game-test-ab.sh --help`
  - test_tier_required（确认已无 move 探针）：`rg -n "phase_b_move_probe|moveProbe|unsupported action: move" scripts/run-game-test-ab.sh`（无命中）
- 遗留事项：
  - `move` 在 `web_test_api.sendControl` 中仍是非法动作（合法集合为 `play/pause/step`）；如需支持移动控制，需要单独定义控制协议并补齐端到端语义。

- 时刻：2026-02-28 22:08:57 CST
- 完成内容：完成“game-test 清理 trunk 占位符 WS 噪音”任务（T28）。
  - 更新 `crates/oasis7/src/bin/oasis7_game_launcher.rs`：静态服务返回 `index.html` 时，自动剥离 trunk 自动热更新脚本（包含 `.well-known/trunk/ws` 的脚本块）。
  - 目的：发布链路不再尝试连接 trunk 热更新端点，避免持续刷 `ERR_NAME_NOT_RESOLVED` / `ERR_CONNECTION_REFUSED` 噪音。
  - 补充单测：新增 `sanitize_index_html_for_embedded_server_*` 用例，覆盖“移除 reload 脚本”和“非 index 文件不受影响”。
  - 更新项目管理文档：`doc/game-test.project.md`（新增 T28，并同步状态/风险结论）。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_game_launcher`
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_game_launcher`
  - test_tier_required：`rg -n "well-known/trunk/ws|__TRUNK_ADDRESS__" crates/oasis7/src/bin/oasis7_game_launcher.rs`
- 遗留事项：
  - `favicon 404` 噪音仍存在（静态目录未提供 `favicon.ico`），可后续单独补资源或在页面显式禁用默认图标请求。

- 时刻：2026-02-28 22:41:58 CST
- 完成内容：完成“A/B 脚本防误用标注”任务（T29）。
  - 更新 `scripts/run-game-test-ab.sh`：新增 Guardrail 注释与 `--help` 说明，明确该脚本仅用于自动化回归哨兵（量化探针），不替代手动长玩与真实玩家卡片结论。
  - 更新 `scripts/run-game-test-ab.sh`：在收尾输出增加提醒（`regression probe only`），降低误把 A/B 结果当最终发布结论的风险。
  - 更新 `testing-manual.md`（S6）：新增防误用条款，明确 `run-game-test-ab.sh` 的边界和发布前仍需执行的手动长玩要求。
  - 更新项目管理文档：`doc/game-test.project.md`（新增 T29，状态/风险同步）。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/run-game-test-ab.sh`
  - test_tier_required（帮助输出）：`./scripts/run-game-test-ab.sh --help`
  - test_tier_required（防误用标注落盘）：`rg -n "Guardrail|Important guardrail|不等价于|发布前结论" scripts/run-game-test-ab.sh testing-manual.md`
- 遗留事项：
  - A/B 指标仍是“回归哨兵”，不能覆盖真实玩家主观体验；发布口径仍需结合手动长玩卡片与录屏证据。

- 时刻：2026-02-28 22:52:05 CST
- 完成内容：完成“按 `doc/game-test.md` 执行真实玩家游玩并填写卡片”任务（T30）。
  - 使用 `./scripts/run-game-test.sh` 启动默认 Web 闭环链路（`ws://127.0.0.1:5011`）。
  - 使用 Playwright（headed）执行一轮手动长玩控制序列并录屏，产物落盘：`output/playwright/playability/20260228-224714-long/`。
  - 新增测试卡片：`doc/playability_test_result/card_2026_02_28_22_47_14.md`。
  - 补充量化与证据文件：
    - `output/playwright/playability/20260228-224714-long/manual_metrics.json`
    - `output/playwright/playability/20260228-224714-long/manual_metrics.md`
    - `output/playwright/playability/20260228-224714-long/console_all_messages_full.log`
  - 更新项目管理文档：`doc/game-test.project.md`（新增 T30、测试记录与本轮风险观测）。
- 测试：
  - test_tier_full（真实玩家闭环 + 录屏）：`./scripts/run-game-test.sh` + `PLAYWRIGHT_CLI_SESSION=game-test-20260228-224626 ./.codex/skills/playwright/scripts/playwright_cli.sh open/video-start/run-code/console/video-stop/close`
  - test_tier_required（产物完整性）：`ls -1 output/playwright/playability/20260228-224714-long/`
  - test_tier_required（卡片落盘）：`test -f doc/playability_test_result/card_2026_02_28_22_47_14.md`
- 遗留事项：
  - `step` 推进一致性仍有波动：本轮 `step_20` 出现 `accepted` 但 `completed_no_progress`（`3/4`，无进展窗口 `9863ms`），需继续治理控制语义与玩家反馈。

- 时刻：2026-02-28 23:00:53 CST
- 完成内容：完成“playability 卡片模板玩家视角优先化”任务（T31）。
  - 更新 `doc/playability_test_card.md`：
    - 将“感谢提示”移动到附录之后，避免读者在附录前误判文档结束；
    - 保持“量化指标 + 链路信息”位于附录（自动回填），主问卷聚焦玩家主观反馈。
  - 更新 `doc/game-test.project.md`：新增 T31 并同步当前阶段与风险说明。
- 测试：
  - test_tier_required（模板段落顺序）：`rg -n "^# 附录$|^## 零、量化指标|^感谢你的测试" doc/playability_test_card.md`
  - test_tier_required（模板行数约束）：`wc -l doc/playability_test_card.md`
  - test_tier_required（项目文档状态同步）：`rg -n "T31|玩家视角优先" doc/game-test.project.md`
- 遗留事项：
  - 无（本次仅模板结构优化，保持字段集合与自动回填口径不变）。

- 时刻：2026-02-28 23:27:06 CST
- 完成内容：完成“按 `doc/game-test.md` 执行真实玩家游玩并填写卡片”任务（T32）。
  - 使用 `./scripts/run-game-test.sh` 启动默认 Web 闭环链路（`ws://127.0.0.1:5011`）。
  - 使用 Playwright（headed）执行一轮深夜手动长玩控制序列并录屏，产物落盘：`output/playwright/playability/20260228-231005-long/`。
  - 新增测试卡片：`doc/playability_test_result/card_2026_02_28_23_27_06.md`。
  - 补充量化与证据文件：
    - `output/playwright/playability/20260228-231005-long/manual_metrics.json`
    - `output/playwright/playability/20260228-231005-long/manual_metrics.md`
    - `output/playwright/playability/20260228-231005-long/manual-control.log`
    - `output/playwright/playability/20260228-231005-long/console_all_messages_full.log`
  - 更新项目管理文档：`doc/game-test.project.md`（新增 T32、测试记录与本轮观测）。
- 测试：
  - test_tier_full（真实玩家闭环 + 录屏）：`./scripts/run-game-test.sh` + `PLAYWRIGHT_CLI_SESSION=playability-manual-20260228-231005 ./.codex/skills/playwright/scripts/playwright_cli.sh open/video-start/eval/screenshot/console/video-stop/close`。
  - test_tier_required（产物完整性）：`ls -1 output/playwright/playability/20260228-231005-long/`。
  - test_tier_required（卡片落盘）：`test -f doc/playability_test_result/card_2026_02_28_23_27_06.md`。
  - test_tier_required（量化结果核对）：`sed -n '1,200p' output/playwright/playability/20260228-231005-long/manual_metrics.md`。
- 遗留事项：
  - 本轮 `A/B` 都通过，但主界面仍缺少“控制结果与推进节奏”的常驻反馈，玩家仍需借助调试信息确认系统状态。

- 时刻：2026-02-28 23:32:35 CST
- 完成内容：完成“game-test 卡片模板对齐修正”任务（T33）。
  - 根因：`card_2026_02_28_23_27_06.md` 采用了旧布局（将“零、量化指标”放在问卷前），未严格对齐最新模板。
  - 修正：按 `doc/playability_test_card.md` 最新结构重排卡片，恢复“主问卷在前、附录与量化在后”的模板顺序。
  - 更新项目管理文档：`doc/game-test.project.md`（新增 T33 并同步状态/修正说明）。
- 测试：
  - test_tier_required（模板顺序核对）：`rg -n "^# 一、理解程度$|^# 附录$|^## 零、量化指标" doc/playability_test_result/card_2026_02_28_23_27_06.md`
  - test_tier_required（模板结尾核对）：`rg -n "感谢你的测试" doc/playability_test_result/card_2026_02_28_23_27_06.md`
  - test_tier_required（文档行数约束）：`wc -l doc/playability_test_result/card_2026_02_28_23_27_06.md doc/game-test.project.md`
- 遗留事项：
  - 无（本次为文档结构修正，不改变测评结论与证据路径）。

- 时刻：2026-02-28 23:38:10 CST
- 完成内容：完成“game-test 归档清理”T0（建档）。
  - 新建设计文档：`doc/game-test-archive-cleanup-2026-02-28.md`。
  - 新建项目管理文档：`doc/game-test-archive-cleanup-2026-02-28.project.md`。
  - 项目状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/game-test-archive-cleanup-2026-02-28.md doc/game-test-archive-cleanup-2026-02-28.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/game-test-archive-cleanup-2026-02-28.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/game-test-archive-cleanup-2026-02-28.project.md`。
- 遗留事项：
  - 执行 T1：归档历史测试卡片并保留主目录活跃样本。

- 时刻：2026-02-28 23:45:11 CST
- 完成内容：完成“game-test 归档清理”T1（历史卡片归档）。
  - 新增归档目录：`doc/playability_test_result/archive/2026-02/`。
  - 将 `2026-02-25~2026-02-27` 共 16 份历史卡片迁入归档目录。
  - 主目录仅保留 2026-02-28 活跃样本（4 份）。
  - 新增入口说明：`doc/playability_test_result/README.md`。
  - 更新项目文档：`doc/game-test-archive-cleanup-2026-02-28.project.md`（T1 勾选完成，状态推进到 T2）。
- 测试：
  - test_tier_required（主目录活跃样本核验）：`ls -la doc/playability_test_result`。
  - test_tier_required（归档落盘核验）：`ls -la doc/playability_test_result/archive/2026-02`。
  - test_tier_required（活跃口径说明存在）：`test -f doc/playability_test_result/README.md`。
- 遗留事项：
  - 执行 T2：归档过时文档并重建 `doc/game-test.project.md` 现行视图。

- 时刻：2026-02-28 23:47:50 CST
- 完成内容：完成“game-test 归档清理”T2（过时文档归档 + 现行视图重建）。
  - 新增项目文档历史快照：`doc/archive/game-test/game-test.project.2026-02-28.pre-archive.md`。
  - 归档旧量化手册：`doc/archive/game-test/playability_test_manual.md`，并将 `doc/playability_test_manual.md` 改为“已归档提示 + 现行入口指引”。
  - 归档阶段性 A/B 文档到 `doc/scripts/archive/`：
    - `run-game-test-ab-no-seek-b-phase-2026-02-27.md`
    - `run-game-test-ab-no-seek-b-phase-2026-02-27.project.md`
  - 重建 `doc/game-test.project.md` 为现行精简视图（活跃样本 + 归档入口 + 当前风险）。
  - 更新项目文档：`doc/game-test-archive-cleanup-2026-02-28.project.md`（T2 勾选完成，状态推进到 T3）。
- 测试：
  - test_tier_required（归档文件存在）：`ls -la doc/archive/game-test`。
  - test_tier_required（脚本文档归档存在）：`ls -la doc/scripts/archive | rg "run-game-test-ab-no-seek-b-phase-2026-02-27"`。
  - test_tier_required（现行项目文档行数约束）：`wc -l doc/game-test.project.md doc/playability_test_manual.md`。
- 遗留事项：
  - 执行 T3：完成回归校验、状态收口与提交。

- 时刻：2026-02-28 23:49:55 CST
- 完成内容：完成“game-test 归档清理”T3（回归校验与结项）。
  - 校验主目录仅保留活跃卡片，历史卡片归档数量与路径正确。
  - 校验 `doc/game-test.project.md` 已切换为现行视图并包含归档入口。
  - 更新项目文档：`doc/game-test-archive-cleanup-2026-02-28.project.md`，状态收敛为“已完成（T0~T3）”。
- 测试：
  - test_tier_required（活跃目录核验）：`ls -1 doc/playability_test_result`。
  - test_tier_required（归档卡片数量核验）：`ls -1 doc/playability_test_result/archive/2026-02 | wc -l`。
  - test_tier_required（归档文档存在）：`ls -1 doc/archive/game-test`。
  - test_tier_required（现行项目文档归档入口）：`rg -n "历史归档|archive/2026-02|项目文档快照|旧量化手册" doc/game-test.project.md`。
  - test_tier_required（文档行数约束）：`wc -l doc/game-test.project.md doc/playability_test_manual.md doc/game-test-archive-cleanup-2026-02-28.project.md`。
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。

- 时刻：2026-02-28 22:15:13 CST
- 完成内容：完成“Viewer Texture Inspector 材质可辨识增强”T5（power 场景门禁收口 + 可追踪性增强）。
  - 更新 `scripts/viewer-texture-inspector.sh`：
    - power 实体镜头链路改为 `wait + select + focus`，避免仅 `focus` 时出现空画面/同图。
    - `direct_entity` 模式下同步关闭 location 干扰层（shell/radiation/damage），减少评审干扰。
    - `meta.txt` 新增可追踪字段：`location_interference_disabled` 与 direct-entity 槽位实际资源（mesh/base/normal/mr/emissive）。
  - 在 `power_bootstrap` 场景执行 direct-entity 回归，门禁由失败变为通过：
    - `power_plant`: `status=passed`, `unique_count_initial=3`, `min_pair_ssim_initial=0.987889`
    - `power_storage`: `status=passed`, `unique_count_initial=3`, `min_pair_ssim_initial=0.990717`
  - 更新项目文档：`doc/scripts/viewer-texture-inspector-material-recognizability-2026-02-28.project.md`（T5 勾选，状态结项）。
- 测试：
  - test_tier_required（语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（帮助输出）：`./scripts/viewer-texture-inspector.sh --help | rg -n "preview-mode|material-profile|closeup-automation-steps|texture-template"`。
  - test_tier_required（power 场景回归）：`./scripts/viewer-texture-inspector.sh --inspect power_plant,power_storage --variants all --scenario power_bootstrap --art-capture --preview-mode direct_entity --material-profile art_review_v1 --no-prewarm --out-dir output/texture_inspector/material_recognizability_t5_power_scene_20260228`。
  - test_tier_required（门禁结果核验）：`find output/texture_inspector/material_recognizability_t5_power_scene_20260228 -name variant_validation.txt -print -exec cat {} \;`。
  - test_tier_required（差异与元数据核验）：
    - `sha256sum output/texture_inspector/material_recognizability_t5_power_scene_20260228/power_plant/*/viewer_art_closeup.png`
    - `sha256sum output/texture_inspector/material_recognizability_t5_power_scene_20260228/power_storage/*/viewer_art_closeup.png`
    - `rg -n "preview_mode_effective|location_interference_disabled|closeup_automation_steps|variant_validation|direct_entity_" output/texture_inspector/material_recognizability_t5_power_scene_20260228/power_plant/default/meta.txt`
- 遗留事项：
  - 当前 power closeup 仍以大面积材质面为主（可辨识已通过门禁），若需更强“造型信息”可在后续独立任务中继续做镜头与构图专项优化。

- 时刻：2026-02-28 22:35:37 CST
- 完成内容：完成“Viewer Texture Inspector 框架合理性治理”T0（建档）。
  - 新建设计文档：`doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.md`。
  - 新建项目管理文档：`doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.project.md`。
  - 明确治理目标：从参数 patch 迁移到“可观测 + 分层门禁 + 回归矩阵”的框架化方案。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.md doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.project.md`。
- 遗留事项：
  - 执行 T1：扩展 viewer capture status 语义可观测字段并补充测试。

- 时刻：2026-02-28 22:44:23 CST
- 完成内容：完成“Viewer Texture Inspector 框架合理性治理”T1（capture status 语义可观测增强）。
  - 更新 `crates/oasis7_viewer/src/internal_capture.rs`：
    - 扩展 status 输出：`selection_kind`、`selection_id`、`camera_mode`、`orbit_radius`、`scene_power_plant_count`、`scene_power_storage_count`。
    - `trigger_internal_capture` 接入 `ViewerSelection`、`ViewerCameraMode`、`Viewer3dScene` 与 `OrbitCamera` 查询，形成语义状态快照。
  - 新增单测 `render_status_dump_includes_selection_and_scene_semantics`，覆盖新增字段。
  - 更新项目文档：`doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.project.md`（T1 完成，推进到 T2）。
- 测试：
  - test_tier_required（单测）：`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer internal_capture -- --nocapture`。
  - test_tier_required（编译检查）：`env -u RUSTC_WRAPPER cargo check -p oasis7_viewer`。
- 遗留事项：
  - 执行 T2：落地 inspector 分层门禁（连接/语义/细节/差异）并写入元数据。

- 时刻：2026-02-28 23:24:47 CST
- 完成内容：完成“Viewer Texture Inspector 框架合理性治理”T2（分层门禁落地收口 + 脚本健壮性修正）。
  - 更新 `scripts/viewer-texture-inspector.sh`：补齐连接/语义/细节/差异四层门禁链路与统一元数据落盘（`selection_gate_*`、`closeup_edge_energy`、`variant_validation_*`）。
  - 新增参数：`--detail-edge-threshold`、`--semantic-gate-mode`；power 关键实体在 `art_capture + direct_entity` 路径启用语义门禁与细节门禁。
  - 框架健壮性修正：`capture_status_value` / `meta_value` 改为缺失 key 时返回空值而非因 `set -euo pipefail` 直接退出，符合“语义字段缺省可退化”设计目标。
- 测试：
  - test_tier_required（脚本语法）：`bash -n scripts/viewer-texture-inspector.sh`。
  - test_tier_required（参数帮助）：`./scripts/viewer-texture-inspector.sh --help`。
  - test_tier_full（power 场景分层回归）：`./scripts/viewer-texture-inspector.sh --inspect power_plant,power_storage --scenario power_bootstrap --art-capture --preview-mode direct_entity --material-profile art_review_v1 --semantic-gate-mode auto --detail-edge-threshold 0.35 --variant-ssim-threshold 0.9995 --no-prewarm --out-dir output/texture_inspector/framework_t2_power_scene_20260228_r2`。
- 遗留事项：
  - 执行 T3：基于本轮回归结果完成“框架合理性结论”文档收口并结项提交。

- 时刻：2026-02-28 23:26:24 CST
- 完成内容：完成“Viewer Texture Inspector 框架合理性治理”T3（power 回归矩阵结论收口 + 文档结项）。
  - 更新设计文档 `doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.md`，新增“T3 回归结论”章节。
  - 更新项目管理文档 `doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.project.md`，T3 勾选完成，状态收敛为“已完成（T0~T3）”。
  - 回归结论：`power_plant` 与 `power_storage` 均呈现“语义门禁通过 + 差异门禁通过 + 细节门禁失败（low_edge_energy）”，证明框架可稳定识别“可连接但不可评审”的视觉问题。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.md doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.project.md`。
  - test_tier_required（设计文档结构 + 结论章节）：`rg -n "目标|范围|接口 / 数据|里程碑|风险|T3 回归结论" doc/scripts/viewer-texture-inspector-framework-rationalization-2026-02-28.md`。
  - test_tier_required（回归证据复核）：`for e in power_plant power_storage; do cat output/texture_inspector/framework_t2_power_scene_20260228_r2/$e/variant_validation.txt; done`。
- 遗留事项：
  - 无（当前项目文档任务已全部完成）。

- 时刻：2026-02-28 23:31:58 CST
- 完成内容：完成“Viewer Texture Inspector 视觉细节系统优化”T0（建档）。
  - 新建设计文档：`doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.md`。
  - 新建项目管理文档：`doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.project.md`。
  - 项目状态设置为“进行中（T0 已完成，执行 T1）”。
- 测试：
  - test_tier_required（文档行数约束）：`wc -l doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.md doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.project.md`。
  - test_tier_required（设计文档结构）：`rg -n "目标|范围|接口 / 数据|里程碑|风险" doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.md`。
  - test_tier_required（项目文档结构）：`rg -n "任务拆解|依赖|状态" doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.project.md`。
- 遗留事项：
  - 执行 T1：实现 viewer 启动层 `OASIS7_VIEWER_PANEL_HIDDEN` 开关并补测试。

- 时刻：2026-02-28 23:36:18 CST
- 完成内容：完成“Viewer Texture Inspector 视觉细节系统优化”T1（viewer 启动层面板隐藏开关）。
  - 更新 `crates/oasis7_viewer/src/app_bootstrap.rs`：
    - 新增 `OASIS7_VIEWER_PANEL_HIDDEN` 解析逻辑（支持 `1/0/true/false/yes/no/on/off`）；
    - 右侧面板初始布局支持基于环境变量覆盖 `panel_hidden`，用于截图构图时隐藏右侧 UI。
  - 新增测试：
    - `parse_env_toggle_supports_on_and_off_values`；
    - `right_panel_layout_state_applies_panel_hidden_override`。
  - 更新项目管理文档：`doc/scripts/viewer-texture-inspector-visual-detail-system-optimization-2026-02-28.project.md`，T1 勾选完成，状态推进至 T2。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer parse_env_toggle_supports_on_and_off_values -- --nocapture`。
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7_viewer right_panel_layout_state_applies_panel_hidden_override -- --nocapture`。
- 遗留事项：
  - 执行 T2：实现 inspector 构图候选策略 + 资源包 + 灯光预设 + 元数据。
- 时刻：2026-02-28 23:09:46 CST
- 完成内容：完成“发布门禁指标策略对齐（正向修复）”T1（runtime + 脚本接线）。
  - 新增 `crates/oasis7/src/bin/oasis7_chain_runtime/reward_runtime_worker.rs`，在 `oasis7_chain_runtime` 进程内启动 reward runtime worker，产出可采样的累计指标与报表。
  - 更新 `crates/oasis7/src/bin/oasis7_chain_runtime.rs`：
    - 新增 reward runtime 参数解析（`--reward-runtime-*`、`--reward-distfs-*`）。
    - `GET /v1/chain/status` 新增 `reward_runtime` 指标快照。
    - 运行主循环新增 worker 健康轮询，worker 异常时进程显式失败。
  - 更新 `scripts/s10-five-node-game-soak.sh`：
    - 启动命令透传 reward runtime 参数；门禁使用 `reward_runtime` 指标。
    - distfs/settlement 比例从“不可用兼容字段”改为真实计数聚合；mint 样本支持 runtime 累计计数兜底。
  - 更新 `scripts/p2p-longrun-soak.sh`：
    - 增加 reward runtime 参数并透传到 `oasis7_chain_runtime`。
    - 采样与门禁改造为 `reward_runtime` 真实指标，聚合每节点最大计数后计算 distfs/settlement 比例。
- 测试：
  - test_tier_required：`bash -n scripts/s10-five-node-game-soak.sh scripts/p2p-longrun-soak.sh`
  - test_tier_required：`env -u RUSTC_WRAPPER cargo check -p oasis7 --bin oasis7_chain_runtime`
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p oasis7 --bin oasis7_chain_runtime`
- 遗留事项：
  - 执行 T2：按发布参数复跑 S9/S10 并确认门禁结论。

- 时刻：2026-02-28 23:09:46 CST
- 完成内容：完成“发布门禁指标策略对齐（正向修复）”T2（发布参数实跑回归）。
  - 复跑 S10：
    - 命令：`./scripts/s10-five-node-game-soak.sh --duration-secs 300 --no-prewarm --max-stall-secs 240 --max-lag-p95 50 --out-dir .tmp/release_gate_s10`
    - 结果：`run_dir=.tmp/release_gate_s10/20260228-222029`，`summary.json.overall_status=ok`，`metric_gate=insufficient_data`（`distfs_metrics_unavailable` 告警）。
  - 复跑 S9（首次）：
    - 命令：`./scripts/p2p-longrun-soak.sh --profile soak_release --topologies triad_distributed --duration-secs 300 --no-prewarm --max-stall-secs 240 --max-lag-p95 50 --max-distfs-failure-ratio 0.1 --chaos-continuous-enable --chaos-continuous-interval-secs 30 --chaos-continuous-start-sec 30 --chaos-continuous-max-events 8 --chaos-continuous-actions restart,pause --chaos-continuous-seed 1772284566 --chaos-continuous-restart-down-secs 1 --chaos-continuous-pause-duration-secs 2 --out-dir .tmp/release_gate_p2p`
    - 结果：`run_dir=.tmp/release_gate_p2p/20260228-223920`，失败原因为 `reward_asset_invariant_violation`。
  - 根因修复：
    - 修复 `scripts/p2p-longrun-soak.sh`，移除将 `running_false/last_error` 反向映射为 `reward_asset_invariant_violation` 的逻辑；该失败现在仅由 `reward_runtime.invariant_ok=false` 触发。
  - 复跑 S9（修复后）：
    - 结果：`run_dir=.tmp/release_gate_p2p/20260228-225152`，`summary.json.overall_status=ok`，`topologies[0].metric_gate.status=pass`。
- 测试：
  - test_tier_required：`bash -n scripts/p2p-longrun-soak.sh scripts/s10-five-node-game-soak.sh`
  - test_tier_required：`./scripts/p2p-longrun-soak.sh --dry-run --no-prewarm --profile soak_smoke --topologies triad_distributed --duration-secs 20 --chaos-continuous-enable --chaos-continuous-max-events 1 --out-dir .tmp/p2p_runtime_dryrun_post_fix`
  - test_tier_required：S10 发布参数实跑（300s）
  - test_tier_required：S9 发布参数实跑（300s，含 continuous chaos）
- 遗留事项：
  - 执行 T3：更新 `testing-manual.md`、项目文档结项并提交。

- 时刻：2026-02-28 23:13:11 CST
- 完成内容：完成“发布门禁指标策略对齐（正向修复）”T3（文档收口与项目结项）。
  - 更新 `testing-manual.md`：
    - 为 S9/S10 增补“发布门禁基线命令（300s）”；
    - 明确 S9 `soak_release` 要求 `topologies[].metric_gate.status=pass`；
    - 明确 S10 `metric_gate=insufficient_data` 仅可作为数据未就绪告警，不能伴随 `metric_gate_failed`。
  - 更新项目文档 `doc/testing/release-gate-metric-policy-alignment-2026-02-28.project.md`：T3 勾选完成，状态收敛为“已完成（T0~T3）”。
- 测试：
  - test_tier_required：`rg -n "发布门禁基线命令|soak_release|metric_gate.status|distfs_metrics_unavailable" testing-manual.md`
  - test_tier_required：`bash -n scripts/p2p-longrun-soak.sh scripts/s10-five-node-game-soak.sh`
- 遗留事项：
  - 无（本项目 T0~T3 已全部完成）。
