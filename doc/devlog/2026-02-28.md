- 时刻：2026-02-28 11:18:44 CST
- 完成内容：完成“Viewer Live step 控制推进稳定性修复”任务（复现 + 修复 + 回归 + 实机复测）。
  - 复现：通过 `./scripts/run-game-test-ab.sh --headless` 复现 `step` 控制“accepted 但无推进”，表现为 B 段 `step` 两次探针存在一条无增量。
  - 根因：`step` 请求将会话置为 paused，而 `ConsensusCommitted` 处理链路此前受 `session.should_emit_event()` 门控，导致已提交动作在 paused 场景可能无法及时推进到 world；同时 step 在 consensus 异步提交下存在瞬时无回放窗口。
  - 代码修复：
    - 更新 `crates/agent_world/src/viewer/live_split_part1.rs`：
      - `handle_consensus_committed` 移除 `session.should_emit_event()` 前置门控（保留 consensus 路径判断），确保 paused 场景也可消费已提交动作。
      - `handle_step_request` 在 consensus 路径新增短时重试（20ms * 30 次）以缓解“提交后瞬时无回放”。
    - 更新 `crates/agent_world/src/viewer/live/tests.rs`：新增 `consensus_committed_advances_world_even_when_session_paused` 回归测试。
  - 文档：
    - 新增设计文档 `doc/world-simulator/viewer-live-step-control-progress-stability-2026-02-28.md`。
    - 新增项目管理文档 `doc/world-simulator/viewer-live-step-control-progress-stability-2026-02-28.project.md`。
- 测试：
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world viewer::live::tests::consensus_committed_advances_world_even_when_session_paused -- --nocapture`
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world viewer::live::tests::live_world_consensus_bridge_applies_only_committed_actions -- --nocapture`
  - test_tier_required：`env -u RUSTC_WRAPPER cargo test -p agent_world viewer::live::tests::step_control_is_deferred_from_request_handler -- --nocapture`
  - test_tier_required：`./scripts/run-game-test-ab.sh --headless`
    - run_id=`20260228-110822`：A PASS / B FAIL（`step_primary` 进、`step_followup` 不进）
    - run_id=`20260228-111419`：A PASS / B FAIL（`step_primary` 不进、`step_followup` 进）
- 遗留事项：
  - 虽然“paused 下 committed 不推进”的路径性问题已修复，但 B 段仍有异步波动；建议下一阶段引入 `step` completion ack（request_id 对齐）并将恢复阈值改为 wall-clock 语义。
