- 日期：2026-02-13
- 完成内容：完成 WRS-3，拆分 `crates/agent_world_builtin_wasm/src/lib.rs`：新增 `crates/agent_world_builtin_wasm/src/rule_body_modules.rs`，承接 move/visibility/transfer/body 规则与状态跟踪逻辑；`lib.rs` 聚焦入口与分发，行数从 1294 降至 900。
- 完成内容：修复拆分后的测试可见性与导入问题（`PositionState` 可见性、`cfg(test)` 导入整理），保持功能行为不变。
- 完成内容：更新项目文档状态：`doc/world-runtime/wasm-runtime-crate-split.project.md`、`doc/world-runtime.project.md`，将 WRS-3/WRS-4 标记完成。
- 完成内容：运行并通过回归：
  - `env -u RUSTC_WRAPPER cargo test -p agent_world_builtin_wasm -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world --features wasmtime agent_default_modules -- --nocapture`
  - `env -u RUSTC_WRAPPER cargo test -p agent_world --test wasm_executor --features wasmtime -- --nocapture`
- 遗留事项：WRS 任务已收口；若继续推进 wasm 运行时能力需新建后续任务拆解。
