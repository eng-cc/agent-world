# 2026-03-12 Devlog

## 00:26:51 CST / producer_system_designer
- 完成内容：延续昨夜制作人试玩，继续以 GUI Agent 复核 launcher 链模式；确认首个失败根因之一为本地 bundle 混版（旧 `world_chain_runtime` 不支持 `--storage-profile`），重建 `output/release/game-launcher-local` 后消除该阻断。进一步验证发现：默认 `chain_node_id=viewer-live-node` 命中旧 `execution_world_dir` 时会触发 `DistributedValidationFailed/latest state root mismatch`，导致链状态退化为 `unreachable`；改用 fresh `chain_node_id=viewer-live-node-fresh-20260312` 后，链可进入 `ready`，`query_explorer_overview` 成功，随后 `start_game` 也可通过。同步新增 launcher 专题三件套并回写 `doc/world-simulator/prd.md`、`doc/world-simulator/project.md` 与 `doc/world-simulator/prd.index.md`，将该问题登记为 `PRD-WORLD_SIMULATOR-034` / `TASK-WORLD_SIMULATOR-103/104`。
- 遗留事项：由 `viewer_engineer` 牵头实现 stale execution world 的结构化识别与 fresh node id 恢复链路；`runtime_engineer` 联审恢复边界，避免破坏历史执行世界数据。

## 09:45:00 CST / viewer_engineer
- 完成内容：完成 `TASK-WORLD_SIMULATOR-104` 实现闭环：`world_web_launcher` 新增 stale execution world 错误识别，命中 `DistributedValidationFailed + latest state root mismatch` 时将链状态提升为 `stale_execution_world`，并在 `/api/state`、`/api/chain/start`、GUI Agent 状态/动作返回中暴露结构化恢复建议（含 `fresh_node_id`、`fresh_chain_status_bind`、`suggested_config`）；新增 GUI Agent `recover_chain` 动作；`agent_world_client_launcher` 同步接入 stale 状态映射、恢复提示与“一键使用 fresh node 恢复”CTA。补齐定向回归测试，验证 stale 状态序列化、GUI Agent 能力暴露以及客户端恢复快照映射。
- 遗留事项：尚未补做新的 GUI Agent 实机截图与 `query_explorer_overview -> start_game` 现场证据；如需正式验收，可在当前实现上继续跑一轮 Web/GUI Agent 闭环复测并补图。

## 10:00:19 CST / qa_engineer
- 完成内容：使用最新本地 `world_web_launcher` + GUI Agent + `agent-browser` 完成一轮真实闭环复验。控制面监听 `127.0.0.1:5413`，默认 `viewer-live-node` 启链后稳定复现 `stale_execution_world`，结构化返回包含 `fresh_node_id=viewer-live-node-fresh-1773280564145` 与 `fresh_chain_status_bind=127.0.0.1:5123`；随后 `recover_chain -> query_explorer_overview -> start_game -> agent-browser 打开 launcher/viewer 页面 -> stop_game -> stop_chain` 全链路成功。现场证据已保存到 `output/manual-playtest/2026-03-12/launcher-console-ready-5413.png`、`output/manual-playtest/2026-03-12/viewer-running-4173.png` 及对应 JSON/快照文件。
- 遗留事项：`start_game` 后日志中仍出现一次 `[game stderr] viewer web bridge error: WebSocket(Protocol(HandshakeIncomplete))`，本次未阻断 Viewer 页面加载与闭环通过，但建议后续单独评估是否为探针/浏览器握手噪音。

## 10:14:00 CST / producer_system_designer
- 完成内容：继续以制作人视角亲自试玩最新提交版本。通过 `world_web_launcher` + GUI Agent 在 `127.0.0.1:5414` 上复现默认链 stale world，并完成 `recover_chain -> start_game`。进入 Web Viewer 后，发现 `agent-browser` 环境下页面标题与钩子正常，但运行在 SwiftShader/WebGL 软件渲染时触发 `wgpu` shader validation panic，浏览器控制台报 `copy_deferred_lighting_id_pipeline` 相关致命错误，导致 Viewer 一直停留在 `connectionStatus=connecting`，无法真正推进 `play/step` 控制；随后按手册切换到 native fallback，执行 `scripts/capture-viewer-frame.sh --scenario llm_bootstrap --addr 127.0.0.1:5131 --auto-focus-target first_agent --automation-steps 'mode=3d;focus=first_agent;zoom=0.8;select=first_agent'`，抓帧状态显示 `connection_status=connected`、`selection_kind=agent`、`selection_id=agent-0`、`snapshot_ready=1`，确认 native 链路下至少能进入场景并聚焦到 agent。
- 遗留事项：当前真正阻断“制作人能在 Web 里继续玩”的不是 stale recovery，而是 Web Viewer 在软件渲染环境的 shader/pipeline 崩溃；建议新开专题，把 `agent-browser` / SwiftShader 下的 Web Viewer 可运行性作为高优先级试玩阻断处理。

## 11:12:00 CST / viewer_engineer
- 完成内容：继续追 producer Web 试玩阻断，补齐 `PRD-WORLD_SIMULATOR-035` / `TASK-WORLD_SIMULATOR-105/106`：1) `crates/agent_world_viewer/src/web_test_api.rs` 新增浏览器 runtime fatal 透出（`window.error` / `unhandledrejection` / 已知渲染 fatal `console.error` / `webglcontextlost`），将致命错误汇总到 `window.__AW_TEST__.getState().lastError`，避免页面长期假停在 `connecting`；2) `scripts/run-game-test-ab.sh` 增加 `lastError` 快失败分支；3) `doc/world-simulator/viewer/viewer-manual.md`、`testing-manual.md` 与模块主文档/索引同步补齐 `test_api=1`、software renderer / SwiftShader 判定与 native fallback 口径。随后做两轮实机分流验证：A. 历史 `4174/5012` 旧进程在当前 headed `agent-browser` 下已不再是 SwiftShader，而是硬件 `ANGLE (Intel/Mesa)`；其失败根因为 `decode error: unknown variant authoritative_batch`，对应旧静态资源/协议漂移。B. 新起 `world_viewer_live --no-llm` + `run-viewer-web.sh`（`127.0.0.1:4176/?ws=ws://127.0.0.1:5014&test_api=1`）后，`agent-browser` 可稳定连上，`sendControl("play")` 与 `sendControl("step", {count:8})` 均返回 `accepted=true`，最终 `getState()` 达到 `connectionStatus=connected`、`logicalTime=17`、`eventSeq=23`、`cameraMode=3d`、`lastError=null`，并产出 `output/playwright/playability/manual-20260312-direct/final.png`。
- 遗留事项：`world_game_launcher` 一键链模式仍可能因旧 `viewer-live-node` execution world 或历史 bundle 混版直接退出；当前“agent-browser 能不能玩”的答案已经明确：在 fresh Web build + direct viewer 链路下可以正常玩，之前卡死不是 agent-browser 误用，而是环境边界（SwiftShader/software renderer）与历史静态资源/链状态混杂导致的复合阻断，后续若要收敛成单命令稳定试玩，还需继续治理 launcher 启动栈的一致性。

## 11:24:30 CST / viewer_engineer
- 完成内容：继续收敛一键试玩栈。确认 `run-game-test.sh` 之前没有把 `--chain-node-id/--chain-status-bind/--chain-disable` 透传给 `world_game_launcher`，导致脚本虽会 fresh build Web 包，但 chain-enabled 场景仍可能复用固定 `viewer-live-node` 命中旧 execution world。已为 `scripts/run-game-test.sh` 增加 fresh `chain_node_id` 默认值（`viewer-live-node-playtest-<RUN_ID>`）、显式 `chain_status_bind` 透传与 `--chain-disable/--chain-node-id/--chain-status-bind` 参数支持，并回写 `PRD-WORLD_SIMULATOR-034` / `TASK-WORLD_SIMULATOR-107/108`。同时修复 `scripts/run-game-test-ab.sh` 在 `set -e` 下对 `wait_for_connected` 结果赋值导致的静默提前退出。随后执行 `./scripts/run-game-test-ab.sh --no-llm` 完成 chain-enabled 实机闭环：产物位于 `output/playwright/playability/20260312-112146/`，`run-game-test.log` 显示 fresh `chain_node_id=viewer-live-node-playtest-20260312-112146`；`ab_metrics.json` 显示 `connectionStatus=connected`、`lastError=null`、`tick 1 -> 21`、`eventSeq 0 -> 27`、`TTFC=18ms`、有效控制命中率 `3/3=100%`，A/B 两阶段均 PASS，证明 stale execution world 已不再阻断 chain-enabled 一键试玩栈。
- 遗留事项：`agent-browser record start` 仍会短暂打断连接，当前脚本已通过 stop/reopen 自动恢复并继续完成闭环，但若后续要把录屏过程也收敛到完全无重连，需要单独排查 `agent-browser` 录屏启动对当前 Viewer WebSocket 会话的影响。

## 11:55:00 CST / qa_engineer
- 完成内容：继续收敛 `agent-browser` 录屏边界。通过稳定直连链路（`world_viewer_live --no-llm` + `run-viewer-web.sh`）验证 `agent-browser record start` 的真实语义是“创建 fresh browser context”；对 Viewer Web 来说，这会把页面重置回冷启动态，并且在当前环境下不会自行恢复连接（25 秒内始终停留 `connectionStatus=connecting`、`tick=0`、`lastError=null`）。进一步确认：为了恢复连接而执行的 `close/open` 也会同时结束该次录屏，因此当前 `playthrough.webm` 只能作为 best-effort pre-sync 片段，不能被当作完整 gameplay 录像。已据此把 `scripts/run-game-test-ab.sh` 改为：录屏一启动就立即 reopen 页面同步会话，不再误等 15 秒，也不再在流程末尾误报“无录屏在进行”；同时输出 `recording_status.txt` 明确说明 `playthrough.webm` 的证据边界。复验 `./scripts/run-game-test-ab.sh --no-llm`（`output/playwright/playability/20260312-115337/`）通过，A/B 两阶段仍为 PASS，`TTFC=19ms`、`tick 1 -> 21`、`eventSeq 0 -> 27`、`lastError=null`。
- 遗留事项：若发布/评审必须要求“不中断的完整视频”，当前需要切换到 `agent-browser` 之外的屏幕录制方案；就现有工具边界而言，截图、`__AW_TEST__` 状态与量化指标仍应作为主证据，`playthrough.webm` 只能作为辅助片段。

## 13:45:00 CST / producer_system_designer
- 完成内容：将启动器试玩入口正式收敛为 bundle-first 口径：新增 `doc/testing/launcher/launcher-bundle-first-playtest-entry-2026-03-12.prd.md` / `.design.md` / `.project.md`，并同步 `testing-manual.md`、`doc/testing/launcher/launcher-manual-test-checklist-2026-03-10.prd.md`、`doc/testing/README.md`、`doc/testing/prd.index.md`、`doc/testing/project.md`。同时为 `scripts/run-game-test.sh` 增加 `--bundle-dir`，使现有 `run-game-test-ab.sh` 可直接透传 bundle 产物入口，不再把“源码态 bootstrap”和“制作人/发布前验收入口”混为一谈。
- 验证记录：`bash -n scripts/run-game-test.sh scripts/run-game-test-ab.sh`、`./scripts/run-game-test.sh --help`、`./scripts/run-game-test-ab.sh --help` 通过；已构建三组对照 bundle：`output/release/game-launcher-bundle-first-20260312`（release fresh web）、`output/release/game-launcher-bundle-first-dev-20260312`（dev fresh web）、`output/release/game-launcher-bundle-first-distreuse-20260312`（dev + 复用 `crates/agent_world_viewer/dist`），并分别执行 `env -u NO_COLOR ./scripts/run-game-test-ab.sh --bundle-dir <bundle> --no-llm --headless`。三组产物均成功走到新 bundle 入口并输出 `Mode: bundle` / `Launcher: <bundle>/run-game.sh`，证据位于 `output/playwright/playability/20260312-132647/`、`output/playwright/playability/20260312-133426/`、`output/playwright/playability/20260312-133853/`；但浏览器内 `window.__AW_TEST__.getState()` 一直停在 `connectionStatus=connecting`、`tick=0`、`lastError=null`，source 模式最新复验 `output/playwright/playability/20260312-134013/` 也复现同一阻断。
- 遗留事项：当前“bundle-first 入口已具备”与“fresh Web 闭环可实际进入游戏”是两件事；前者已完成，后者仍被 fresh/fresh-like Web 构建链路阻断，需由 `viewer_engineer` 继续排查当前 trunk / dist 产物为何在 agent-browser 下持续停在 `connecting`。
