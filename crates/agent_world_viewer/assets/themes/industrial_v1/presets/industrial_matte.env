# shellcheck shell=bash
_theme_preset_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
source "$_theme_preset_dir/industrial_default.env"
export AGENT_WORLD_VIEWER_MATERIAL_VARIANT_PRESET=matte
unset _theme_preset_dir
