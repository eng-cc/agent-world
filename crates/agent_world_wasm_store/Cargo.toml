[package]
name = "agent_world_wasm_store"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
agent_world_wasm_abi = { path = "../agent_world_wasm_abi" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
