# Deprecated sample retained only to prevent stale path references.
#
# `oasis7_viewer_live` no longer supports:
# - `--release-config`
# - any `--node-*` control-plane flags
#
# For P2P release locking, configure `oasis7_chain_runtime` through:
# - `oasis7_game_launcher`
# - `oasis7_web_launcher`
# - `oasis7_client_launcher`
