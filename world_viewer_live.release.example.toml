# Deprecated sample retained only to prevent stale path references.
#
# `world_viewer_live` no longer supports:
# - `--release-config`
# - any `--node-*` control-plane flags
#
# For P2P release locking, configure `world_chain_runtime` through:
# - `world_game_launcher`
# - `world_web_launcher`
# - `oasis7_client_launcher`
