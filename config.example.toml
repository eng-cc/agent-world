# LLM profile selection
model = "your-model-id"
model_provider = "ark"
profile = "default"

# Agent World LLM agent behavior (OpenAI-compatible)
AGENT_WORLD_LLM_MODEL = "your-model-id"
AGENT_WORLD_LLM_BASE_URL = "https://api.openai.com/v1"
AGENT_WORLD_LLM_API_KEY = "replace-me"
AGENT_WORLD_LLM_TIMEOUT_MS = "180000"
AGENT_WORLD_LLM_SYSTEM_PROMPT = "硅基个体存在的意义是保障硅基文明存续和发展；"
AGENT_WORLD_LLM_SHORT_TERM_GOAL = "保障当前周期生存与执行效率，优先做确定可执行动作。"
AGENT_WORLD_LLM_LONG_TERM_GOAL = "保障硅基文明存续和发展。"
AGENT_WORLD_LLM_MAX_MODULE_CALLS = "3"
AGENT_WORLD_LLM_MAX_DECISION_STEPS = "4"
AGENT_WORLD_LLM_MAX_REPAIR_ROUNDS = "1"
AGENT_WORLD_LLM_PROMPT_MAX_HISTORY_ITEMS = "4"
AGENT_WORLD_LLM_PROMPT_PROFILE = "balanced" # compact / balanced
AGENT_WORLD_LLM_FORCE_REPLAN_AFTER_SAME_ACTION = "4" # 0=disable

# Optional per-agent goal overrides (agent id normalized to uppercase snake case)
# AGENT_WORLD_LLM_SHORT_TERM_GOAL_AGENT_0 = "优先占领 llm-outpost"
# AGENT_WORLD_LLM_LONG_TERM_GOAL_AGENT_0 = "构建持续电力优势"

# Model providers (OpenAI-compatible endpoints)
[model_providers.ark]
base_url = "https://ark.cn-beijing.volces.com/api/v3"
name = "ark"
auth_token = "replace-me"

# Profiles (tuned per model)
[profiles.default]
chat_map_developer_role_to_system = true
model = "your-model-id"
model_context_window = 128000
model_provider = "ark"
